// <auto-generated />
// 由 ConfigKit 生成：Level 表的行类型。
// ⚠️ 手改会被下次导出覆盖。要改数据请改源表。

using System;

namespace NBC.Game.Config
{
    /// <summary>Level 表的一行（对应一行数据）。</summary>
    [Serializable]
    public sealed class Config_Level
    {
        public int id;  // 编号（int）
        public string sceneName;  // 场景名(唯一)（string）
        public string displayName;  // 显示名(给玩家看)（string）
        public int monsterWaves;  // 怪物波数（int）
        public int timeLimitSeconds;  // 时限（秒）（int）
        public int bossSkillId;  // 首领技能（可空）（ref:Skill?），**0 表示无引用**（主键永远 >= 1，所以 0 不会被误认）
    }
}
