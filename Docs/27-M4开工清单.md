# M4 开工清单

> 建立：**2026-09-26**（M3 收关同日）。对应需求：`Docs\01-项目需求文档.md`（Q4 优先级）、`Docs\20-M2M5重排期草案.md`。
> M3 的收关报告在 `Docs\26-M3收关报告.md`（含留给 M4 的 7 条）。

---

## 一、M4 是什么（范围一句话）

**M3 把"联机战斗"跑通了（状态同步 + 副本 + 掉落），M4 要把"玩得下去"补齐**：

| 方向 | 内容 | 优先级理由（Q4） |
| --- | --- | --- |
| **S1 事件下发与任务闭环** | 服务端把伤害/死亡事件也发下去；客户端接进 `EventCenter` → 任务进度能涨 | **M3 留下的断口**，不接上"联机打怪"对任务系统等于没发生 |
| **S2 成就** | 与任务**共用同一个 `ConditionTracker`**（`register(resetProgress:false)` = 一直生效） | Q4 把成就排在任务之后，且它与任务是同一套条件结构 |
| **S3 持久化** | MySQL 落库：条件进度 / 任务状态 / 掉落记录（`IConditionProgressStore` 换实现即可） | 需求 §5.2；也是"账号"的前置 |
| **S4 帧同步（2v2）** | PVP 小房间走帧同步（D2 定点数学），与状态同步并存 | Q2 的混合方案里"另一半"；规矩：**状态同步没稳之前不并行做帧同步** |

⚠️ **不做**（本阶段有意留下）：客户端预测/回滚、AOI/分线/分服（Q3 砍掉）、真正的账号系统。

---

## 二、切片（每片一件事，做完再下一片）

| 片 | 内容 | 状态 |
| --- | --- | --- |
| **M4-S1** | **服务端事件 → 客户端事件中心 → 任务条件**（本文件 §三） | ✅ 2026-09-26 |
| **M4-S1b** | **可测性修正**：BOSS 仇恨范围 + 回家、出生点移出仇恨圈、快照带 `owner_player_id`、调试窗自动追击（§八） | ✅ 2026-09-26 |
| M4-S2 | 成就：与任务共用条件系统 + 成就面板 | ✅ 2026-09-26（**负责人实测 684 全绿 + 面板三条成就可见**） |
| **M4-S3** | **持久化：`IConditionProgressStore` 的 MySQL 实现 + 落库时机**（§十一） | 🔨 **数据层完成**（探针 32 全绿；**真库往返待你给密码**）+ `reward_granted` 运行时守卫待做 |
| M4-S4 | 帧同步 2v2：定点数学 + 输入帧广播 + 确定性校验 | 待开工 |

---

## 三、M4-S1 交付清单（2026-09-26）

### 3.1 这件事的起点：M3 收关时**如实画出来的那条断口**

`Docs\图\02-副本·掉落·任务闭环.md` 里那张"任务闭环现状"图本来画着一条虚线：

```
服务端 → NetSession（收下来就完了）        ← 断在这里
本地战斗 → EventCenter → ConditionEventBridge → ConditionTracker → QuestRuntime（已通）
```

⇒ **联机打死怪，任务进度不会涨**。S1 就是把这条虚线接上。

### 3.2 改动（两端各一半，加一座桥）

| 端 | 文件 | 做了什么 |
| --- | --- | --- |
| 服务端 | `Server\NBC.Server.Game\DungeonBattle.cs` | 每次扣血记一条 `DamageEvent`、死亡记一条 `DeathEvent`（**英雄与怪都发**，客户端按 `kind` 分派）；`CopyPendingHits` / `CopyPendingDeaths`（取走即清空，照掉落的形状） |
| 服务端 | `Server\NBC.Server.Game\RoomBattleService.cs` | 每 tick 新增 `BroadcastCombatEvents`：**先伤害、后死亡**（与 M2 本地战斗的因果顺序一致）；抽出 `SendToRoom` —— 快照/战斗事件/掉落**三条广播共用同一份"同一份字节发给全房"**的实现 |
| 服务端 | `Server\NBC.Server.Host\Program.cs` | 退出统计多一行：`事件：伤害 N 条、死亡 N 条、掉落 N 条` |
| 客户端 | `Game\Net\NetSession.cs` | 新增 `DamageReceived` / `DeathReceived` 事件 + `LastDamage` / `LastDeath` / `HitsReceived` / `DeathsReceived`；`HandleServerEvent` 从"只认掉落"改成三个分支 |
| 客户端 | `Game\Battle\ServerEventBridge.cs`（**新增**） | 服务端事件 → `EventCenter`：伤害→`DamageDealt`、死亡按 `kind`→`MonsterDied`/`HeroDied`、掉落**只有归我**才发 `ItemDropped` |
| 客户端 | `Game\Battle\BattleEvents.cs` | 新增 `ItemDropped` + `ItemDroppedPayload` |
| 客户端 | `Game\GameFlow\BattleSession.cs` | 多 Bind 一条：`ItemDropped` → `ConditionEventBridge` 的 `CollectItem` |
| 编辑器 | `Editor\NetDebugWindow.cs` | 新增「任务条件（M4-S1：联机也能涨）」区：进度 N/3、达成回调次数、两座桥的计数、一键重置 |
| 测试 | `Tests\EditMode\Game\ServerEventBridgeTests.cs`（**新增 8 条**） | 见 §四 |
| 测试 | `Tests\EditMode\Game\NetSessionTests.cs`（**+2 条**） | 伤害/死亡事件的记数与转发 |

### 3.3 三条**设计取舍**（都是有理由的，不是随手写）

1. **死亡按 `kind` 分派成两个事件**（`MonsterDied` / `HeroDied`），而不是"发一个带 kind 的、让订阅方自己判"。
   —— 沿用 M2 已经定下的理由（`BattleEvents.cs` 文件头）：合并会让"玩家死了"被当成"击杀了一个目标 0"，
   而 `targetId = 0` 是"任意目标" ⇒ **任何**击杀任务都会涨进度，**静默且看起来很正常**。S1 用一条回归用例把它钉住。
2. **掉落只在"归我"时发游戏事件**：`DropEvent.WinnerPlayerId` 是服务端算的归属（M3 = 击杀者）。
   别人的战利品若也当成"我获得了"，"收集 N 个物品"会被别人的掉落推进 —— 同样是静默的错。
   ⇒ 归属判断放在**产生事件的地方**（桥），订阅方拿到就是"我的"；而且**不重复判断**
   （`BattleSession` 那边只写 `payload => payload.ItemId`，同一规则两处实现迟早不一致）。
3. **`NetSession` 故意不解释 `kind`**（不写"1 就是怪"）：`Game\Net\` 是**引擎无关子集**
   （`Server\_net-probe` 直接编它），而 `EBattleAgentKind` 在 `Game\Battle\` ——
   引它会当场破坏探针的边界（实测见 §五.2）。分派是桥的活，桥住在战斗层。

### 3.4 与"服务端权威"的一致性

客户端**一个随机数都不掷、一次伤害都不算**：伤害多少、谁死了、掉什么，全部来自服务端事件。
`ServerEventBridge` 只做**翻译**（换事件类型、判归属），不做判定。这与 D3/D4 一致。

---

## 四、M4-S1 的验收（可复现的实测）

### 4.1 探针（`Server\_net-probe`）：83 → **88 全绿**

新增第【十三】节 5 条，全部跑在**真 socket + 两个客户端**上：

| 断言 | 证明什么 |
| --- | --- |
| 每次扣血都下发一条伤害事件，两端**逐字段一致** | 伤害事件确实发出去了，且是"同一份字节" |
| `applied` 是**实际扣掉的血**（打 3 次、每次 10，`remaining_hp` = 打之前 − 10） | 语义正确（不是"打出去的伤害值"） |
| 死亡事件带 `kind=1` / `config_id` / `killer_id`，两端一致 | 客户端有足够信息分派与归属 |
| 同一帧里**伤害在前、死亡在后** | 因果顺序契约（表现层不会先看到死、再看到挨打） |
| 服务端计数如实（伤害 = 扣血次数，死亡 = 死的单位数） | 统计不是装饰 |

> ⚠️ 断言里的数字全部是**算出来的**（`damage=10` ×3、`remaining_hp = hpBefore − 10`），不是手抄的。

### 4.2 EditMode：658 → **668**（+10）

| 用例 | 断言 |
| --- | --- |
`MonsterDeath_BecomesMonsterDied` | 怪死 → `MonsterDied`，且 `HeroDied` **没被**触发 |
`HeroDeath_BecomesHeroDied_AndNotMonsterDied` | **M2 那个坑的回归钉**：英雄死绝不能被当成击杀 |
`Damage_BecomesDamageDealt` | `applied=5 / remaining=0` 如实转发（打只剩 5 血的怪） |
`Drop_Mine_BecomesItemDropped` | 归我的掉落 → `ItemDropped` |
`Drop_OfAnotherPlayer_IsNotRaisedAsMine` | 别人的掉落**不发**成"我获得了"；但网络层照样记数 |
`Dispose_StopsForwarding` | 退订后不再转发；重复 Dispose 无害 |
`Constructor_WithoutSession_Throws` | 没给会话当场抛（fail loud） |
`ServerDeathEvents_ProgressQuestCondition` | **端到端**：喂 3 只野狼的死亡事件 → 条件进度 2/3 → 3/3 → 达成回调**只发一次**（蜘蛛 6002 不计入） |
`NetSessionTests.DamageEvent_IsRecordedAndForwarded` | 网络层：记数 / `LastDamage` / 不串到掉落 |
`NetSessionTests.DeathEvent_IsRecordedAndForwarded` | 网络层：`kind` 与 `config_id` 原样带过 |

> 端到端那条跑的是**产品代码的真实链路**（两个桥都是产品代码，不是测试替身）。

### 4.4 客户端实跑验收（**负责人实测，2026-09-26**）

| 项 | 结果 |
| --- | --- |
| 谁跑的 | 项目负责人（Unity 编辑器 + 真实服务端进程） |
| 实测 | 进房 → 打死两只野狼 → **任务完成，达成回调 1 次** |
| 证明什么 | ① 服务端事件 → `ServerEventBridge` → `EventCenter` → `ConditionEventBridge` → `ConditionTracker` **在真客户端里走通**（不是只有探针绿）；② "**达成回调只该发一次**"这条在真实跑动中成立（重复回调会让任务发两次奖） |
| 附带 | 窗口里那条演示条件的目标是**自动算的**（`6001 × 2`，取自快照阵容）—— 见 §七"演示条件的目标不写死" |

> ⚠️ EditMode 用例数要让负责人再跑一次确认（`SnapshotViewTests` +1 ⇒ 期望 **669**）。

### 4.3 编译闸门

| 闸门 | 结果 |
| --- | --- |
| `Server\_api-probe\ApiProbe.csproj`（C# 9 + 真 Unity 程序集，含测试代码） | **0 错误 0 警告** |
| `Server\_net-probe\NetProbe.csproj` | **0 错误 0 警告** |
| `NBC.Server.Host.csproj -t:Compile` | **0 错误 0 警告** |

---

## 五、本轮踩到的坑（都值得记住）

1. **`ServerEvent` 的 oneof 不叫 `Payload`**：信封 `ServerMessage` 是 `PayloadCase`，而事件是 **`EventCase`**
   （因为 `nbc_m3.proto` 里那个 oneof 叫 `event`，生成物加后缀就成了 `EventOneofCase`）。
   写错是 `CS0117`，闸门当场抓到 —— 已把这条写进 `NetSession.HandleServerEvent` 的注释里。
2. **`Game\Net\` 是引擎无关子集，不许引 `Game\Battle\`**：我给 `NetSession` 加了一行
   `using NBC.Game.Battle;`（为了 `EBattleAgentKind`），`_net-probe` 立刻 **CS0234**。
   ⇒ 桥搬到 `Game\Battle\ServerEventBridge.cs`，`NetSession` 只如实转发、不解释 `kind`。
   **判据同 D14：判"能不能进服务端 / 能不能进探针"要看整条依赖链。**
3. **`ToByteArray()` 要 `using Google.Protobuf;`**：新测试第一次编译报 `CS1061`（不是 CS0246）。
   —— 这是本项目第 10 次撞"第三方扩展方法/类型要自己引"这条规律。
4. **MSB3027/MSB3021 又来了**：负责人开着服务端 → Host 的 `bin` 被锁 → 解决方案级构建失败。
   处理办法（老规矩）：**不动他的进程**，用 `-t:Compile` 做只编译校验；整体构建与 Host 重启等他关掉服务端之后再做。

---

## 六、S1 之后立刻要做的两件小事

1. **Host 需要重编一次**（`bin\...\NBC.Server.Host.exe` 还是旧的）：负责人关掉服务端后
   `dotnet build Server\NBC.Server.Host\NBC.Server.Host.csproj -m:1` 再启动，就能看到新的
   `[统计] 事件：…` 一行。（⚠️ 这正是 `Docs\排查手册\06` 记的那个坑：**双击 exe 不会编译**。）
2. **窗口里那条演示条件不是配置表来的**：它是为了让"接线对不对"肉眼可见而**造**出来的
   （`DemoConditionKey = 5001`）。真任务走的是同一个 `ConditionTracker`
   （见 `QuestRuntime`），但"从 `QuestCondition` 表读条件"这一步在 S2/S3 才接。

---

## 七、M4-S1b：可测性修正（2026-09-26，负责人实测反馈）

**反馈原话**：「668 全绿，**玩家老是被 boss 打死，且目标不在范围内，难以测试**」。
先量，再改 —— 量出来的账是这样的（都是真值，不是感觉）：

| 量 | 改前 | 含义 |
| --- | --- | --- |
| BOSS 索敌 | `NearestAliveHero`，**无范围** | 一进房就**全图锁定**你 |
| BOSS 移速 | 90mm/帧（2.7 m/s） | 从 3000mm 外走到你面前约 **1.5 秒** |
| BOSS 伤害 | `Monster.attack`=60 / 15 帧冷却 | **120 dps** ⇒ 1200 血 **10 秒左右**没了 |
| 英雄出生点 | `(-1000, 0)` | 距 BOSS（`-3000,0`）**正好 2000mm = 它的普攻射程** ⇒ **一进房就在它脸上** |
| 快照里的英雄 | 只有 `kind` | 两个客户端**分不出你我** ⇒ 调试工具可能操纵别人的角色 |

### 改了四处（每处都有可复现的验收）

| # | 改动 | 理由与验收 |
| --- | --- | --- |
| 1 | `BossBrain` 加**仇恨范围**（`DungeonBattle.BossAggroRangeMm = 2200mm`）：圈外的英雄视为"没看见" | 语义是"**够得着就追，够不着就回家**"，不是"全图仇恨"。2200 只比射程 2000 大一点 ⇒ 交战由玩家决定。探针：出生点距 BOSS 2500mm > 2200mm，站着不动 BOSS **不动**（状态站桩） |
| 2 | `BossBrain` 加 **leash（回位）**：没目标且离家 > `BossLeashSlackMm = 200mm` 就走回出生点 | 否则"甩掉它"只是让它停在原地，久了会漂到副本门口。探针：把它挪开 2000mm → 英雄跑出圈 → 60 帧后它**回到出生点**（离家 ≤ 200mm、状态站桩） |
| 3 | `HeroSpawnPoint` 的环半径 **1000mm → 500mm** | 四个席位距 BOSS 的最小距离从 2000mm 变成 **2500mm > 仇恨范围 2200mm** ⇒ 进房先安全。⚠️ 一个席位原本**正落在 BOSS 射程边界上**，这就是"老被打死"的直接原因 |
| 4 | 快照加 **`owner_player_id`**（`EntitySnapshot` 第 10 号字段，**兼容改动**，`CONTRACT_VERSION` 保持 1） | 客户端要认出"哪个英雄是我"。**没有它就只能靠 `kind == 0` 猜**，猜错的表现是"调试工具在操纵别人的角色"，**静默且看着正常**（同 M2 那两个事件取舍的教训）。同时把它做成 `SnapshotView.FindHero(playerId)`（纯 C#、可测），**表现层以后也要用** |

### 客户端调试窗（治"目标不在范围内"这个体感）

| 改动 | 为什么 |
| --- | --- |
| 目标改成**离我最近的活怪**（原 `FirstAliveMonsterId` 取"快照里第一只"，而快照顺序是创建顺序 ⇒ 常常指到地图另一头） | 你不用再手填编号、也不会"打了没反应" |
| **显示距离 + 是否在射程内**（`距离 2314mm ❌ 太远，打不到`） | 原来只有一个编号，得自己心算 —— 现象就是"打不出去且没有提示" |
| **「自动追击并攻击」开关**：不在射程就朝目标走，进圈就按一下普攻、再等冷却 | **只是替你按键**：位置/伤害/掉落照样全由服务端算（不是客户端预测）。面板上能看到"已按 N 次普攻 / 冷却剩余 N 帧" |
| 距离用**切比雪夫距离** | 与服务端 `DungeonBattle.DistanceMm` **同一种量法**；用欧氏距离会在四角误判成"还差一点"，于是永远不按攻击 |
| 射程与冷却改成读**共享层** `NBC.Shared.Battle.BattleRules` | 客户端也要这两个数（走到哪儿停、按键节奏）。在窗口里再写一份就会**静默漂移** —— 服务端改射程、客户端还在老位置按键。服务端那两个 `const` 现在是**指向共享层的别名**，只可能有一个真值 |

### 实测（探针 88 → **92 全绿**）

新增/改写的 6 条：出生点在仇恨圈外（用表里的 `spawnRadiusMm` 算）、圈外不追、进圈有反应、
出圈回家（leash）、走进射程才挨打、快照里英雄带 `owner_player_id`（怪是 0）。
EditMode：`SnapshotViewTests` 新增 1 条（**两个英雄时不能认错人**、自己的英雄死了返回 null）。

> ⚠️ **过程中我写错了 4 处用例**（都是断言而不是实现）：`MoveUntilInRange` 写死"往 +X 走"
> （当初是给右边的狼用的）却拿去追左边的 BOSS ⇒ 英雄反向跑；"进圈后距离一定变小"
> （其实英雄常常直接落进射程，那时 BOSS 是**打**而不是**走**）；leash 用例用"引怪"造位移
> （BOSS 一进射程就开打，只挪了 90mm）；`OwnerOfFirstHero` 取"第一个英雄的主人"
> （快照里两个英雄都在，第一个永远是先入职的那个）。
> ⇒ 又一条老教训的复现：**断言里的数字/前提必须算出来**，"看起来会那样"经常不是那样。

### 演示条件的目标**不写死**（2026-09-26 负责人实测："没办法达成 3 只狼的任务目标"）

窗口里那条演示条件是"接线对不对"的可见证据，但它原来写死 **`击杀野狼 × 3`**，
而副本 1001 的 `Dungeon.csv` 只有 `"6001,6001"` ⇒ **目标天生达不成**（我当时顺手写了个 3，
没去对阵容）。现在：

| 改动 | 做法 |
| --- | --- |
| 目标只数 | **从快照里的真实阵容算**：按 `config_id` 分组数怪，优先 `6001`（野狼），没有就取"出现最多的那种怪"。副本 1001 → `6001 × 2`；1002 → `6002 × 3`（平局按编号升序打破，确定性） |
| 为什么不需要"哪个是 BOSS" | BOSS 只有**一只**、小怪是**一批** ⇒ "出现最多的那种怪"天然就是小怪。**不用**给协议再加 `is_boss` 字段 |
| 手填覆盖 | 面板上 `目标只数覆盖（0 = 自动）` + 「按本副本阵容重设」按钮 —— 想测 3/5/10 都不用改代码 |
| 换房间 | 换副本时**按新阵容重新登记**（否则进了蜘蛛洞还在要求杀野狼，又是一个达不成的目标） |
| 登记时机 | 不能在建会话时就登记（那时还没有快照、不知道阵容）⇒ 推迟到**第一张快照之后**（`EnsureDemoConditionRegistered` 每帧试一次） |

> 📌 这是"**断言/目标里的数字必须算出来**"那一族的第 N 次复现：**写死的 3 与真源里的 2 冲突**，
> 而它不会报错 —— 表现只是"任务永远差一只"。规则原来只管"测试里的数字"，
> 这次说明**调试工具的默认值同样要从真源算**（否则工具会撒谎）。

---

## 八、⚠️ 真任务表 ↔ 副本 / 掉落 ↔ 联机事件源，三处对不上（2026-09-26 发现）

S1 实跑验收通过之后，我去读**真任务表**（`Quest.csv` / `QuestCondition.csv`）想接真任务 ——
结果发现现存的 4 个任务**在联机路径下一个都完不成**。三处都是"写了但从没被验证过"的配置：

| # | 表 A 要什么（证据） | 表 B 给什么（证据） | 后果 |
| --- | --- | --- | --- |
| 1 | `QuestCondition.csv` 4001 / 4007：`KillMonster 6001 × 3` | `Dungeon.csv` 1001：`monsters = "6001,6001"`（2 只）；1002 是 3 只 **6002** | "击杀 3 只野狼"**永远达不成**（我窗口里那个写死的 3 大概就是从任务表抄的，当时没核对副本） |
| 2 | `QuestCondition.csv` 4002：`CollectItem 7001 × 2`（狼皮） | `DropTable.csv`：6001 掉 **9001 / 9002**；全表只掉 9001/9002/9003 —— **从不掉 7001** | "带回 2 张狼皮"**永远达不成**（7001 只出现在任务条件与 `Reward` 里） |
| 3 | `QuestCondition.csv` 4004 / 4008：`ReachArea` | 联机路径**没有区域事件源**：`AreaEntered` 只由 M2 的 `BattleSession`（本地战斗）触发，`ServerEventBridge` 只翻译伤害/死亡/掉落 | 3003 / 3004 的"抵达"条件在联机时**永远不涨** |

⚠️ 还有一条更根本的：**没有 `Item.csv`** —— 物品编号（7001/7002/8001/9001…）**没有任何表定义**，
所以"任务收集的物品到底存不存在"这件事，**现在无处可查**。

### 为什么没被更早发现（这是本轮最值钱的一条）

`ConfigKit` 的校验是**单表**的（`range` / `len` / `unique` / `ref:` 指向存在）；
而这四处全是**跨表**（或跨"实现能力"）的约束：

* 任务条件的 `KillMonster X × N` ≤ **某个副本能提供的 X 的数量**？
* `CollectItem I` 的 `I` 是否**出现在某条掉落或奖励里**？
* `eventType` 的每一种，**当前实现里有没有事件源**？（`ReachArea` 在联机路径上就没有）

⇒ **单表校验全绿 ≠ 这张表能被玩通**。这与"闸门绿 ≠ Unity 绿"是同一族：
**判据只覆盖了一半，剩下那一半不报错。**

### 建议的修法（三步，按性价比排序）

| 步 | 做什么 | 为什么这样排 |
| --- | --- | --- |
| ① **对齐表** | `Dungeon.csv` 1001 → `"6001,6001,6001"`（3 只狼，与任务一致）；`DropTable.csv` 让 6001 掉 **7001 狼皮**（例如把 9002 改成 7001，或加一行）；3003/3004 的 `ReachArea` 条件暂时去掉或换成 `KillMonster` | 让"现有任务能被玩通"先成立；改的都是**源 CSV**，重启服务端即生效（窗口的自动目标也会跟着变 3） |
| ② **加跨表校验** | 在 `ConfigKit` 里加三条规则（上表那三问） | **这才是防复发**：不加的话，下次加任务/换副本还会踩同一类坑，而且**静默** |
| ③ **把真任务接进窗口** | 编辑器侧加一个 `IConfigSource`（用 `AssetDatabase` 直读 `.asset`），窗口用**真** `QuestRuntime` 替掉那条演示条件 | 做完才有"真任务在联机里能接取→进度→完成→交付→发奖"的可见证据（也是 M4-S2 成就的复用点） |

### 8.1 已做（2026-09-26，负责人选的就是 ①+②）

| 步 | 做了什么 | 证据 |
| --- | --- | --- |
| **① 对齐表** | `Dungeon.csv` 1001 → `"6001,6001,6001"`（3 只狼）；`DropTable.csv` 新增 `6,6001,7001,1,1,10000`（狼必掉 1 张皮）；`QuestCondition.csv` 删掉两条 `ReachArea`（4004/4008，联机没有事件源），`Quest.csv` 跟着改 `conditionIds`；**另外发现并修掉**：`4003 CollectItem 7002`（草药）也是循环依赖（只在它自己任务的奖励里）⇒ 改成 `KillMonster 6002 × 3`，任务 3002 由「草药采集」改为「清剿蛛群」（**采集类需要"可交互物"系统，M4+ 再做**，已写在表里的 note 列） | `Dungeon.tsv` 现在是 `6001,6001,6001`；`DropTable.tsv` 多了第 6 行 |
| **② 跨表校验** | 新增 `CrossTableChecks`（3 条规则）+ `ConfigChecks.RunAll`（**单表→跨表同一个入口**，流水线与自测都调它）+ 诊断码 `CFG0020/0021/0022` + `ConfigPolicy.EventTypesWithoutSource`（把"实现缺口"做成**数据**） | 见下"实测" |

**实测**：

| 项 | 结果 |
| --- | --- |
| 阳性对照（**未对齐**的真表） | `CFG0020 × 2`（4001/4007 要 3 只、副本只给 2）+ `CFG0021 × 1`（7002 循环依赖）+ `CFG0022 × 1`（ReachArea）＝ **3 错 1 警，且不产出任何文件** |
| 对齐后 | **0 错 0 警告**，写出 36 个文件 |
| ConfigKit 自测 | 49 → **54 条全绿**（新增 5 条：CFG0020 阳/阴、CFG0021 阳/阴、CFG0022） |
| 探针 | **93 全绿**（阵容变了但断言都是从表算的，一条没改） |

⚠️ **过程中我自己踩了三个坑，都值得记**：
1. **C# 字符串里混进半角引号**（`"...这是"还没做"的系统..."`）→ `CS1003` —— 本项目第 6 次同一形态（规矩早有：中文里用「」）；
2. **`CFG0021` 第一版判据太松**（"出现在掉落**或奖励**里"）⇒ 被 `Reward` 里"自己给自己"的两条奖励骗过去，**是自测的阳性对照当场抓出来的** ⇒ 判据收紧成"掉落 **或别的任务**的奖励"；
3. **自测夹具**：`Grid(...)` 是**变参**（一个参数=一行），我把多行条件拼成一个带换行的字符串传进去 ⇒ 只有第一行进了表，现象是"外键指向 4002 但那边没有这个主键"，**看着像检查器坏了**。顺带把自测的 `FirstOf` 失败信息改成**带诊断原文**（原来只报编号，下次还得再猜）。

---

## 九、M4-S1c：把**真任务**接进调试窗口（2026-09-26）

目的：让"任务"这个模块在**联机里**有可见证据 —— 不是只有那条演示条件，而是**读配置表**的
`Quest` / `QuestCondition` / `Reward` 三张表驱动的：**接取 → 进度 → 完成 → 交付 → 发奖**。

| 件 | 做法 | 为什么这样做 |
| --- | --- | --- |
| 编辑器专用的配置来源 | 新增 `Client\Assets\_Project\Editor\EditorConfigSource.cs`：`IConfigSource` 的实现，用 **`AssetDatabase.LoadAssetAtPath`** 直读 `Assets/_Project/Configs/<表>Config.asset` | 运行时的 `AssetConfigSource` 要走 `AssetManager`（YooAsset）；**编辑器里没进 Play、资源层没装配**，那条路走不通。⚠️ 资产目录复用 `ConfigImporter.AssetDirectory`（导入器写、这里读，**只留一个真值**） |
| 真任务的装配 | 窗口直接 `BattleSession.FromConfigMgr(heroId, sink)`，只用它的 `Quests` / `Conditions` | 里面就是"`ConditionTracker` + `ConditionEventBridge` + `QuestRuntime` + `IQuestRewardSink`"那条链 —— **再写一遍接线就是两处实现**（迟早不一致）。本地那个 `BattleWorld` 不参与联机 |
| 界面 | 用真面板的**同一个视图模型** `QuestPanelModel`（`CopyOfferRows` / `CopyTrackingRows` / `HandleAction`） | 窗口与面板**一套逻辑**；按钮名沿用 `Accept_<id>` / `Submit_<id>` 契约 |
| 发奖 | `EditorQuestRewardSink : IQuestRewardSink`（窗口内的嵌套类），把奖励变成界面上一行 + 日志 | 走的是**同一个接缝**；M4 要把它挪到服务端时，只是换一个实现（`IQuestRewardSink` 文件头早就写明这件事） |
| 英雄编号 | **从表里取**（`Hero` 第一行），不写死 | 表是唯一真源；服务端固定 1001 是它那边的事（`DungeonBattle.HeroConfigId`，M4+ 应由服务端下发） |

**用法**：打开窗口 → 配置表自动预加载（`GameTables.All` 7 张）→ 「真任务」区显示**可接取**列表 →
点「接取」→ 去连接/进房打怪（或勾「自动追击并攻击」）→ 回来看到**进度在涨** → 满了点「交付」→
「发奖记录」出现一行。

**验证（我能做的部分）**：
- `ApiProbe` 闸门 **0 错 0 警告**（Editor 脚本在闸门编译范围内）；
- `GameTables.All` 的 **7 张表资产都存在**（`Hero/Skill/Level/Monster/Quest/QuestCondition/Reward`），
  所以窗口不会报"读不到配置资产"；
- 任务链本身的逻辑早被 EditMode 覆盖（`QuestRuntimeTests` 24 条、`ConditionTrackerTests` 27 条、M2 端到端 8 条）。
  ⚠️ **剩下要你实跑确认**：接取 → 打怪涨进度 → 交付 → 发奖（窗口是编辑器程序集，EditMode 测试引用不到它）。

---

## 十、M4-S2 成就（2026-09-26）

### 10.1 一句话：成就与任务**只差三点**

| | 任务（`QuestRuntime`） | 成就（`AchievementRuntime`） |
| --- | --- | --- |
| 登记时机 | 玩家点「接取」时 | **构造时全部登记**（没有接取） |
| `Register` 的 `resetProgress` | `true`（接了才开始算） | **`false`（一直生效、跨局累计）** |
| 结束方式 | 玩家点「交付」才发奖 | **条件一齐当场解锁发奖**（没有交付） |

⇒ 所以本片**没有 `EAchievementState`** —— 成就只有"锁着 / 解锁了"两态。
多一个枚举只会多一处能不同步的状态（`ConditionTracker` 文件头第 ① 条同一个道理）。

### 10.2 本片最容易写错的一处：**登记顺序**（已写成能证伪的用例）

`ConditionTracker` 语义②：`resetProgress: false` 时，**如果注册的那一刻就已经达成，当场回调一次**。
这是**故意**的（"上次已经打了 10 只，这次登录就该解锁"）。但它意味着
**构造函数里的 `Register` 会重入到自己的 `OnConditionMet`**：

```
❌ 登记一条 → 记一条归属
   Register(4009, ..., false) 当场回调 → 回调查 m_ownerOfCondition[4009]
   → 还没记进去 → 认不出这是自己的条件 → 安静 return
   ⇒ 成就永远不解锁，而且一行日志都没有（"看起来像玄学"的典型）

✅ 三段：
   ① 先把全部归属记好
   ② 再逐条登记（此刻重入回调已经认得出来）
   ③ 全部登记完再统一评一遍
```

**第 ③ 步不是冗余**：`AreAllConditionsMet` 走 `m_tracker.IsMet`，
而**还没登记的条件 `IsMet` 恒为 false** ⇒ "两条都早已达成"的成就，
在 ② 的循环里每条单独触发时都凑不齐，只能靠 ③ 收尾。

**这条判据有能证伪的对照**（`Server\_condition-probe` 新增两条，45 → **47 全绿**）：
- `订阅晚了 -> 收不到那次『注册即达成』的通知`（⇒ "先订阅、后登记"是硬要求）
- `归属表要在 Register 之前就绪（否则当场回调会安静丢弃）`

### 10.3 CFG0023：一条条件只能被**一个**持有者引用

`ConditionTracker.Register` 对重复登记**当场抛 `InvalidOperationException`**
（它故意不静默覆盖：静默覆盖会让 bug 永远查不出来）。
⇒ "两个任务共用一条条件"与"任务与成就共用一条条件"都是
**数据单看全绿、炸在运行期**，而且成就**在启动时就登记**，
所以表现是"任务接不了 / 一接就闪退"，跟配置表"看起来"毫无关系。

**这条只有加了成就才完整**：以前只有任务时它就该存在，现在多了 `Quest`×`Achievement` 这一族组合。

### 10.4 顺带修掉 CFG0020 的两处**误报**（被真表逼出来的）

加了 `Achievement` 表之后，CFG0020 立刻在真表上报了两个错 —— 判据本身没错，错在**适用范围**：

| 排除 | 理由 |
| --- | --- |
| 只查**任务**引用的条件 | 成就用 `resetProgress: false`，是**跨局累计**的（"累计击杀 10 只野狼"，一局只刷 3 只也能达成）；任务必须**一局内做完** ⇒ "一局能提供多少"只对任务成立 |
| `targetId = 0` 跳过 | `0` 表示"**任意**怪"，它当然不在任何副本的怪列表里；拿"副本刷不刷这种怪"去判它属于**纯误报** |

> 📌 **"天天误报的警告等于没有警告"** —— 判据要跟着语义走，不能一刀切。

**两条豁免都做了变异测试**（否则"不报"的用例可能是假绿）：

| 变异 | 期望 | 实测 |
| --- | --- | --- |
| 去掉「仅任务」豁免 | 成就那条变红、任意怪那条仍绿 | ✅ 55 通过 1 失败 |
| 两条豁免都去掉 | 两条全红 | ✅ 54 通过 2 失败 |
| 还原 | 58 全绿 | ✅ 58 通过 0 失败 |

### 10.5 交付物

| 件 | 位置 | 说明 |
| --- | --- | --- |
| 成就表 | `Configs\Design\Achievement.csv` | 3 行：9001 初出茅庐→4009、9002 狼王终结者→4010、9003 猎手的直觉→4011 |
| 成就专属条件 | `Configs\Design\QuestCondition.csv` | 4009 `KillMonster 6001 ×10`、4010 `KillMonster 6003 ×1`、4011 `KillMonster 0 ×5` |
| 成就模块 | `Client\Assets\_Project\Game\Achievement\` | **3 文件**：`AchievementTypes` / `AchievementEvents` / `AchievementRuntime` |
| 装配 | `Game\GameFlow\BattleSession.cs` | 加成就表**重载**（老签名保留，M2 那批调用点一行未改）；`FromConfigMgr` 用 `TryGet` 取成就表 |
| 表清单 | `Game\Config\GameTables.cs` | `All` 加 `"Achievement"`（7 → 8 张） |
| 调试窗口 | `Editor\NetDebugWindow.cs` | 新增「成就」区；解锁/失败都写日志 —— 成就**没有玩家操作**，日志是唯一痕迹 |
| 跨表规则 | `Tools\ConfigKit\...\CrossTableChecks.cs` | 新增 CFG0023 + 修 CFG0020 两处误报 |
| 测试 | `Tests\EditMode\Game\AchievementRuntimeTests.cs` | **15 条** |
| 教学留档 | `Docs\教学\M4-A-成就与跨表校验.md` | 七段式 + 类图 + 数据流图（含"最容易画错"表） |

⚠️ 窗口里成就表**没导入**时不会硬失败：会显示一条黄色提示 + 一个「重新加载配置表」按钮
（成就是"锦上添花"，不该因为它没导入就让整局起不来）。而"表没加载"这件事**不静默** ——
`GameTables.PreloadAll` 的失败回调会先报一次。

### 10.6 实测

| 项 | 结果 |
| --- | --- |
| ConfigKit 自测 | 54 → **58 全绿**（跨表共 9 条） |
| ConfigKit 真表 | **0 错 0 警告**，产出 36 → **40 文件** |
| 条件探针 | 45 → **47 全绿** |
| `_net-probe` | **93 全绿**（无回归） |
| 客户端闸门 `_api-probe` | **0 错** |
| `NBC.sln -t:Compile` + Host `-t:Compile` | **0 错 0 警告** |
| EditMode | 669 → **684 全绿**（负责人实测，2026-09-26；+15 = 本片的 `AchievementRuntimeTests`） |

### 10.7 ✅ 负责人实测验收（2026-09-26，**已通过**）

> 负责人原话：**「684全绿、网络调试面板能看到全部三条成就」**

| 项 | 结果 |
| --- | --- |
| EditMode | **684 全绿** |
| 调试面板 | 三条成就**全部可见**（`9001 初出茅庐` / `9002 狼王终结者` / `9003 猎手的直觉`） |
| 配置表导入 | `AchievementConfig.asset` 已生成、`QuestConditionConfig.asset` 已随新条件更新 |

> ⚠️ **我预测的 683 少了 1，原因记下来**：我拿的是"**上一次有记录的**"基线 668，
> 而 668 与这次之间还夹着一条**已经提交但从没人跑过**的 `SnapshotViewTests` +1（M4-S1b 的
> `FindHero_MatchesOwnerPlayerId_NotJustTheFirstHero`）。真实基线是 **669**，669 + 15 = 684 ——
> 增量（+15）我记得是对的，错的是**基线**。
> 📌 教训：**"上次跑过多少"和"仓库里有多少条"是两个数**。写"预计达到 N"时要用**能对上账的基线**
> （本次的算法应是"上次实测数 + 期间新加但没跑的用例数 + 本次新增"），否则每次都会差一截。

---

## 十一、M4-S3 进度持久化（2026-09-26）

### 11.1 环境前提：NuGet **现在能用了**（推翻了旧结论）

`Docs\11` 四之三 ② 原来写着「本沙箱还原不了 NuGet（NU1301），所以任何需要新包的活 AI 都做不了」。
M4-S3 要接 MySQL，我先**真跑了一次**再动手：

```
Test-NetConnection api.nuget.org -Port 443   → True
dotnet restore（引用 MySqlConnector 2.3.7）  → 已还原（2.48 秒）
```

⇒ 那条结论**按条件读**：它描述的是"当时出不了网"，不是"永远做不到"。
已在 `Docs\11` 就地更正，并把判据改成 **"现测，别背结论"**（同 SSH 那边的 22/443）。

### 11.2 新增一个工程：`NBC.Server.Data`（数据层）

**为什么不塞进现有的三个工程**（每条挡掉一个候选）：

| 候选 | 挡掉的理由 |
| --- | --- |
| `NBC.Shared` | 它是 `netstandard2.1` 的**双端共享层**，Unity 也要编。放 MySQL = 让客户端也拖数据库驱动，违反 D1/DET-02「共享层零第三方」 |
| `NBC.Server.Game` | 纯逻辑（战斗/副本/AI），要能在**没有数据库**的环境里跑测试。查询里出现 `MySqlConnection` = "想验一个伤害公式，得先起 MySQL" |
| `NBC.Server.Core` | 网络与主循环基础设施（会话/传输/Tick/房间），与"数据存哪"无关 |

⇒ 这就是 §13.2 架构图里那个独立的**数据层**。依赖方向 `Host → Data → Shared`，单向。

| 文件 | 职责 |
| --- | --- |
| `DatabaseOptions` | 纯 POCO 连接参数 + `NBC_DB_PASSWORD` 环境变量覆盖 + **`Describe()` 绝不打印密码** |
| `DbConnectionFactory` | 开连接 + **把连接失败翻译成"该怎么办"**（DB-10：不给裸异常） |
| `IConditionProgressDao` | 读写接缝（**为了能离线验写回缓存的逻辑**，见 §11.4） |
| `ConditionProgressDao` | Dapper 参数化 SQL：读全部 / 一条语句幂等 upsert / 删玩家 |
| `CachingConditionProgressStore` | `IConditionProgressStore` 的**写回缓存**实现（本片的核心） |

### 11.3 两张新表（**迁移脚本只增不删**）

⚠️ `Docs\08-数据库脚本.sql` 第 38 行是 `DROP DATABASE IF EXISTS nbc_db;` —— 它是**初始化**脚本，
跑一次会清库。所以新增一个**幂等、只增不删**的迁移：
`Docs\08b-数据库迁移-M4S3-进度表.sql`（`CREATE TABLE IF NOT EXISTS`，可重复执行）。
`08` 也同步加上了这两张表（全新机器跑 `08` 就什么都有）。

| 表 | 主键 | 为什么要它 |
| --- | --- | --- |
| `condition_progress` | `(player_id, condition_key)` | 主键是「玩家 + **条件**」而不是「玩家 + 任务」：任务与成就**共用条件系统**，而 `IConditionProgressStore` 说话的单位就是条件编号 ⇒ 一张表同时承载两者。存**绝对值**⇒ 落库**幂等**（重试安全） |
| `reward_granted` | `(player_id, owner_kind, owner_id)` | **见 §11.5 —— 这张表是加了持久化之后才暴露出来的一个 bug 的解药** |

### 11.4 核心设计：为什么**必须**是写回缓存（不是"每次 Set 就 UPDATE"）

看一眼调用点就明白（`ConditionTracker.Notify` 里）：

```csharp
foreach (已登记的条件) {
    int before = m_store.GetProgress(pair.Key);    // 每个条件读一次
    m_store.SetProgress(pair.Key, after.Current);  // 每个条件写一次
}
```

打死一只怪可能触发**十几次 Get + 十几次 Set**，30Hz 下每秒几百次。
一旦这里做同步查询 = "打一只怪卡一下"，而且卡在**逻辑线程**上，直接违反 **DB-03**。

⇒ **接口是同步的，实现就必须是内存的。** 这不是优化，是唯一能同时满足"接口好用"+"DB-03"的做法。

**落库时机**（DB-04：异步、脱离主循环线程、结果回投主循环队列）：

| 时机 | 为什么 |
| --- | --- |
| 进副本 / 登录时 `LoadAsync()` | 进度是跨局累计的，**必须先把全部读回来**（否则"这个成就早该解锁"会被漏掉） |
| 离开副本 / 成就解锁时 `FlushAsync()` | 成就解锁是"里程碑时刻"，值得立刻落盘 |
| 定时兜底（主循环低频调 `FlushAsync`） | 崩溃时最多丢一个周期 |
| 优雅关闭（SRV-17） | 等一次 `FlushAsync` 完成再退 |

**三条写死的线程语义**（都在文件头）：

1. **绝不持锁做 IO**：锁里只拷一份脏集快照，出了锁再发 SQL。持锁发 SQL = 主循环卡在等网络上（等于没写缓存）。
2. **单飞（single-flight）**：上一次没写完就不开新的。理由不是省事，是**正确性** —— 两次写若乱序落地，后落的那次会把**旧的**绝对值写进库 = **静默数据回退**。
3. **`Remove` 要写 0**（不是只删内存）：否则"内存删了、库里还在" ⇒ 下次登录又读回来，表现是"我明明重置过，怎么又有了"。

### 11.5 ⚠️ 本片最值钱的一条：持久化**自己制造了一个 bug**

`AchievementRuntime` 用 `resetProgress: false` 登记条件，而 `ConditionTracker` 的语义②是
**"注册时若已达成，当场回调一次"**（这正是"登录即解锁"要的行为）。

以前进度存内存、进程一重启就没了 ⇒ 永远不会重复解锁。
**一旦进度落库，服务端每重启一次就会把所有已完成的成就再发一遍奖。**

⇒ "解锁状态"能从 `condition_progress` 推导，但"**奖励发过没有**"推导不出来，**必须记账**。
所以有了 `reward_granted` 台账。

> 📌 这是"**加一个正确的东西，会暴露另一个地方的错误假设**"的典型样本：
> 持久化本身没写错，是原有代码隐含假设了"进度会随进程消失"。

⚠️ **台账的运行时守卫（`IRewardLedger` + `AchievementRuntime` 里查台账）是 S3 的下一步**，
本片先把**表**与**数据层**做完 —— 表已经建好，守卫不会需要第二次迁移。

### 11.6 实测

| 项 | 结果 |
| --- | --- |
| `NBC.sln -t:Compile` | **0 错 0 警告**（唯一的"1 个警告"是**你的 Host 进程锁着 bin**，环境问题不是代码问题：`MSB3061/MSB3026 ... 文件被"NBC.Server.Host (9964)"锁定`） |
| 新工程 `NBC.Server.Data` | **0 错 0 警告** |
| `_db-probe`（新探针） | **32 全绿 / 0 失败** |

`_db-probe` 三块，**第一、三块现在就能跑**（不需要密码）：

| 块 | 内容 | 条数 |
| --- | --- | --- |
| 一 | 写回缓存逻辑（假 DAO）：脏集合并 / 幂等 / Remove 写 0 / Load 合并 / 单飞 / 失败后重新标脏 | 24 |
| 二 | **真 MySQL**：建表检查 → 写 → 读回 → **新建 store 模拟重启** → 值还在 → 绝对值覆盖 → 收尾清理 | 6（**需密码**） |
| 三 | **DB-10 报错形状**（故意连不上）：密码错→「密码不对」+ 怎么给密码；端口没人听→「连不上」+ 三条排查；两者**不能互相误报**；消息里**不打印密码** | 5 |

> ⚠️ 第二块没密码时**明确打印"跳过 + 怎么给密码"**，而不是安静少跑一半 ——
> "探针全绿"可能其实是"第二块一条都没跑"，那正是本项目一直在防的假绿。

**探针当场抓到的 3 个失败，全是我的断言写错了**（如实记，因为这三条的教训是通用地）：

| 失败 | 我的错 | 教训 |
| --- | --- | --- |
| 幂等那条 | 断言 `Stored.Count == 1`（写了几次），而不是**库里的最终状态** | **幂等的定义是"重复执行后状态不变"，不是"没有重复执行"** —— 同 M1-B5「调用过 ≠ 内容对」 |
| Remove 那条 | 同上（`Stored` 是累积列表） | 判据要盯**结果**，不盯中间物 |
| `MarkUnflushed` 那条 | 手搓了一个"假装在飞"的批次，**脏集根本没被取走** | **"模拟状态"必须真的到达那个状态**，否则测的是我脑子里的模型 |

### 11.7 ✅ 负责人已完成（2026-09-26）

> 负责人原话：**「密码设置完成，进度表能看到六张表」**

1. ✅ **密码已设**：`[Environment]::SetEnvironmentVariable("NBC_DB_PASSWORD","…","User")`
   （**User 级**，所以不进聊天、不进仓库；⚠️ 已开的终端要新开一个才看得到 —— 见下面那条坑）
2. ✅ **迁移已跑**：`Docs\08b-数据库迁移-M4S3-进度表.sql`（幂等、不清库）

**独立核验**（不靠探针自报，直接用 `mysql.exe` 问库）：

| 核验项 | 结果 |
| --- | --- |
| 表数量 | **6**：`account` / `battle_player_detail` / `battle_record` / **`condition_progress`** / `player_profile` / **`reward_granted`** |
| `condition_progress` 主键 | `player_id`(PRI) + `condition_key`(PRI) ✅ 复合主键在 |
| `reward_granted` 主键 | `player_id` + `owner_kind` + `owner_id` ✅ 三列复合主键在 |
| 两张新表行数 | **0 / 0**（探针自己收尾干净） |
| 原有数据 | `account` **4** 行、`player_profile` **4** 行 —— **没被动过**（迁移只增不删） |

⚠️ **一个环境坑（值得记）**：**`User` 级环境变量对"已经开着的进程"不可见** ——
我这边每个 pwsh 都是新进程却没读到它，一度以为是"没设成功"。
判据：用 `[Environment]::GetEnvironmentVariable("NBC_DB_PASSWORD","User")` **直接读注册表那一份**，
只要它有值就说明**设成功了**，读不到只是当前进程的旧环境块。
（同族：W9「别自研判据」—— 先分清"没设"和"这个进程看不见"。）

### 11.8 S3 还没做的（说清楚，不含糊）

| 事 | 为什么留到下一步 |
| --- | --- |
| 服务端**权威化**：现在跑 `QuestRuntime`/`AchievementRuntime` 的是**客户端** | 这是更大的架构决定（协议 + 双端分工）。数据层已按"谁托管都行"设计（**归属靠构造绑定**，不依赖调用方是谁） |
| 战绩落库 SRV-13（`battle_record` / `battle_player_detail`） | 与进度持久化同一套数据层，接上去就是几张 DAO |
| 优雅关闭时等落库（SRV-17） | 要接进 Host 的关闭流程 |
| Host 里真正**装配**数据层（现在 `NBC.Server.Data` 只被探针引用） | 装配要等"服务端权威化"定下来（否则装了也没人用）；先用探针把 SQL 与逻辑都验住了 |


### 11.10 ✅ 台账守卫（`IRewardLedger`）—— 本片的后半段

**先把 bug 证成真的，再修**（不然"修好了"的用例可能是假绿）：

| 用例 | 断言 | 作用 |
| --- | --- | --- |
| `WithoutLedger_AlreadyMetProgress_GrantsAgainOnEveryStartup` | 不带台账 → 重启后 `GrantCount` **变成 2** | **阳性对照**：证明"重启重复发奖"是真的 |
| `WithLedger_AlreadyMetProgress_MarksUnlockedButDoesNotPayAgain` | 带台账 → `GrantCount` 仍 **1**，且状态**仍是已解锁** | 修好了。⚠️ 两条判据都要：只看"没重复发"会漏掉"成就显示成没解锁" |
| `WithLedger_RestoredUnlock_DoesNotReplayUnlockEvent` | 重启时**不重放** `Unlocked` 事件 | **恢复状态 ≠ 发生事件**（同 M3-B：移动是状态、动作是事件）。否则每次上线都看到一次"🏆 解锁" |
| `WithLedger_MetButNeverGranted_PaysOnRecovery` | 进度已达成 + 台账没记录 → **补发** | 台账的第二个好处：**崩溃在中间能自愈**（"按次数记账"做不到这点） |
| `NullLedger_Throws` | 传 null 当场报错 | 让"我没接台账"变成**编译期/启动期**看得见的决定 |
| `Ledger_MarkGranted_IsIdempotent` | 重复记账只算一笔 | 台账契约 |
| `Ledger_SeparatesQuestAndAchievement` | 同编号的任务/成就互不干扰 | 编号空间是分开的（对应 DB 的 `owner_kind`） |

**三个设计决定**（都有理由，不是随手写）：

| 决定 | 理由 |
| --- | --- |
| 台账是**必填参数**，不给默认值 | 给个 `InMemoryRewardLedger` 默认值"看起来友好"，但那会让"服务端重启重复发奖"**静默**留在代码里 —— 而它只在重启时才出现。必填 ⇒ 每个构造点都得想一次 |
| 从**持久化进度**恢复的解锁**不发** `Unlocked` 事件 | 那是"刚刚解锁"的庆祝信号；重放会让玩家以为又拿了一次 |
| `Unlock` 里**先发奖、再记账** | 反过来的话，`Grant` 抛异常就变成"记了账但没发" ⇒ 玩家**永远拿不到**。现在最坏是"发了但没记上" ⇒ 下次重启**补发**（多给，不少给）。两个方向都不完美，但**少给玩家比多给更糟** |

**改名的一次对照样本**（判断标准是"改名成本 vs 名字带来的误解"）：

| 类型 | 结论 |
| --- | --- |
| `MySqlConditionProgressStore` → **`CachingConditionProgressStore`** | **当场改**：一个调用方都还没有，改名免费 |
| `IRewardLedger` | **一开始就不带 `Quest`**：新接口命名是免费的，那就一次说对 |
| `IQuestRewardSink` / `QuestConditionLine` | **不改**：要动 8 个文件 + 2 个文件改名（Unity 要重导 `.meta`）⇒ 写进文件头，等下次大改一起做 |

**待你跑**：EditMode 684 → **691**（+7）。这一批**只用到内存台账**，所以它验的是
"台账的判定逻辑"，**不验**"台账能活过进程重启"（那是持久化实现的事）。

### 11.11 ✅ 持久化台账（`MySqlRewardLedger`）+ **接缝搬了一次家**

#### ① 探针：**51 全绿**（真库往返全跑起来了）

| 块 | 条数 | 关键几条 |
| --- | --- | --- |
| 一 写回缓存逻辑（假 DAO） | 24 | 单飞 / 幂等 / Remove 写 0 / `MarkUnflushed` 不覆盖新值 |
| 二 **真 MySQL：进度往返** | 6 | **重启后进度还在** / 绝对值覆盖（10 覆盖 4 而不是 14）/ 收尾干净 |
| 三 DB-10 报错形状 | 5 | 密码错→「密码不对」；端口没人听→「连不上」+ 三条排查；**不互相误报**；**不打印密码** |
| 四 台账逻辑（假 DAO） | 8 | 单调性 / 两个编号空间 / **落库失败那一批要回到脏集** |
| 五 **真 MySQL：台账往返** | 5 | **重启后还记得发过**（防重复发奖的全部依据）/ 主键去重生效 / 收尾干净 |

#### ② 一个**结构性错误**：接缝放错了程序集（闸门当场逼回来）

`IRewardLedger` 我一开始放在 `Game\Quest\`（和 `IQuestRewardSink` 一起）。
写完数据层才发现：**`NBC.Server.Data` 只引用 `NBC.Shared`，而 `Game\` 是 Unity 程序集**
⇒ **服务端根本实现不了这个接缝**。

⇒ 搬家到 `Shared\Reward\`（命名空间 `NBC.Shared.Reward`）。
对照：`IConditionProgressStore` 一开始就在 `Shared\Condition\` —— 它是对的，**我抄错了位置**。

> 📌 **判据（比"谁用它"更难想到的那一问）**：**"谁要实现它？"**
> 一个要被**服务端**实现的接缝，必须住在双端共享层；只放在 `Game\` 里，
> 编译器早晚会说"服务端引不到它"。同族：D14「**看整条依赖链**」。

#### ③ 台账为什么能比进度缓存**简单得多**（不是复制粘贴）

| | 进度表 | 台账 |
| --- | --- | --- |
| 值的性质 | **会变**（1→2→3，还能 `Remove`） | **单调**（发过就是发过） |
| 写冲突 | "旧值覆盖新值" = **静默回退** | 不存在：重复写同一行无副作用 |
| 合并规则 | "同一次落库期间又被改过"很要命 | 不需要：后写的和先写的一样 |
| 需要的机制 | 脏集 + 单飞 + **`MarkUnflushed` 要比值** | 脏集 + 单飞（**失败时无条件放回**） |

⇒ **同一个模式，复杂度取决于数据语义。** 缩水不是偷懒，是"这份数据本来就不需要那些机制"。

#### ④ ⚠️ 一个**写反了**的 bug（我自己的注释先发现的）

`MySqlRewardLedger.FlushAsync` 第一版里，我写了注释"失败必须把这一批放回脏集"，
**但代码没做**（注释与代码互相矛盾 —— 读到那一段就该停下来）。

它的后果不轻：`MarkGranted` 是"内存里已经有它就**不再标脏**"，
所以脏集一旦被取走又没写成功，那个键**再也不会被标脏**
⇒ "发了奖但台账没落库"**永久丢失** ⇒ 下次重启**再发一遍**（等于台账白装）。

修法：`FlushAsync` 自己 `catch { RestoreDirty(batch); throw; }`。
**不用"让调用方补一刀"那种设计** —— 调用方手里没有那一批（它是内部的），
所以那种 API 只能靠"记得"来正确，而"靠记得"迟早会漏。

**判据用可观察行为**（`LedgerFailureKeepsRecord`）：故意让写失败 → 断言**下一次落库会不会带上它**，
而不是去读内部脏集字段。

### 11.9 ⚠️ 一条待你定夺的**惯例问题**（不影响功能，但影响"证据能不能复现"）

`Server\.gitignore` 把几个探针都排除了（`_api-probe` / `_condition-probe` / `_net-probe` / `_unity-ref-probe` / `_nupkg-verify`），
我按惯例把 `_db-probe/` 也加了进去（`git ls-files Server/_net-probe` = **0 个文件**，确认了这条惯例）。

**但这个惯例有一个真实代价**：文档里到处引用探针数字（"探针 93 全绿"、"`_db-probe` 32 全绿"），
而**探针源码不在仓库里 ⇒ 克隆下来的人复现不了这些数字**，那些数字就只是"我说过"。

而且 `_db-probe` 与 `_api-probe` **不是同一类**：

| 探针 | 为什么被排除 | 技术上能不能进仓库 |
| --- | --- | --- |
| `_api-probe` | `HintPath` 指向**本机 Unity 安装目录**，换机器就失效 | ❌ 不能 |
| `_net-probe` / `_condition-probe` / **`_db-probe`** | 只是"验证脚手架"这个习惯 | ✅ **能**（没有任何本机路径依赖） |

⇒ **建议（等你定）**：把 `_db-probe` / `_net-probe` / `_condition-probe` 这类**无本机依赖**的探针纳入仓库，
让"实测数字"变成**可复现的证据**而不只是结论。这与项目一直在强调的
"**判据要能被别人重跑**"是同一条。我没有擅自改惯例 —— 这属于仓库结构决定，不该我单方面动。
---

## 十二、📍 明天从这里开始（2026-09-26 收工时的真实状态）

> 本节是**交接点**，不是计划书。上面各节的 ✅/🔨 是权威，这里只回答"明天第一件事干什么"。

### 12.1 ✅ 今天已完成（都已提交推送，`HEAD = 7f14cbd`）

| # | 事 | 证据 |
| --- | --- | --- |
| 1 | **M4-S2 成就**（负责人验收） | EditMode **684 全绿** + 调试面板三条成就可见 |
| 2 | **M4-S3 数据层 + 真库往返** | `_db-probe` **51 全绿**（含"重启后进度还在 / 重启后还记得发过"） |
| 3 | **`reward_granted` 运行时守卫**（§11.10） | 阳性对照 + 7 条新用例；`AchievementRuntimeTests` 15 → 22 |
| 4 | **修 Unity CS0012**（`FromConfigMgr` 的推测性可选参数） | 见 §十三 |
| 5 | **新增 asmdef 边界闸门**（§十三） | 变异测试证明它会红；12 个程序集全绿 |
| 6 | **新增 `Docs\API\`**（README / 00 / 01 / 07） | 链接检查 0 BROKEN |

### 12.2 🔨 明天第一件事：**Unity 里确认那两条**

我只能验到"闸门层面"，**Unity 里那一次重编必须你来**：

1. 切回 Unity 让它重编 → **Console 应当不再有 `CS0012`**
   （修法见 §十三；如果还有别的错，把原文贴我）。
2. 跑 **EditMode，期望 691**（684 + 7 条台账用例）。
   ⚠️ 这一批只用**内存台账**，验的是"台账判定逻辑"，
   **不验**"台账能活过进程重启"（那个由 `_db-probe` 的真库往返验）。

### 12.3 之后按优先级选（我的建议顺序）

| 序 | 事 | 为什么这个顺序 |
| --- | --- | --- |
| 1 | **教学留档 `M4-B-持久化与写回缓存`** | 项目规矩「模块收关当场写课」，而 S3 的数据层已经**真库验证通过**了 ⇒ 正是写课的时候 |
| 2 | **`Docs\API\` 的 02~06**（Framework / Game / Server / Protocol / ConfigTables） | 你上一轮问的就是这个；`01` 已定风格，按它写。**⚠️ 先等你确认风格**（更短？更偏"照着做"？） |
| 3 | **S3 尾工**：服务端权威化 / SRV-13 战绩落库 / SRV-17 优雅关闭 / Host 里装配数据层 | §11.8。⚠️ 这几件互相依赖（权威化定了，装配才有意义） |
| 4 | **M4-S4 帧同步 2v2** | 定点与 `BattleRandom` 已铺好，主要是**帧协议与确定性校验** |
| 5 | 顺手改名的窗口 | `IQuestRewardSink` / `QuestConditionLine` 的 `Quest` 是历史遗留；**建议和"奖励挪到服务端权威"一起做**（那时本来就要动这几个文件） |

### 12.4 ⚠️ 两件"等你定"的事（我不擅自决定）

| 事 | 我的建议 | 为什么不该我单方面动 |
| --- | --- | --- |
| **无本机依赖的探针要不要进仓库**（§11.9） | 建议纳入 `_db-probe` / `_net-probe` / `_condition-probe` | 文档到处引用探针数字，而**源码不在仓库 ⇒ 克隆下来复现不了那些数字**。但这属于**仓库结构决定** |
| **`Docs\API\` 后续几篇的详略** | 按 `01` 的密度写 | 你是读者，详略该你定 |

### 12.5 明天开机的三条命令（30 秒自检）

```powershell
cd "E:\U3D Projects\0_MyFile\3D联网战斗Demo"

# ① 服务端 + asmdef 边界（这条最重要：它是唯一能抓 CS0012 的）
dotnet build Server\NBC.sln -m:1
powershell -ExecutionPolicy Bypass -File Tools\Check-AsmdefBoundaries.ps1

# ② 双端共享逻辑（47 条，不需要 Unity/数据库）
Server\_condition-probe\bin\Debug\net8.0\NBC.ConditionProbe.exe

# ③ 真库往返（51 条；需要 NBC_DB_PASSWORD —— User 级设过就行）
$env:NBC_DB_PASSWORD = [Environment]::GetEnvironmentVariable("NBC_DB_PASSWORD","User")
Server\_db-probe\bin\Debug\net8.0\NBC.DbProbe.exe
```

**成功判据**：①`0 个错误` + `12 个程序集全绿` ②`通过 47，失败 0` ③`通过 51，失败 0`
---

## 十三、修 Unity `CS0012` + 新增 asmdef 边界闸门（2026-09-26）

### 13.1 负责人实测的报错原文

```
Assets\_Project\Boot\M2DemoBehaviour.cs(521,29): error CS0012:
  The type 'IRewardLedger' is defined in an assembly that is not referenced.
  You must add a reference to assembly 'NBC.Shared, Version=..., PublicKeyToken=null'.
```

### 13.2 根因：类型通过**「被调方法的签名」**泄漏，而调用方源码里一个字都没提它

我给 `BattleSession.FromConfigMgr` 加了一个**可选**的 `IRewardLedger` 参数
（类型在 `NBC.Shared`），而调用方 `M2DemoBehaviour` 在 `NBC.Boot`，它的 `references` 是
`NBC.Framework / NBC.Framework.UI / NBC.Framework.YooAsset / NBC.Game` —— **没有 `NBC.Shared`**。

C# **必须解析被调方法的完整签名（含可选参数的默认值）** ⇒ 需要那个程序集。

⚠️ 注意这一条的**新形态**：`M2DemoBehaviour.cs` 里**既没有** `using NBC.Shared...`，
**也没有**写出 `IRewardLedger` 这个词 —— 所以
`AsmdefReferenceGuardTests`（按 `using` 扫）**看不见**它。

### 13.3 修法：**不给那个方法加参数**（而不是给 Boot 加引用）

| 选项 | 为什么不选 |
| --- | --- |
| 给 `Boot.asmdef` 加 `NBC.Shared` | 编译器的确说"该加引用"，**但那个参数本来就没人用得上**：客户端这一侧没有持久化台账（进度还在本地），加了它就是"猜测性的通用" |
| ✅ **删掉那个可选参数** | ① 需求上不需要；② 更重要的：**一个"没人用得上"的参数，代价不只是多一行 —— 它会把一个程序集依赖钉进调用方的依赖图里** |

台账仍然通过 **10 参构造函数**注入（测试 / 服务端用）；
`FromConfigMgr` 内部传 `null`（= 内存台账），并在**调用点**写清了为什么。

### 13.4 新增 `Tools\Check-AsmdefBoundaries.ps1`（补上单程序集闸门的盲区）

把每个 asmdef 的**文件集合**与**引用集合**照抄成 csproj，一个个真编：

| 细节 | 为什么 |
| --- | --- |
| `<Compile Include>` **排除嵌套 asmdef 目录** | `Game\Model\Model.asmdef` 就在 `Game\` 里面 —— 不排除就会把 `NBC.Model` 的源码**也**编进 `NBC.Game`，边界当场失效 |
| `noEngineReferences: true` 的程序集**一个引擎引用都不给** | 顺带把 D1/DET-02「共享层不许碰 UnityEngine」变成**机械保证** |
| **两个**引用字段都要读：`references` + `precompiledReferences` | 后者**写的是带 `.dll` 的文件名**；`NBC.Protocol` 正是靠它拿 `Google.Protobuf.dll`（它的 `references` 是**空**的） |
| `overrideReferences: false` 时要补**自动引用的插件** | 那是 Unity 的默认行为（`NBC.Game` 能 `using Google.Protobuf` 全靠这条） |

### 13.5 ⚠️ 两次被"**先证明它会红**"救回来（这是本节最值钱的部分）

**第一次**：漏了 `precompiledReferences` ⇒ `NBC.Protocol` 报 `CS0400`「找不到 Google」，
还**连带**把 `Game / Boot / Editor / Tests` 全带红（它们都引用 Protocol）——
**看起来像"到处都错"，其实只有一个根因。**

**第二次（严重）**：第一版**没有** `<DisableTransitiveProjectReferences>true</DisableTransitiveProjectReferences>`。

- MSBuild 的 `ProjectReference` **默认把引用传递下去**（Boot → Game → Shared ⇒ Boot 也看得见 Shared）
- 而 **Unity 的 asmdef 引用绝不传递** —— 这正是本项目记了 11 次以上的那条
  「asmdef / ProjectReference 引用不传递」

⇒ 后果：我把变异（坏参数）加回去时，**单程序集闸门绿、这个新闸门也绿** ——
**它当时是个摆设**。加上那一行之后：

| 变异测试 | 结果 |
| --- | --- |
| 基线（已修） | **12 个程序集全绿** ✅ |
| 变异（把可选参数加回去） | 单程序集闸门 **0 错**（盲区坐实）／asmdef 闸门 **[红] NBC.Boot**，复现出 Unity 那条 `CS0012`，**同一个文件同一个列号 `521,29`** |
| 还原 | **12 个程序集全绿** ✅ |

> 📌 **教训**：**一个新加的检查，必须先用变异证明它会红。**
> 否则你只是多了一个"看起来很像检查"的东西 —— 同族：「阴性对照天生容易假绿」。

### 13.6 三道闸门的分工（新人最容易搞混的一点）

| 闸门 | 它认得什么 | 它**不**认得什么 |
| --- | --- | --- |
| `_api-probe` | 语法 / API 存不存在 / C# 9 | **程序集边界**（它把源码编进**一个**程序集） |
| `_asmdef-probe`（本次新增） | **程序集引用**（CS0012 / CS0246 的跨程序集形态） | 行为 |
| Unity EditMode | 行为 + **真正的 asmdef 规则** | 要开 Unity（慢） |

> 📌 **`_api-probe` 绿 ≠ Unity 绿** —— 这就是第三道闸门存在的**唯一**理由。