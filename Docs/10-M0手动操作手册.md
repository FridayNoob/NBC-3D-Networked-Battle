# M0 · 项目奠基 —— 手动操作手册

| 项 | 内容 |
| --- | --- |
| 手册版本 | v1.0 |
| 编写日期 | **2026-09-16** |
| 对应里程碑 | M0 · 项目奠基（需求文档 §18，第 1 周） |
| 适用规则 | `Docs\00-项目工作规则.md` —— **规则 13（最高优先级）**：AI 不做编辑器点击/测试/打包；**规则 4**：非代码操作须给详细步骤 |
| 目标 | 让工程可打开、可打包、可连库、依赖冻结，并**提前暴露 IL2CPP/AOT 与插件架构边界两个风险** |

---

## 一、总览与分工

### 1.1 为什么要先做这些（M0 的目的）

M0 不是"把环境装好"这么简单，它的核心目的是**把两个会在后期爆炸的风险提前引爆**：

| 风险 | 为什么必须在第 1 周验证 | 本手册对应段落 |
| --- | --- | --- |
| **R4：IL2CPP 下 xLua / protobuf 的 AOT 问题** | Editor 里一切正常、打包后崩溃，是最难查的一类问题。若拖到 M6 才发现，可能推翻协议层实现。**M0 已实测命中并换库（protobuf-net → Google.Protobuf）** | **§3 阶段 D**、**§4 阶段 E** |
| **R13：插件无法在纯 .NET 服务端运行** | 这决定 AI 架构（裁决 A1/A2/A3）。要用**事实证明**，而不是靠推测 | **§5 阶段 F** |

### 1.2 分工表（规则 13）

| 阶段 | 编号 | 谁做 | 说明 |
| --- | --- | --- | --- |
| 一、创建 Unity 工程 | D1 | 👤 **你手动** | Unity Hub 点击操作 |
| 二、工程关键设置 | D2~D4 | 👤 **你手动** | Project Settings 面板操作 |
| 三、导入依赖与插件 | D5~D11 | ✅ **全部完成** | D5 / **D6** / D7 / D7b / D8 / D9 / D10 / **D11** 均已完成（2026-09-20） |
| 四、数据库准备 | D12 | ✅ **已完成**（你执行 · AI 只读核验） | 执行方式与 `error: 42` 的坑见本节 D12；核验记录见「D12 实测记录」 |
| 五、服务端骨架 | D13 | 🤖 AI 已建文件 + 👤 你 build/run | AI 写 `.csproj`，你运行 `dotnet build` / `dotnet run` |
| 六、插件边界验证 | D14 | ✅ **已完成**（AI 代跑实验 · 你委托） | 实验记录见 `Docs\01-项目需求文档.md` **§5.4.6** 与 `Docs\12-D14实验操作单.md`；探针可随时自己重跑 |
| 七、工程骨架 | D15 | 🤖 AI 已建暂存文件 + 👤 **你手工搬运** | 目录与 9 个文件都在 `ClientStaging/`，**按 §1.3 逐步手工建立**（不用脚本，规则 15） |
| 八、Git 初始化 | D16 | ✅ **已完成**（你执行 · AI 只读审计） | 提交 `f83771d`（47 文件）；审计记录见 §「D16 实测记录」；**待补一次提交**（`.gitignore` + 7 个 `.gitkeep`） |
| 九、IL2CPP 打包验证 | D17 | 👤 **你手动** | Build Settings 出包并运行 |

> **AI 在本阶段只做**：写文件（`.csproj`、`.gitignore`、README、SQL 脚本、asmdef、只读诊断/校验脚本）、只读命令分析。
> **不碰**：Unity 编辑器、打包、测试运行、`git commit`/`push`、任何需要凭据的操作。
> **也不再用一键脚本替你完成流程** —— 依据规则 15（以培养你的操作能力与知识为目标）。

### 1.3 D15 的目录骨架：手工建立（教学版）

**为什么这一步要你自己敲，而不是给你一个脚本**

依据规则 15：本项目的目标包括提升你的开发能力。把"建 14 个目录 + 搬 9 个文件"打包成一条命令，
你确实省了 5 分钟，但你会**不知道这 23 个动作分别是什么、为什么要这些目录、怎么确认做对了**。
而这套目录结构接下来要陪你走完 M1~M6，理解它比省 5 分钟值钱。

**你需要的两条命令（PowerShell）**

| 用途 | 命令 | 说明 |
| --- | --- | --- |
| 建目录（含所有父级、已存在不报错） | `New-Item -ItemType Directory -Force -Path <路径>` | `-Force` = "已存在也当成功"，所以可以放心重复执行 |
| 复制文件 | `Copy-Item <源> <目标> -Force` | `-Force` = 覆盖已存在的目标 |

> **怎么验证命令真的生效**：`Test-Path <路径>` 返回 `True`/`False`。
> 这是后面每个步骤的通用验证手法 —— **不要靠"看起来没报错"判断成功**。

**第一步：进入工程根目录并确认 Unity 工程存在**

```powershell
cd "E:\U3D Projects\0_MyFile\3D联网战斗Demo"
Test-Path "Client\Assets"      # 必须是 True（D1 已完成）；False 说明工程还没建
```

**第二步：建立目录骨架**（与需求文档 §2.4 一致）

```powershell
$A = "Client\Assets"
@(
  "_Project\Framework", "_Project\Game\Model", "_Project\Game\View",
  "_Project\Game\Controller", "_Project\Game\Service",
  "_Project\Editor", "_Project\Configs", "_Project\Art",
  "_Project\Tests\Manual", "_Project\Tests\EditMode", "_Project\Tests\PlayMode",
  "LuaScripts", "Scenes", "Art"
) | ForEach-Object { New-Item -ItemType Directory -Force -Path "$A\$_" | Out-Null }
```

**这 14 个目录各自是干什么的**（不然就是照着敲，没有意义）：

| 目录 | 作用 | 为什么必须独立 |
| --- | --- | --- |
| `_Project\Framework` | 框架底座（单例/事件中心/对象池/UI/音效等） | 需求 13 要求可整包导出复用，必须与业务隔离 |
| `_Project\Game\Model` | **纯 C# 逻辑层**（帧同步的战斗逻辑） | **禁引 UnityEngine**，由 asmdef 编译期强制 |
| `_Project\Game\View` | 表现层（UI / 特效 / 动画 / 相机） | MVC 的 V，只做显示 |
| `_Project\Game\Controller` | 调度层（输入 → Model → 通知 View） | MVC 的 C，唯一调度中心 |
| `_Project\Game\Service` | 跨模块服务（网络 / 热更 / 配置） | 与 Controller 职责不同：服务是无状态能力 |
| `_Project\Editor` | 编辑器工具（配置表导出等） | 独立 asmdef + 仅 Editor 平台，否则会被打进包 |
| `_Project\Configs` | Excel 导出的 SO 配置资产 | 配置是"数据"不是"代码"，便于单独打包 |
| `_Project\Art` | 工程自有美术资源 | 与第三方资源（`ThirdParty`）分开，便于清理 |
| `_Project\Tests\Manual` | 手工验证脚本（如 D17 的 AOT 探针） | 与自动化测试分开，避免被当测试程序集处理 |
| `_Project\Tests\EditMode` / `PlayMode` | 自动化测试（NUnit） | 需要独立 asmdef，否则 NUnit 引用会污染正式程序集 |
| `LuaScripts` | Lua 业务脚本 | **热更对象**，要独立于 C# 编译产物 |
| `Scenes` | 场景文件 | Unity 约定 |
| `Art`（Assets 根下） | 第三方/外部来源美术资源 | 与 `_Project\Art` 区分来源，便于整体替换 |

**第三步：搬运 9 个暂存文件**

`ClientStaging\` 里放的是 AI 写好的 9 个文件（因为 D1 之前 `Client\Assets` 还不存在，AI 没地方放）。
逐个复制并**同时理解它是什么**：

```powershell
# ① D17 的 AOT 探针脚本（M0 最关键的验证工具）
Copy-Item "ClientStaging\NBV0_EnvironmentProbe.cs" "$A\_Project\Tests\Manual\" -Force

# ② 编译符号自动注入器（检测 dll 是否存在，自动维护 NBC_HAS_* 符号）
Copy-Item "ClientStaging\Editor\NBV0_DefineInitializer.cs" "$A\_Project\Editor\" -Force

# ③ IL2CPP 裁剪保护清单（放在 Assets 根下，Unity 才会识别）
Copy-Item "ClientStaging\link.xml" "$A\link.xml" -Force

# ④~⑨ 6 个程序集定义（asmdef）
Copy-Item "ClientStaging\AsmDefs\Framework.asmdef"      "$A\_Project\Framework\Framework.asmdef" -Force
Copy-Item "ClientStaging\AsmDefs\Model.asmdef"          "$A\_Project\Game\Model\Model.asmdef" -Force
Copy-Item "ClientStaging\AsmDefs\Game.asmdef"           "$A\_Project\Game\Game.asmdef" -Force
Copy-Item "ClientStaging\AsmDefs\Editor.asmdef"         "$A\_Project\Editor\Editor.asmdef" -Force
Copy-Item "ClientStaging\AsmDefs\Tests_EditMode.asmdef" "$A\_Project\Tests\EditMode\Tests_EditMode.asmdef" -Force
Copy-Item "ClientStaging\AsmDefs\Tests_PlayMode.asmdef" "$A\_Project\Tests\PlayMode\Tests_PlayMode.asmdef" -Force
```

> **为什么 `link.xml` 和 `Framework.asmdef` 都是空壳？**
> `link.xml` 此刻声明的程序集（`Assembly-CSharp`，为 xLua 保留；**protobuf 已换库、不再需要条目**）**还没导入**，所以它现在不起作用 —— 这是正常的，
> 它要等 D6/D7 导入插件后才生效。`Framework.asmdef` 也还没有任何 `.cs` 文件，M1 才填内容。
> **"现在就位、稍后生效"是刻意设计**：这样 M1 一开工目录与程序集边界就已正确，不用回头补。

**第四步：一次性验证全部 23 个动作**

```powershell
$A = "Client\Assets"
$expect = @(
  "$A\_Project\Framework", "$A\_Project\Game\Model", "$A\_Project\Game\View",
  "$A\_Project\Game\Controller", "$A\_Project\Game\Service", "$A\_Project\Editor",
  "$A\_Project\Configs", "$A\_Project\Art", "$A\_Project\Tests\Manual",
  "$A\_Project\Tests\EditMode", "$A\_Project\Tests\PlayMode",
  "$A\LuaScripts", "$A\Scenes", "$A\Art",
  "$A\link.xml",
  "$A\_Project\Tests\Manual\NBV0_EnvironmentProbe.cs",
  "$A\_Project\Editor\NBV0_DefineInitializer.cs",
  "$A\_Project\Framework\Framework.asmdef",
  "$A\_Project\Game\Model\Model.asmdef",
  "$A\_Project\Game\Game.asmdef",
  "$A\_Project\Editor\Editor.asmdef",
  "$A\_Project\Tests\EditMode\Tests_EditMode.asmdef",
  "$A\_Project\Tests\PlayMode\Tests_PlayMode.asmdef"
)
$missing = $expect | Where-Object { -not (Test-Path $_) }
if ($missing) { Write-Host "缺失 $($missing.Count) 项:" -ForegroundColor Red; $missing }
else { Write-Host "全部 $($expect.Count) 项就位" -ForegroundColor Green }
```

**预期结果**：输出 `全部 23 项就位`。

**失败时先看什么**

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| `Test-Path "Client\Assets"` 是 False | D1 还没做 | 先完成 D1（Unity Hub 建 3D URP 工程） |
| 提示"找不到路径 …ClientStaging…" | 当前目录不对 | `cd` 到仓库根目录（含 `ClientStaging` 的那个目录） |
| 复制后 Unity 里看不到文件 | Unity 需要刷新 | 切到 Unity 窗口，或菜单 **Assets → Refresh**（`Ctrl+R`） |
| Unity Console 报"找不到 NBC.Framework" | **属正常**：该程序集 M1 才有内容 | 可临时从 `Model`/`Editor`/`Tests` 的 asmdef 里删掉该引用，M1 再加回 |


---

## 二、阶段一：创建 Unity 工程

### D1 · 用 Unity Hub 创建 URP 工程 👤

**前置检查（1 分钟）**

1. 打开 **Unity Hub**
2. 左侧 **Installs** → 确认已有 **2022.3.62f3c1**（版本已于 2026-09-20 从 `2022.3.17f1c1` 升级，原因见需求文档 **R16**）
   - 本机安装位置：新版在 `D:\SOFT\Unity\Hub\Editor\2022.3.62f3c1`（Hub 副安装路径），旧版仍在 `C:\Program Files\Unity\Hub\Editor\2022.3.17f1c1`
3. 点该版本右侧的**齿轮 / 三个点 → Add modules**，确认勾选了：
   - ☑ **Microsoft Visual Studio Community 2022**（或你已装的 VS2022）
   - ☑ **Windows Build Support (IL2CPP)**
   - ☑ **Documentation**（可选）

> ⚠️ **如果没有 "Windows Build Support (IL2CPP)"，现在补装**。IL2CPP 是需求文档 §5.1 冻结的脚本后端，
> 缺了它后面 D17 一定失败。（Mono 后端不需要它，但我们不用 Mono。）
> ✅ 该模块**已确认装好**（两版编辑器的 `PlaybackEngines` 下都有 `windowsstandalonesupport`）。

**创建工程**

4. Unity Hub 左侧 **Projects** → 右上角 **New project**
5. 顶部选中编辑器版本 **2022.3.62f3c1**
6. 模板列表选择 **3D (URP)** —— 这是 Built-in 之外唯一的通用 3D 模板
   - 若模板列表里找不到 "3D (URP)"：先随便建一个 **3D (Built-in)** 工程，后面在 D2 用 Package Manager 装 URP 包并切换（我会在 D2 补充该分支步骤）
7. 右侧填写：
   - **Project name**：`Client`
   - **Location**：`E:\U3D Projects\0_MyFile\3D联网战斗Demo`
   - ☐ **不要勾** "Connect to Unity Cloud"（本机作品集项目，避免引入额外依赖）
8. 点 **Create project**，等待首次导入（约 2~5 分钟）

**预期结果**

- 目录出现：`E:\U3D Projects\0_MyFile\3D联网战斗Demo\Client\`
- `Client\Assets\`、`Client\Packages\`、`Client\ProjectSettings\` 三个目录存在
- 打开后 `Assets\Scenes\SampleScene.unity` 可看到 URP 示例场景，Console 无红色报错

**如果失败看什么**

| 现象 | 排查 |
| --- | --- |
| 创建卡住/进度条不动 | 关闭杀毒软件实时防护后重试；检查 C 盘剩余空间 > 10GB |
| 打开后大量红色报错 | `Window → Package Manager` 检查 URP 包是否装好；或删掉 `Client\Library` 重开 |
| 模板列表是空的 | 断网导致模板拉取失败；检查网络/代理后重启 Hub |

---

## 三、阶段二：工程关键设置

> 这一阶段决定了后续所有资源和性能数据的基础，**必须在导入资源之前设好**（颜色空间后期改会导致所有材质需要重导）。

### D2 · Player Settings（玩家设置）👤

菜单：**Edit → Project Settings → Player**

选中 **Settings for PC, Mac & Linux Standalone** 页签，逐项设置：

| 位置 | 项 | 设为 | 为什么 |
| --- | --- | --- | --- |
| Player → 顶部 | **Company Name** | `NBC` | 影响存档路径 |
| Player → 顶部 | **Product Name** | `NBC`（全称 Networked Battle Combat） | 出包名 `NBC.exe` |
| Player → Other Settings | **Scripting Backend** | **IL2CPP** | 需求文档 §5.1 冻结 |
| Player → Other Settings | **Api Compatibility Level** | **.NET Standard 2.1** | 与 xLua / Google.Protobuf 兼容性最好 |
| Player → Other Settings | **Managed Stripping Level** | **Low** | ⚠️ **先设 Low**。后续项目稳定后可尝试 Medium，但**xLua 依赖反射、容易被裁剪**（Google.Protobuf 是生成代码、零反射，不受影响），这是 R4 风险点 |
| Player → Other Settings | **Allow 'unsafe' Code** | ☑ 勾选 | xLua 的部分封装需要 |
| Player → Other Settings | **Active Input Handling** | **Input Manager (Old)** | 需求文档 §5.1：先用旧版 Input 把逻辑跑通 |
| Player → Configuration | **Api Compatibility / 脚本运行时版本** | `.NET Standard 2.1` | 同上 |
| Player → Publishing Settings | **Use Incremental GC** | ☑ 勾选 | 服务 §12.2 的 GC Alloc 指标 |
| Player → Splash Image | **Show Splash Screen** | ☐ 取消（Personal 版不可关，跳过） | — |

### D3 · Quality / Graphics 设置 👤

菜单：**Edit → Project Settings → Graphics**

1. **Scriptable Render Pipeline Settings**：确认已指向一个 **URP Asset**（如 `Assets/Settings/UniversalRP-HighQuality.asset`）
2. 若为空 → 在 `Assets/Settings/` 右键 **Create → Rendering → URP Asset (with Universal Renderer)**，然后回填到此处

菜单：**Edit → Project Settings → Quality**

3. 删掉不需要的画质档位，**只保留三档**（需求文档 OPT-13 要求提供 Low/Mid/High）：

| 档位名 | 建议设置 |
| --- | --- |
| **Low** | 阴影关、分辨率缩放 0.8、抗锯齿关 |
| **Mid** | 阴影距离 30、MSAA 2x |
| **High** | 阴影距离 50、MSAA 4x、开启 HDR |

> 三档的差异要**记录下来**（后面写 `Docs\07-性能优化报告.md` 时要用真实的档位差异数据）。

菜单：**Edit → Project Settings → Player → Other Settings**

4. **Color Space** 设为 **Linear**（URP 标准；若创建时模板已设好则跳过）

### D4 · 必要包检查 👤

菜单：**Window → Package Manager**

切到 **Unity Registry**，确认以下包已安装（未装则 **Install**）：

| 包名 | 用途 | 需求文档对应 |
| --- | --- | --- |
| **Universal RP** | 渲染管线 | §5.1 |
| **Cinemachine** | 第三人称相机 | CAM-01~04 |
| **Test Framework** | 单元测试 | §14.3 机械校验 |
| **Input System** | ☐ **不装**（我们用旧版 Input） | — |

**预期结果**：Console 无报错；Graphics 指向有效的 URP Asset。

---

## 四、阶段三：导入依赖与插件

### ⚠️ 导入顺序很重要

按 D5 → D11 的顺序导入。原因：**YooAsset 与 xLua 会改动资源导入流程**，先装它们可以避免后续资源的重复导入。

### D5 · YooAsset（资源/AB 层）👤

YooAsset 通过 **OpenUPM** 发布，需要先加作用域注册表：

1. 菜单：**Edit → Project Settings → Package Manager**
2. 在 **Scoped Registries** 区域点 **+**，填入：

| 字段 | 值 |
| --- | --- |
| **Name** | `package.openupm.com` |
| **URL** | `https://package.openupm.com` |
| **Scope(s)** | `com.tuyoogame.yooasset` |

3. 点 **Apply**
4. 菜单：**Window → Package Manager** → 左上角下拉选 **My Registries**
5. 找到 **YooAsset** → **Install**

**预期结果**：`Packages` 下出现 YooAsset；菜单栏出现 **YooAsset** 顶级菜单。

**验证**：点 **YooAsset → AssetBundle Collector** 能打开窗口（此时还没配置，内容为空是正常的）。

> 参考：[YooAsset 快速开始文档](https://yoo-asset-doc.vercel.app/docs/2.3.x/guide-editor/QuickStart)

### D6 · xLua（Lua 绑定）👤

> ✅ 离线源码已在：`E:\U3D Projects\0_MyFile\xLua-master\`（GitHub `master` 分支；`CHANGELOG.txt` 最新条目 = **v2.1.15**）
>
> ⚠️ **本节原先写错了，正是它导致你报 `DllNotFoundException: xlua`**：
> 原文写"`Client\Assets\ThirdParty\XLua\` 下应有 `Src`、`Plugins`、`Examples` 等" ——
> 实际上**仓库里 `Assets\Plugins\` 与 `Assets\XLua\` 是【平级】的**，`Plugins` 并不在 `XLua` 里面。
> 只复制 `XLua` 就会丢掉 C 原生库。**下面是修正后的正确做法。**

**① 先理解 xLua 的仓库布局（不理解必踩坑）**

```
xLua-master/
└─ Assets/
   ├─ XLua/                     ← ① C# 源码：Src / Examples / Tutorial / Editor / Resources / Doc
   └─ Plugins/                  ← ② 【C 原生库】，按平台分目录
      ├─ x86_64/   xlua.dll (Windows 64) + libxlua.so (Linux 64)   ← 编辑器与 PC 出包都用它
      ├─ x86/      xlua.dll (Windows 32) + libxlua.so
      ├─ Android/  iOS/  WebGL/  WSA/  arm64/  xlua.bundle/
      └─ （每个目录都有配套 .meta，决定 Unity 的平台导入设置）
```

**为什么缺了 ② 就报 `DllNotFoundException: xlua`**：
`XLua/Src/LuaDLL.cs:39` 写着 `const string LUADLL = "xlua";`（非 iOS/WebGL 平台），
C# 侧通过 `[DllImport("xlua")]` 调进 C 层。**只有 ① 没有 ②**，运行时自然找不到这个库。
注意它**不是编译错误，而是运行时异常** —— 所以 Console 里那一堆错全部来自 `XLua/Examples` 的静态构造。

**② 步骤**

1. 复制 ① 源码（**你已完成**）：`xLua-master\Assets\XLua\*` → `Client\Assets\ThirdParty\XLua\`
2. **复制 ② 原生库（本次漏掉的一步）** —— 本项目只做 PC 64 位，所以只取 `x86_64`：

   ```powershell
   $src = 'E:\U3D Projects\0_MyFile\xLua-master\Assets\Plugins'
   $dst = 'E:\U3D Projects\0_MyFile\3D联网战斗Demo\Client\Assets\Plugins'
   Copy-Item "$src\x86_64"      $dst -Recurse -Force   # 目录本体（xlua.dll + libxlua.so + 它们的 .meta）
   Copy-Item "$src\x86_64.meta" $dst -Force            # ⚠️ 目录自己的 .meta 是"兄弟文件"，必须单独复制
   ```

   > ⚠️ **`x86_64.meta` 必须一起复制**：Unity 靠这个 `.meta` 里的平台设置决定"这个原生库给哪个平台用"。
   > 只复制目录、不复制 `.meta`，Unity 会新生成一个**默认设置**的 `meta`，可能把库判到错误平台。
   >
   > **只取 `x86_64` 的理由**：需求 §12 只做 PC，`x86`/`Android`/`iOS`/`WebGL`/`WSA`/`arm64`/`xlua.bundle`
   > 全是用不到的二进制（合计约 6 MB）。将来要做 32 位出包再补 `x86`。
   > （`x86_64` 里的 `libxlua.so` 是 Linux 版，Windows-only 项目可删掉它和它的 `.meta`，省约 440 KB。）

3. 回到 Unity，点一下窗口让它**重新导入** → 等编译完成
4. ⚠️ **然后必须完整重启 Unity**（关掉再打开工程）—— 见下面「⚠️ 坑 2」
5. 菜单 **XLua → Generate Code** → 生成 `XLua/Gen/` 下的静态绑定代码

**预期结果**
- `Client\Assets\Plugins\x86_64\xlua.dll` 存在（约 514 KB）
- Console **不再出现** `DllNotFoundException: xlua`
- 菜单栏出现 **XLua** 顶级菜单；`Generate Code` 无报错

> ### ⚠️ 坑 2：**加原生库必须重启 Unity，而且静态构造失败会"毒死"类型**（2026-09-20 实测）
>
> 现象：把 `xlua.dll` 复制进去后（**Unity 当时是开着的**），Console 仍报
> `DllNotFoundException: xlua`，这次位置变成 `CSObjectWrapEditor.Generator..cctor`（静态构造函数）。
> **怎么点菜单、怎么 `Assets → Refresh`、怎么重新导入都没用** —— 报的永远是
> `TypeInitializationException: The type initializer for 'CSObjectWrapEditor.Generator' threw an exception.`
>
> **两个叠加的原因**：
> 1. **原生插件是在会话启动时登记的**。会话开着的时候才把 DLL 放进工程，Mono 的原生库搜索路径没更新到 →
>    `[DllImport("xlua")]` 解析失败。（`Editor.log` 里 `Initialize engine version` 只出现 1 次 = 会话从未重启）
> 2. **C# 静态构造函数一旦抛异常，该类型在【当前域】内永久损坏**。之后任何用到它的地方都会重抛
>    `TypeInitializationException`，**不会重新执行静态构造** → 刷新、重导入都无效，**只有域重载/重启才能恢复**。
>
> **修法**：**关闭 Unity → 重新打开工程** → 再点 `XLua → Generate Code`。
>
> **如何自证"DLL 本身没问题"（本次用过的排查手法，可复用）**：
> 在普通 PowerShell 里直接加载它 —— 能把"文件问题"和"Unity 问题"分开：
> ```powershell
> Add-Type -Namespace NBC -Name Win -MemberDefinition @'
> [DllImport("kernel32", SetLastError=true, CharSet=CharSet.Unicode)]
> public static extern IntPtr LoadLibraryW(string p);
> [DllImport("kernel32", SetLastError=true)]
> public static extern IntPtr GetProcAddress(IntPtr h, string n);
> '@
> $p = "E:\U3D Projects\0_MyFile\3D联网战斗Demo\Client\Assets\Plugins\x86_64\xlua.dll"
> $h = [NBC.Win]::LoadLibraryW($p)
> "handle=$h  err=$([System.Runtime.InteropServices.Marshal]::GetLastWin32Error())"
> "导出 xlua_get_lib_version: $([NBC.Win]::GetProcAddress($h,'xlua_get_lib_version'))"
> ```
> 本次实测：`handle` 非 0、`err=0`、导出存在 ✅ → **证明 DLL、位数、路径（含中文）全都没问题，问题在 Unity 侧**。
>
> ### ⚠️ 坑 3：`libxlua.so` 被标成了 `Editor` 可用（`OS: AnyOS`）
> xLua 仓库自带的 `libxlua.so.meta` 里 `Editor: enabled 1, CPU x86_64, OS: AnyOS` —— 于是在 Windows 编辑器里
> `Refreshing native plugins compatible for Editor` 会数出 **2** 个插件（`xlua.dll` + 一个 Linux 的 `.so`）。
> 这**不是**本次报错的原因，但它是噪音。**本项目只做 Windows**，建议在 Inspector 里把 `libxlua.so` 的
> **Editor 勾去掉**（可逆），或直接删掉 `libxlua.so` + `libxlua.so.meta`。

> ⚠️ **版本握手（很重要）**：`XLua/Src/LuaEnv.cs:62` 有
> ```csharp
> const int LIB_VERSION_EXPECT = 105;
> // 不匹配就 throw new InvalidProgramException("wrong lib version expect:105 but got:...")
> ```
> → **C# 源码与原生库必须来自同一份源码树**。
> 如果你从别的项目（例如 `E:\U3D Projects\LearnXLua\`）拿 `xlua.dll`，报错会从
> `DllNotFoundException` 变成 `InvalidProgramException: wrong lib version expect:105 but got:XXX`。
> **务必用 `xLua-master` 里配套的那份。**

**验证（D6 的验收标准）**：`XLua → Generate Code` 完成且 Console 无报错。

> 📌 **关于 `Examples` / `Tutorial`**：它们占 212 个文件、会拉长编译时间，但**是学 xLua 最好的参考**
> （按规则 15 建议先留着）；打包前可以移出工程。**注意**：本次那批红色报错全部来自 Examples，
> 修好原生库后应当全部消失；若仍有个别报错，先怀疑是 Examples 里需要热更注入的示例
> （如 `08_Hotfix`），把报错原文贴出来我判断。

> 📌 **一个连带的坑已修**：`link.xml` 里原来写的 `<assembly fullname="XLua">` / `XLua.Example`
> **永远匹配不到任何程序集** —— 因为 xLua 全树没有 `.asmdef`，它的 C# 全部编进 **`Assembly-CSharp`**
> （实测 `Assembly-CSharp.dll` 里有 109 个 `XLua.*` 类型）。已改为 `Assembly-CSharp`，
> 详见 `Client\Assets\link.xml` 的注释与 `Docs\09-IL2CPP验证清单.md`。
>
> 📌 **跑完 `Generate Code` 后项目里有 3 份 `link.xml`，Unity 会全部合并**（实测确认）：
> ① `Assets/link.xml`（我们手写，粗粒度）② `Assets/XLua/Gen/link.xml`（xLua 自动生成，精到 `<type>`）
> ③ `Packages/com.arongranberg.astar/link.xml`（A\* 自带）。**②** 每次 Generate Code 都会被覆盖，**不要手改**。
> 生成产物落在 **`Assets\XLua\Gen\`（108 个文件）**（xLua 默认路径，不在 `ThirdParty` 下）—— 这是它的默认行为，保持原样即可。

### D7 · Google.Protobuf（网络序列化）👤

> 🔄 **2026-09-20 换库**：本节原为"导入 **protobuf-net** 4 个 DLL + 预编译序列化器"。
> D17 第一次出包实测证明 **protobuf-net 3.2.30 在 Unity 2022.3 + IL2CPP 下，只要消息含 `repeated`（集合）字段就必崩**
> （`NotSupportedException: ...RuntimeParameterInfo::GetTypeModifiers`；**不是裁剪问题，加 `link.xml` 无效**，
> "默认自动发现"与"显式 TypeModel"两条路都失败）。逐字堆栈见 `Docs\09-IL2CPP验证清单.md` **§五**。
> 现改为 **Google.Protobuf 3.21.1** —— `protoc` 在**编译期**生成显式 C#，**零运行时反射**，天然 AOT 安全。

**为什么换：一句话** —— 不再有"运行时构建类型模型"这回事，也就没有 AOT / 裁剪风险。
代价是**必须生成代码**，而生成命令要**版本配对**。

**① 放 2 个 DLL**（protobuf-net 时代是 4 个）

| DLL | 放到 | 说明 |
| --- | --- | --- |
| `Google.Protobuf.dll`（**3.21.1**） | `Client\Assets\ThirdParty\GoogleProtobuf\` | 主程序集（实测 `Google.Protobuf 3.21.1.0`） |
| `System.Runtime.CompilerServices.Unsafe.dll`（**6.0.0**） | 同上 | **Unity 不提供**（NetStandard shim 里没有），必须随行 |

> ⚠️ **不要放** `System.Memory.dll` / `System.Buffers.dll` / `System.Numerics.Vectors.dll` —— Unity 的 NetStandard shim 已提供，放进去可能造成版本回退。
> 取 DLL 的还原脚本与"怎么判断要不要放"见 `Docs\05-依赖清单.md` **§2.2**。

**② `.proto` 是协议的唯一来源**

| 项 | 位置 |
| --- | --- |
| `.proto` 源文件 | 仓库顶层 **`Protocol/`**（当前只有 `Protocol/nbc_probe.proto` —— M0 探针用，含标量 + `repeated int32`） |
| 生成的 C# | `Client\Assets\_Project\Protocol\`（当前 `NbcProbe.cs`，命名空间 **`NBC.Protocol`**） |
| 是否提交 | ✅ **生成产物提交进仓库**（文件头有 `<auto-generated>`），这样别人 clone 下来**不需要装 protoc** |

**③ 生成命令（在仓库根目录执行，用相对 ASCII 路径）**

```bat
cd /d "E:\U3D Projects\0_MyFile\3D联网战斗Demo"
"E:\U3D Projects\TeachNet\Protobuf\protoc.exe" --proto_path=Protocol --csharp_out=Client/Assets/_Project/Protocol Protocol/nbc_probe.proto
```

> ⚠️ **protoc 的版本必须与运行时配对**：本项目 = **protoc 3.21.1 ↔ Google.Protobuf 3.21.1**。
> 本机另有一份 `protoc 35.1`，它要配 Google.Protobuf **3.35.1** —— **本项目不用它**。

> ⚠️ **绝不要给 protoc 传含中文的路径**：`argv` 经 ANSI 转换后承载不了中文，会报
> `Could not make proto path relative ... No such file or directory`。
> 而**工作目录**可以是中文 —— 用**相对路径**就绕过了。原理与绕法见 `Docs\11-环境配置说明.md` 的**「protoc 生成协议代码」**一节。

**④ 编译符号 `NBC_HAS_PROTOBUF`**

- 探针用 `#if NBC_HAS_PROTOBUF` 判断"是否接入了序列化库"。**符号名刻意不含库名** —— 以后再换库不用改符号。
- `_Project/Editor/NBV0_DefineInitializer.cs` 会**自动检测 `Google.Protobuf.dll` 并注入**该符号，
  同时**主动清理遗留的旧符号 `NBC_HAS_PROTOBUF_NET`**（否则它会一直留在 Player Settings 里）。
- 自检：菜单 **Tools → NBC → 诊断 M0 探针依赖** → 应显示
  `Google.Protobuf : …\Assets\ThirdParty\GoogleProtobuf\Google.Protobuf.dll`。

**预期结果**

- `Client\Assets\ThirdParty\GoogleProtobuf\` 下有 **2 个 DLL**
- `Client\Assets\_Project\Protocol\NbcProbe.cs` 存在且头部是 `<auto-generated>`
- Console 无报错；编译符号里出现 **`NBC_HAS_PROTOBUF`**

**如果失败看什么**

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| `Reference has errors 'System.Runtime.CompilerServices.Unsafe'` | 少放了第二个 DLL | 把 `System.Runtime.CompilerServices.Unsafe.dll` 一起放进 `ThirdParty\GoogleProtobuf\` |
| protoc 报 `Could not make proto path relative` / `No such file or directory` | 给 protoc 传了**含中文的路径** | 改用**相对路径**（工作目录可以是中文） |
| 生成代码编译不过 / 运行期报错 | protoc 与运行时**版本不配对** | 确认两边都是 **3.21.1** |
| 探针显示"跳过：未定义 NBC_HAS_PROTOBUF" | 符号未注入 | 跑 `Tools → NBC → 诊断 M0 探针依赖`；必要时手动在 Player Settings 补 |

> 📌 **换库的连带收益**：`link.xml` 里 protobuf 的 **3 条条目已全部删除** —— 生成代码零反射，不需要裁剪保护。
> 现在 `link.xml` 只剩 1 条有效条目（`Assembly-CSharp`，为 xLua 保留）。

### D8 · Behavior Designer 经典版（行为树）👤

> ✅ **你已有离线包，不必走 Asset Store**：
> `E:\U3D Projects\0_MyFile\Behavior Designer - Behavior Trees for Everyone 1.7.13 u2022.3.unitypackage`
> 我已用 `tar` 读包内 `pathname` 只读核对过：**装到 `Assets/Behavior Designer/`，无 Pro / Entities / DOTS 痕迹 → 确认是经典版**。

1. Unity 菜单 **Assets → Import Package → Custom Package...** → 选上面那个 `.unitypackage` → **Import**
2. 导入时若弹出列表，可**取消勾选** `Integrations`（示例集成，M0 用不到，减少导入量）
3. ⚠️ **确认是经典版而不是 Pro 版**
   - 判断方法：经典版装在 **`Assets/Behavior Designer/`**，编辑器窗口菜单是 **Tools → Behavior Designer → Editor**
   - Pro 版会要求 **Entities** 包，且菜单是 **Tools → Behavior Designer Pro**
   - **两版序列化结构不同，不能中途切换**（需求文档 R14）

**预期结果**
- `Assets/Behavior Designer/` 出现（含 `Runtime/` 587 个文件、`Editor/` 9 个）
- 菜单栏出现 **Tools → Behavior Designer → Editor**
- 能打开空的行为树编辑器窗口

**验证**：把 `Assets/Behavior Designer/Runtime` 下的示例 Prefab 拖进场景，Play 一次，能看到节点高亮。**用完记得退出 Play。**

> 📌 **两件要提前知道的事**：
> 1. **运行时是 dll 形式**（`Runtime/BehaviorDesigner.Runtime.dll`），包里还带了一个
>    `Runtime Source Code Location.pdf` —— 意味着**源码不在这个包里**。需求文档 BT-09「插件原理笔记」要读底层实现时，
>    需要按那个 PDF 的说明另行获取源码。**别到时候才发现"读不到源码"。**
> 2. 它还会往 `Assets/Gizmos/` 放 2 个图标文件（`Behavior Designer * Icon.png`）。

### D9 · A\* Pathfinding Project Pro（客户端寻路）👤

> ✅ **你已有离线包**：`E:\U3D Projects\0_MyFile\A Pathfinding Project Pro 5.4.7.unitypackage`（68.8 MB）
> 我读包内的 `package.json` / `pathname` 核对后，发现了 **3 个和原手册不一样的事实**，请按下面做。

1. Unity 菜单 **Assets → Import Package → Custom Package...** → 选上面那个 `.unitypackage` → **Import**

**⚠️ 事实 1：它不再装到 `Assets/AstarPathfindingProject/`，而是 UPM 嵌入式包 `Packages/com.arongranberg.astar/`**

> 5.x 起 Aron Granberg 改成了 UPM 包分发。所以：
> - **验收判据要改**：不是找 `Assets/AstarPathfindingProject/`，而是确认
>   **`Client/Packages/com.arongranberg.astar/package.json` 存在**，且 Package Manager → **In Project** 里能看到 **A\* Pathfinding Project**
> - 它自带 `link.xml`（IL2CPP 防裁剪），这是好事

**⚠️ 事实 2：它会引入 3 个 DOTS 相关包 —— 这是本次导入最大的风险点**

`com.arongranberg.astar` 的依赖（读自它的 `package.json`）：

| 依赖包 | 版本 | 说明 |
| --- | --- | --- |
| `com.unity.burst` | **1.8.7** | **需要 C++ 编译器**（本机 `cl.exe` 已确认存在 ✅）；**会明显拉长 IL2CPP 出包时间**（影响 D17） |
| `com.unity.collections` | **1.5.1** | Unity 2022.3 可用 |
| `com.unity.mathematics` | **1.2.6** | Unity 2022.3 可用 |

**因此导入时 Unity 需要能访问 Unity 包注册表**（或其本地缓存）来拉这 3 个包。
如果拉不到，导入会卡住或报错 —— 这时候**先别乱点**，把 Console 报错贴给我。

**导入后请检查 `Client/Packages/manifest.json`**：

| 应该出现 | 不应该出现 |
| --- | --- |
| `com.unity.burst` / `com.unity.collections` / `com.unity.mathematics` | `com.arongranberg.astar`（嵌入式包**不进 manifest**，它是 Unity 自动发现的） |

> 这一条和「付费插件不进仓库」的决定直接相关：我们**忽略** `Client/Packages/com.arongranberg.astar/`，
> 但 `manifest.json` 与 `packages-lock.json` **要提交**。所以如果 `manifest.json` 里意外写进了 `com.arongranberg.astar`，
> 别人 clone 后会报"找不到包"。**请把这两个文件导入前后的变化告诉我，我来核对。**

**⚠️ 事实 3：官方示例场景带 `~` 后缀，Unity 不会导入**

包里的示例放在 `Packages/com.arongranberg.astar/ExampleScenes~/`（1528 个文件）。
**`~` 结尾的目录 Unity 一律不导入**（这是 UPM 的"发行包瘦身"惯例）。
想跑官方示例（M0 的「跑通插件官方示例」任务）时，需要**手动把 `ExampleScenes~` 改名成 `ExampleScenes`**，等 Unity 导入完再运行。

**验证**：新建场景 → 建一个空物体 → **Add Component → `AstarPath`** → 在其 Inspector 的 **Graphs** 标签页 → **Add New Graph → Grid Graph** → 点 **Scan**，Console 无报错。

> 📌 **以下三条是 2026-09-20 实测/读源码核对出来的（不是猜的）**：
>
> | 项 | 实测结果 |
> | --- | --- |
> | 组件名 | **`AstarPath`**。⚠️ 包内**不存在** `Pathfinder` 类（那是 3.x 的旧名），别再找它 |
> | 编辑器菜单 | 只有 **`Edit → Pathfinding → Scan All Graphs`**（快捷键 `Ctrl+Alt+S`）、`Link Pair` / `Unlink Pair` / `Delete Links`。**5.4.7 没有 `Window` / `Tools` 菜单** —— 图编辑与设置**内嵌在 `AstarPath` 的 Inspector 里** |
> | 程序集名 | `AstarPathfindingProject`（`AstarPathfindingProject.asmdef`）—— 后续别的程序集要引用它时用这个名字 |
>
> ⚠️ **它的 asmdef 带 `defineConstraints: [MODULE_BURST, MODULE_MATHEMATICS, MODULE_COLLECTIONS]`** ——
> 三个包缺任何一个，**整个 A* 程序集根本不会被编译**（这也是它一导入就弹"Missing dependencies"对话框的原因）。
> 本项目的 `packages-lock.json` 已解析到 `burst 1.8.11` / `collections 1.5.1` / `mathematics 1.2.6` ✅

### D10 · DOTween 👤

> ✅ **你已有离线包**：`E:\U3D Projects\0_MyFile\DOTween_1_3_030.unityPackage`
> 我读包内 `readme.txt` 核对过：**这是免费版**（不含 `DOTweenPro`），装在 `Assets/Plugins/Demigiant/DOTween/`。

1. Unity 菜单 **Assets → Import Package → Custom Package...** → 选上面那个包 → **Import**
2. ⚠️ **必须**打开 **Tools → Demigiant → DOTween Utility Panel** → 点 **Setup DOTween...**
   然后它会弹出 **Add/Remove Modules** 面板，按项目需要开关模块（Unity 系统模块 + 外部资源模块）
3. 代码里用 `using DG.Tweening;`

> 📌 **为什么 Setup 这步不能跳**：DOTween 的 readme 明确写了 ——
> 导入后要执行 Setup 才会**生成/激活模块**（它按"你实际用到的 Unity 子系统"裁剪模块，避免无用依赖）。
> 跳过的话常见后果是 `DOTweenModuleUI` 之类的模块缺失 → 用 UI 补间时报编译错误。

**预期结果**：菜单 **Tools → Demigiant → DOTween Utility Panel** 可打开并显示版本号。

**登记版本**：**DOTween 1.3.030（免费版）**

### D11 · 依赖版本登记 🤖 + 👤

1. 👤 你查看各插件版本号：
   - Unity：**Help → About Unity** 看版本
   - YooAsset / Cinemachine：**Package Manager** 里看
   - 各 Asset Store 插件：在 `Assets/<插件名>/` 下找 `README`/`version.txt`，或看 Package Manager → My Assets 的版本
2. 🤖 把版本号告诉我，我回填 `Docs\05-依赖清单.md`，完成 **M0 的"版本冻结"** 动作

---

## 五、阶段四：数据库准备

### D12 · 建库建表与测试数据 ✅（2026-09-20 已完成）

**前置**：MySQL80 服务已确认在运行（本机已核实）。

1. 打开命令行（`Win + R` → `cmd`）
2. 登录 MySQL：
   ```
   mysql -u root -p --default-character-set=utf8mb4
   ```
   输入你的 root 密码

   > ✅ **已探测确认**：`mysql` 客户端**已在 PATH 中**
   > （`C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe`），MySQL80 服务为 Running，
   > 因此直接用上面的命令即可，**不需要写完整路径**。
   > 加 `--default-character-set=utf8mb4` 是为了避免中文昵称写入乱码。

3. 执行建库脚本（脚本已生成到 `Docs\08-数据库脚本.sql`）

   > ⚠️ **2026-09-20 实测踩坑并修正**：**不要**用 `source` 喂本项目的路径，会报
   > `ERROR: Failed to open file '...', error: 42`。
   > `error: 42` 在 Windows CRT 里是 **`EILSEQ`（非法字节序列）** —— 即
   > **`mysql.exe` 自己打不开含非 ASCII 字符的路径**（路径编码转换失败），**与脚本内容无关**。
   > 已实测：`cmd` 能正常打开同一路径（`more < "该路径"` 读出全部 204 行），**只有 mysql 的 `fopen` 不行**。

   ✅ **方式 A（推荐）：用 cmd 的输入重定向，绕过 mysql 自己的文件打开代码**

   ```
   cd /d "E:\U3D Projects\0_MyFile\3D联网战斗Demo"
   mysql -u root -p --default-character-set=utf8mb4 < "Docs\08-数据库脚本.sql"
   ```

   > ⚠️ **必须在 `cmd` 里执行**：`<` 在 **PowerShell 5.1 里是保留操作符**，会直接报
   > "`<` 运算符保留给未来使用"，根本不会重定向。想用 PowerShell，就走方式 B。

   ⛔ **方式 B（仍想用 `source`）：把脚本复制到纯 ASCII 路径**（目录名与文件名都要 ASCII）

   ```
   mkdir C:\nbc
   copy "E:\U3D Projects\0_MyFile\3D联网战斗Demo\Docs\08-数据库脚本.sql" C:\nbc\nbc_db.sql
   ```
   ```
   mysql> source C:/nbc/nbc_db.sql
   ```
   > 缺点：副本会与 `Docs` 下的原文件**脱节**（改了原文件忘了重新复制，就会跑到旧数据上）。
   > 所以只在"需要留在 `mysql>` 交互里逐条看结果"时才用它。

   > ❌ **8.3 短路径这条路走不通**：实测 `for %I in ("<长路径>") do @echo %~sI` 返回的还是中文长名，
   > 说明 E: 盘**没有启用 8.3 别名**。

   > 脚本**可重复执行**（开头会 `DROP DATABASE IF EXISTS nbc_db`），开发期可随时重建。
   > ⚠️ 但这也意味着**重跑会清空数据**。

4. 验证：
   ```
   USE nbc_db;
   SHOW TABLES;
   SELECT a.username, a.salt, a.password_hash, p.nickname, p.level, p.gold
   FROM account a JOIN player_profile p ON p.account_id = a.account_id
   ORDER BY a.account_id;
   ```

**预期结果**
- `SHOW TABLES` 显示 4 张表：`account`、`player_profile`、`battle_record`、`battle_player_detail`
- `SELECT` 能查到 4 个测试账号，**明文密码统一为 `123456`**（库里存的是 `SHA256(salt + 密码)`，不是明文）
- 脚本末尾自带两条验证查询，会自动打印上述结果

**如果失败看什么**

| 报错 | 原因 | 处理 |
| --- | --- | --- |
| `Access denied for user 'root'` | 密码错误 | 用 MySQL Installer 重置，或换一个有权限的账号 |
| `Unknown database` | 脚本没先 `CREATE DATABASE`，或执行方式错 | 脚本首行就有 `CREATE DATABASE`；确认用的是「方式 A 的 `<` 重定向」 |
| **`Failed to open file '...', error: 42`** | **`mysql.exe` 打不开含中文/非 ASCII 的路径**（errno 42 = `EILSEQ`），**不是路径写错** | ✅ 用 **方式 A**（cmd 里 `<` 重定向）；或按方式 B 复制到纯 ASCII 路径再 `source` |
| `Failed to open file '...', error: 2` | 这才是**真的找不到文件**（errno 2 = `ENOENT`） | 核对盘符与相对路径；`<` 重定向时工作目录要对 |
| 用 `source` 时提示 `<` 运算符保留 | 在 **PowerShell** 里执行了 `<` 重定向 | PowerShell 5.1 不支持 `<`；改用 `cmd`，或走方式 B |
| 中文乱码 | 连接字符集不是 utf8mb4 | 登录时加 `--default-character-set=utf8mb4` |
| 外键创建失败 | 表创建顺序问题 | 脚本内表顺序必须是 account → player_profile → battle_record → battle_player_detail（已保证） |

### D12 实测记录（2026-09-20 通过）

**执行方式**：`cmd` 里的输入重定向（**不是** `source`）

```bat
cd /d "E:\U3D Projects\0_MyFile\3D联网战斗Demo"
mysql -u root -p --default-character-set=utf8mb4 < "Docs\08-数据库脚本.sql"
```

> **为什么不能用 `source`**：`source E:/.../3D联网战斗Demo/Docs/08-数据库脚本.sql` 会报
> `ERROR: Failed to open file '...', error: 42`。`error: 42` 是 Windows CRT 的 **`EILSEQ`（非法字节序列）**，
> 即 **`mysql.exe` 自己打不开含非 ASCII 字符的路径**（路径编码转换失败），**与脚本内容无关**。
> 实测对照：`cmd /c 'more < "<同一路径>"'` **退出码 0、读出全部行** → cmd 能打开，只有 mysql 不行。

**核验方式（AI 侧，全程不需要数据库凭据）**：直接看 MySQL 的物理数据目录

```powershell
Get-ChildItem 'C:\ProgramData\MySQL\MySQL Server 8.0\Data\nbc_db' -Force | Select-Object Name, Length, LastWriteTime
```

| 核验项 | 结果 |
| --- | --- |
| 库目录 `...\Data\nbc_db\` | ✅ 存在 |
| InnoDB 表数据文件 | ✅ **恰好 4 个 `.ibd`**：`account` / `player_profile` / `battle_record` / `battle_player_detail` |
| 与脚本的表是否一一对应 | ✅ **不多不少**（漏建或多建都会在这里暴露） |
| 创建时间 | ✅ 2026/9/20 **11:38:49 ~ 11:38:52**（本次执行时刻） |

**诚实边界**：`.ibd` 只能证明"**4 张表被真正建出来了**"，
**证明不了行数**（4 个测试账号）。行数需要 `SELECT`，而那要数据库凭据 —— 按规则 13，AI 不接触凭据。
**行数由你执行时脚本末尾自带的验证查询打印确认。**
（对比 D13/D16：那两步我能自己复现命令，所以能拿到逐字输出；D12 不行，这就是边界。）

---

## 六、阶段五：服务端骨架

### D13 · .NET 8 解决方案骨架 🤖 建文件 + 👤 build/run

> ✅ **AI 侧已完成**（2026-09-16）。实际生成的文件（比原计划略有扩充，都带注释说明设计意图）：

```
Server/
├─ NBC.sln                                  # VS2022 格式（17.x），5 个工程
├─ Directory.Build.props                    # 统一 LangVersion=12 / Nullable / 输出到 Builds/Server
├─ NBC.Shared/
│  ├─ NBC.Shared.csproj                     # ★ netstandard2.1（双端共享，绝不能改成 net8.0）
│  └─ SharedInfo.cs                         # 共享层标识与常量（逻辑帧率 30 / 快照率 20 / 契约版本）
├─ NBC.Server.Core/
│  ├─ NBC.Server.Core.csproj
│  ├─ ClientSession.cs                      # 会话与阶段状态机（SRV-03）
│  ├─ TickScheduler.cs                      # 定长帧调度器骨架（DET-03 / LOCK-01）
│  └─ INetTransport.cs                      # 传输抽象（单机模式的关键，§13.5）
├─ NBC.Server.Game/
│  ├─ NBC.Server.Game.csproj
│  └─ BattleInstance.cs                     # 战斗实例 + SyncMode 枚举（三种同步共用）
├─ NBC.Server.Host/
│  ├─ NBC.Server.Host.csproj                # 控制台，输出 NBC.Server.Host.exe
│  ├─ Program.cs                            # 入口：打印版本并验证 NBC.Shared 可引用
│  └─ appsettings.json                      # 端口 / 模拟参数 / 数据库 / 日志配置
└─ NBC.Server.Tests/
   ├─ NBC.Server.Tests.csproj
   └─ SkeletonSelfCheck.cs                  # 无 xUnit 的占位自检（M1 换成真测试）
```

> **M0 阶段刻意不引入任何 NuGet 包**（Google.Protobuf / Dapper / MySqlConnector / xUnit 都先注释掉），
> 目的是让 `dotnet build` **零还原、一次通过**。各依赖在对应里程碑再打开（`.csproj` 里已写好注释掉的 `PackageReference`）。

**👤 你的动作 1：编译**

```
cd "E:\U3D Projects\0_MyFile\3D联网战斗Demo\Server"
dotnet build
```

**预期结果**：`Build succeeded. 0 Error(s)`（有几条 Warning 可以接受，把 Warning 贴给我看即可）

**👤 你的动作 2：运行一次验证骨架贯通**

```
dotnet run --project NBC.Server.Host
```

**预期输出**（应看到类似内容）：

```
==============================================
  NBC Server (Networked Battle Combat)
  Version : 0.1.0.0
  Runtime : ...
  OS      : ...
  Machine : ...
==============================================

[Check] NBC.Shared 可引用，共享层版本标识：NBC.Shared

[M0] 骨架启动成功。以下模块将在后续里程碑接入：
     M1: 日志 / 定点数学（NBC.Shared）
     M3: 配置加载 / 数据库 / TCP 网络层 / 房间流程
     M4: 定长帧循环 / 三种同步策略

按 Ctrl+C 退出（M0 阶段会立即结束）。
```

> **这一行 `[Check] NBC.Shared 可引用` 的意义**：它证明 `netstandard2.1` 的共享层能被 .NET 8 服务端引用。
> 这是"双端共享同一份战斗逻辑"（需求文档 §4.2 R5）的**第一条机械证据** —— 另一半证据在 D17 的 Unity 侧。

**如果失败看什么**

| 报错 | 原因 | 处理 |
| --- | --- | --- |
| `error MSB4025: 未能加载项目文件` | `.csproj` 格式问题（AI 生成的文件若被误改会触发） | 把完整报错贴给我 |
| `error NETSDK1045: 当前 .NET SDK 不支持 net8.0` | SDK 版本不对 | 本机有 8.0.204，用 `dotnet --list-sdks` 确认 |
| `NBC.Shared` 相关的编译错误 | 共享层目标框架被误改 | 确认是 `netstandard2.1` |
| `dotnet run` 报找不到工程 | 工作目录不对 | 在 `Server\` 目录下执行，或用完整路径 |

> ⚠️ **`NBC.Shared` 必须是 `netstandard2.1`**：它要同时被 Unity（.NET Standard 2.1）和 .NET 8 引用。
> 若设成 `net8.0`，Unity 将无法引用它，双端共享逻辑就断了（需求文档 §4.2 R5）。

---

## 七、阶段六：验证插件架构边界（M0 的关键实验）

### D14 · 证明"依赖 Unity 运行时的东西进不了纯 .NET 服务端" ✅（2026-09-20 已完成）

> **状态**：✅ **已完成**。你在 2026-09-20 把这一步委托给我代跑（"这儿实验你来做吧"），
> 它在规则 13 的边界内（**只做 .NET 编译与运行，完全不碰 Unity 编辑器**）。
> 完整记录：`Docs\01-项目需求文档.md` **§5.4.6**；操作单与复现步骤：`Docs\12-D14实验操作单.md`。

**这一步要回答的问题**：Behavior Designer 的运行时建立在 `MonoBehaviour` / `GameObject` / 时间系统之上，
**它能不能在独立 .NET 控制台服务端进程里工作？**

**做法（比原计划更根本）**：原计划是"临时引用 Behavior Designer 的 dll 看编译报错"，但那样测的是
"**这个插件**行不行"，结论会被插件版本影响。所以改成**向下挖一层** ——
新建独立探针工程 `Server\_unity-ref-probe\`，**直接引用 Unity 自己的程序集**，
用 9 级探针测出"托管代码 / 引擎原生"的精确分界（该工程不在 `NBC.sln` 里，不影响正常构建）。

**实测结果（3 / 9 级失败）**：

| 分层 | 能否在纯 .NET 进程工作 | 证据 |
| --- | --- | --- |
| 纯托管数学（`Vector3` / `Mathf`） | ✅ **能，且结果正确** | `magnitude=5`、`Normalize=(0.6,0.8,0)`、`Mathf.Sqrt(16)=4` |
| 类型元数据（反射取 `Type`、基类链） | ✅ **能** | `MonoBehaviour` 的基类链 `Behaviour → Component` 可读出 |
| **引擎对象 / 原生调用** | ❌ **不能** | `new GameObject()`、`go.name`、`Time.deltaTime` 全部抛异常 |

**报错原文（这就是本次实验要的"实测证据"）**：

```text
System.Security.SecurityException: ECall methods must be packaged into a system module.
```

**关键判读**：这是**运行时**异常而不是编译期错误 —— **引用 Unity 程序集能编译通过**，
失败只发生在**真正调用引擎**的那一刻。所以"编译成功"不能用来证明"服务端能跑行为树"。

**结论**：架构裁决 **A1/A2/A3 成立**（AI 分两套实现、帧同步不由服务端跑 AI、寻路分两套）。
`Docs\01-项目需求文档.md` §5.4.5「事实 1」已据此从**推断**改写为**实测**，
并**收窄了原来的过头表述**（原来写"引用不了它"，准确说法是"能引用、纯托管能跑、一碰引擎对象就抛 `ECall`"）。

> **面试价值**：这段"我写了个 9 级探针，量出了托管与引擎原生的分界线"的经历，
> 比"我看文档说它不能"有力得多。可直接背的讲法见 `Docs\12-D14实验操作单.md` §八。
>
> **想自己重跑一遍**（推荐，规则 15）：
> ```powershell
> cd "E:\U3D Projects\0_MyFile\3D联网战斗Demo\Server\_unity-ref-probe"
> dotnet run
> # 预期：最后一行 "失败级别数: 3 / 9"，退出码 1（设计如此）
> ```

---

## 八、阶段七：Unity 工程骨架

### D15 · 目录结构与基础文件 🤖

**AI 侧动作**（按需求文档 §2.4 的目录约定）：

在完成了 D1 的 `Client\` 工程之后，新建：

```
Client/Assets/
├─ _Project/
│  ├─ Framework/            # 框架底座（例外 E1，来自唐老师框架）
│  │  ├─ Singleton/  Event/  Pool/  Mono/  Scene/  Resource/  Input/  Audio/  UI/
│  │  ├─ Timer/  Log/  PerfHUD/
│  │  └─ Framework.asmdef
│  ├─ Game/
│  │  ├─ Model/  View/  Controller/  Service/
│  │  └─ Game.asmdef
│  ├─ Configs/              # 生成的 SO 配置资产
│  ├─ Editor/               # 配置表工具、YooAsset 分组配置
│  ├─ Tests/                # EditMode / PlayMode
│  └─ Art/
├─ LuaScripts/              # Lua 业务脚本（热更对象）
├─ ThirdParty/              # xLua / Google.Protobuf（你已导入）
└─ Scenes/
```

**同时新建的根级文件**：

| 文件 | 用途 |
| --- | --- |
| `.gitignore` | Unity 标准（忽略 `Library/`、`Temp/`、`obj/`、`Builds/`、`*.csproj`、`*.sln` 等） |
| `.gitattributes` | 强制文本换行与 LFS 规则（大文件走 LFS，可选） |
| `README.md` | 项目说明、如何运行、技术亮点（M6 会扩充） |
| `Docs\08-数据库脚本.sql` | 需求文档 §13.4 的四张表 + 测试数据 |
| `Docs\09-IL2CPP验证清单.md` | D17 的检查清单 |

> **asmdef 的 `noEngineReferences`**：`NBC.Model` 这类纯逻辑程序集要设 `noEngineReferences: true`，
> 用机械手段保证"Model 层不引用 UnityEngine"（需求文档 §14.3 MOD-01/02）。**本期先建，内容在 M1 填。**

### D16 · Git 初始化 👤

> 按规则 13，`git` 的写操作由你执行；AI 只做只读分析（`git status`/`log`/`diff`）。

**第 0 步：先确认"该忽略的都被忽略了"（用地面真值，别只看 `.gitignore` 文本）**

```powershell
cd "E:\U3D Projects\0_MyFile\3D联网战斗Demo"
git init
git add --dry-run .        # 列出"将被加入仓库"的文件
```

**`git add --dry-run .` 的输出里必须没有**：`Library/`、`Temp/`、`obj/`、`bin/`、`Builds/`，
以及三个付费插件目录（见下面 ⚠️）。

> 📌 **别用 `git check-ignore -v` 做这个判断**：它会把被 `!` 取反规则命中的路径也打印出来且退出码为 0，
> 容易误读成"已忽略"。判据要用"**最终会不会进仓库**"。详见 `Docs\00-项目工作规则.md` 的 **W6**。

**第 1 步：提交文档与服务端基线**

```powershell
git add .gitignore .gitattributes README.md Docs/ DeepSeekOutput/ Server/ ClientStaging/ Tools/
git commit -m "chore(m0): 初始化项目仓库与文档基线"
```

**预期结果**：`git log` 显示 1 条提交。

> ⚠️ **本次不要 `git add Client/`**：Unity 工程刚创建，且**付费插件还没导入**。
> 等 D8/D9/D10 导入完成、你确认 `git add --dry-run .` 输出干净之后，再单独提交 `Client/`。
>
> ⚠️ **付费插件不进仓库（2026-09-20 决定）**：Behavior Designer（Commercial License，按席位）、
> A\* Pathfinding Project Pro（Commercial License）、DOTween 都**不再分发**，
> 所以 `.gitignore` 已按**实测落地路径**忽略：
>
> | 插件 | 落地路径（实测） |
> | --- | --- |
> | Behavior Designer | `Client/Assets/Behavior Designer/` + `Client/Assets/Gizmos/Behavior Designer*` |
> | A\* Pathfinding Project Pro | `Client/Packages/com.arongranberg.astar/`（5.x 起是 UPM 嵌入式包） |
> | DOTween | `Client/Assets/Plugins/Demigiant/` |
>
> 代价：**clone 下来不含这三个插件**，导入方法写在仓库根 `README.md` 的「第三方插件如何导入」一节。
>
> 需要 push 时按**规则 7**：先给我看提交列表与目标分支，你明确同意后才 push。

---

## 九、阶段八：IL2CPP 打包验证（M0 的最终关卡）

### D17 · 出包并运行 👤

> 这一步的核心目的：**在项目还没写几行代码时，就把 AOT/裁剪问题暴露出来**。
> 现在发现问题，改起来是几小时；等到 M6 才发现，可能是几周。

**步骤**

1. 菜单：**File → Build Settings**
2. **Platform** 选 **Windows, Mac, Linux** → Target Platform **Windows** → Architecture **x86_64**
3. 点 **Switch Platform**（等编译完成）
4. 点 **Player Settings** → 确认 D2 的设置还在（尤其 **IL2CPP** + **.NET Standard 2.1** + **Stripping Level: Low**）
5. 关闭 **Development Build** 和 **Script Debugging**（这次要验证 Release 包）
6. **准备 AOT 验证场景**（⚠️ 2026-09-20 修正：这一段原来写"这个脚本我（AI）会写好给你"，
   而脚本**早就写好了** —— 真正缺的是**场景**：文档里点名的 `Scene_Test_AOT` **从未被创建**）

   **① 先确认编译符号已注入**（否则对应探针会被编译掉 → **假通过**）
   - 菜单 **Tools → NBC → 诊断 M0 探针依赖**，看输出里的两行依赖与"当前编译符号"
   - 期望：`Google.Protobuf : …\Assets\ThirdParty\GoogleProtobuf\Google.Protobuf.dll`、
     `xLua : …\Assets\Plugins\x86_64\xlua.dll`，且编译符号同时含
     **`NBC_HAS_PROTOBUF`**、**`NBC_HAS_XLUA`**、`DOTWEEN`
   - ⚠️ **不应再出现旧符号 `NBC_HAS_PROTOBUF_NET`**（换库前的名字；初始化器会主动清理它）
   - 这两个符号由 `_Project/Editor/NBV0_DefineInitializer.cs` **自动维护**（检测到 dll 就加、没有就删；
     它只增删自己那两个符号，**不会动 `DOTWEEN`**）。若没注入，把诊断输出贴给我
   - 兜底：也可手动在 **Player Settings → Other Settings → Scripting Define Symbols** 里补

   **② 新建探针场景并挂脚本**
   - **File → New Scene**（空场景即可）→ **Ctrl+S** 保存为 `Assets/Scenes/Scene_AOTProbe.unity`
   - **GameObject → Create Empty**（命名如 `AOTProbe`）
   - 选中它 → **Add Component** → 搜 **NBV0** → 选 **`NBV0_EnvironmentProbe`**
     （脚本在 `Assets/_Project/Tests/Manual/NBV0_EnvironmentProbe.cs`；两个开关
     `testProtobuf` / `testXLua` 默认都勾着）
   - **Ctrl+S** 保存

   **③ 设为唯一的构建场景**
   - **File → Build Settings** → 把 `Scene_AOTProbe` 拖进 **Scenes In Build** 并勾选，确保在**第 1 位**
   - M0 阶段建议**只留它**，把 `SampleScene` 取消勾选（对本步验证没有用处）
7. 点 **Build** → 输出目录 `E:\U3D Projects\0_MyFile\3D联网战斗Demo\Builds\Client\`
8. 双击运行 `Builds\Client\NBC.exe`

**验收标准**

| 检查项 | 通过标准 |
| --- | --- |
| 打包过程 | 无报错，生成 `NBC.exe` + `UnityPlayer.dll` + `NBC_Data/` |
| 运行 | 游戏能启动，不闪退 |
| Unity 日志 | 无异常 |
| **protobuf（Google.Protobuf）** | 序列化往返**数据一致**（这是 R4 的直接验证） |
| **xLua** | Lua 脚本能执行，"lua ok" 出现在日志中 |

**如果失败看什么**

| 现象 | 大概率原因 | 处理 |
| --- | --- | --- |
| 打包就失败，提示找不到 C++ 编译器 | 缺 VS2022 的 **"使用 C++ 的桌面开发"** 工作负载 | 打开 VS Installer → 修改 → 勾选该工作负载 |
| 打包失败提示 Windows SDK | 缺 Windows 10/11 SDK | VS Installer 里勾选对应 SDK |
| 打包报 `Reference has errors 'System.Runtime.CompilerServices.Unsafe'` | **少放了 DLL** | 见 D7 ①：`ThirdParty\GoogleProtobuf\` 应有 **2 个** DLL |
| 探针显示"跳过：未定义 NBC_HAS_PROTOBUF" | 编译符号没注入 | 见本步 ①；检查 `Google.Protobuf.dll` 是否在 `Assets` 下 |
| Lua 调用报 `attempt to call a nil value` | xLua 绑定代码未生成 | 菜单 **XLua → Generate Code** 后重新打包 |
| Lua 相关类型被裁掉 | Stripping Level 太高 | 保持 **Low**，或加 `link.xml` 保留 |

> **`link.xml` 提示**：如果 Low 仍被裁剪，在 `Assets/` 下建 `link.xml` 声明保留 **xLua** 的程序集。
> **Google.Protobuf 不需要条目**（生成代码是普通 C#、零反射）。

---

## 十、附：本机多客户端联机的网络前置（M3 会用到，M0 先规划）

M0 现在不需要联网，但**网络环境要在写代码前就定清楚**，否则 M3 第一次联调时会卡在"连不上"上，
而那种问题排查起来很浪费时间（分不清是防火墙、绑定地址还是协议问题）。

### 需要你决定的三件事

| # | 决定 | 影响 |
| --- | --- | --- |
| **N1** | **联机范围**：① 同一台机器多开（4 个客户端 + 1 个服务端）② 局域网两台机器 ③ 都要 | 决定服务端监听地址与是否放行防火墙 |
| **N2** | **服务端监听地址** | 同机多开用 `127.0.0.1` 即可，**不会触发防火墙提示**；局域网要监听 `0.0.0.0`，**首次会弹防火墙授权** |
| **N3** | 演示时用哪种 | 面试现场通常只能用一台机器 → 建议**优先保证同机多开**，局域网作为加分 |

### 建议方案（推荐，改动最小）

**先用同机多开把联网打通**（`127.0.0.1`），等 M3 联调通过后再处理局域网：

| 项 | 值 | 说明 |
| --- | --- | --- |
| 服务端监听 | 配置文件 `ListenIp`，README/手册统一写 `127.0.0.1` | 同机多开足够；已写在 `Server/NBC.Server.Host/appsettings.json` 的 `Server.ListenIp` |
| 端口 | `9000`（已在配置文件里） | 避免与常用端口冲突；可在配置文件改 |
| 客户端连接目标 | `127.0.0.1:9000` | — |
| 防火墙 | **无需改动** | 环回地址不走防火墙 |

### 若要做局域网联机（M3 之后再处理）

1. 把 `appsettings.json` 的 `Server.ListenIp` 改为 `0.0.0.0`
2. 首次启动服务端时 Windows 会弹出防火墙授权 → 勾选 **专用网络**（不要勾公用网络）
3. 查本机内网 IP：命令行执行 `ipconfig`，找 `IPv4 地址`（形如 `192.168.x.x`）
4. 客户端连接目标改成该 IP
5. **排查顺序**（连不上时按此顺序查，别乱试）：
   - ① 服务端是否真的在监听 → 命令行 `netstat -ano | findstr :9000`
   - ② 本机能连自己吗 → `Test-NetConnection 127.0.0.1 -Port 9000`
   - ③ 对方能 ping 通吗 → `ping 192.168.x.x`
   - ④ 端口在对方机器通吗 → `Test-NetConnection 192.168.x.x -Port 9000`
   - ⑤ 都通但连不上 → 才是代码/协议问题（这时才该来找我）

### 同机多开的注意事项（M3 会用）

| 问题 | 原因 | 应对 |
| --- | --- | --- |
| 多个客户端抢同一个存档/配置目录 | Unity 的 `persistentDataPath` 按**公司名+产品名**生成，多开是同一个目录 | 启动参数传入"实例号"，用它给可写目录加子目录（如 `.../NBC/Instance1`）。**这条要在 M1 设计热更可写目录时就预留** |
| 多开后帧率掉 | 4 个客户端 + 1 个服务端同时跑，机器负载高 | 演示时把客户端画质调到 **Low** 档（需求文档 OPT-13 的三档正好派上用场） |
| 服务端日志混在一起 | 多客户端连到同一个服务端，日志需要区分 session | 日志每行带 `sessionId`（§13.3 SRV-14 已要求） |

> **M0 阶段你不需要做任何网络配置**，只要知道 N1 的答案（联机范围）并告诉我即可，我会在 M3 按它来定实现。

---

## 十一、M0 验收清单

全部打勾才进入 M1：

- [x] **D1** Unity URP 工程创建成功，`Client\` 目录就位
      —— ✅ **2026-09-20 实测通过**（**创建时**版本 `2022.3.17f1c1 (a01dafa986ec)`；URP **14.0.9**；模板自带 `Assets\Settings\` 下 Performant/Balanced/HighFidelity 三套 URP Asset；Test Framework 1.1.33 已装；`.gitignore` 覆盖经 26 项模拟验证全部正确）
      —— ⚠️ **后续变更**：同日因 A\* 5.4.7 要求 **Unity ≥2022.3.21**（需求文档 **R16**），工程升级到 **`2022.3.62f3c1`**；升级后 **11 项回归全部通过**（Quality 仍是 Low/Mid/High、颜色空间仍 Linear、脚本后端仍 IL2CPP、URP 绑定未丢、protobuf DLL 与 6 个 asmdef 均在 —— 当时是 protobuf-net 4 个，**换库后为 Google.Protobuf 2 个**）。完整核对见 `Docs\11-环境配置说明.md` **§6.5**
- [x] **D2** Player Settings：IL2CPP / .NET Standard 2.1 / Stripping Low / unsafe 允许
      —— ✅ **2026-09-20 实测通过**（`companyName=NBC`、`productName=NBC`、`scriptingBackend: Standalone=1`(IL2CPP)、`managedStrippingLevel: Standalone=1`(Low)、`allowUnsafeCode=1`、`apiCompatibilityLevel=6`(.NET Standard 2.1)、`activeInputHandler=0`(旧版 Input)）
- [x] **D3** Graphics 指向有效 URP Asset；Quality 只保留 Low/Mid/High 三档
      —— ✅ **2026-09-20 实测通过**（三档已改名 `Low`/`Mid`/`High` 并已写盘；各档分别绑定 `URP-Performant`/`URP-Balanced`/`URP-HighFidelity`；Graphics 默认管线指向 `URP-HighFidelity`；Color Space = **Linear**）
- [x] **D4** Cinemachine、Test Framework 已装
      —— ✅ **2026-09-20 实测通过**（Cinemachine **2.9.7**（当时与 `RPG_Git` 同版本）；Test Framework 1.1.33 模板自带）
      —— ⚠️ **后续变更**：升级 Unity 到 `2022.3.62f3c1` 时，编辑器**自动**把 Cinemachine 顶到 **2.10.3**、Test Framework 仍 1.1.33。已按实际版本登记（`Docs\05-依赖清单.md` C10），"与 RPG_Git 对齐"不再成立
- [x] **D5** YooAsset 安装成功，`YooAsset` 菜单可用
      —— ✅ **2026-09-20 实测通过**（**3.0.5**，来源 `registry`（OpenUPM）；其依赖 `com.unity.scriptablebuildpipeline` **1.21.25** 已自动安装）
- [x] **D6** xLua 导入成功，`XLua → Generate Code` 无报错
      —— ✅ **2026-09-20 实测通过**（重启 Unity 后 `XLua → Generate Code` 成功）
      —— 产物：**`Client\Assets\XLua\Gen\`（108 个文件）**，含 `DelegatesGensBridge.cs` / `EnumWrap.cs` / `PackUnpack.cs` / 各类 `*_Wrap.cs`
      —— 原生库就位：**`Client\Assets\Plugins\x86_64\xlua.dll`**（525,824 B，含配套 `.meta`）
      —— ⚠️ **过程中踩了两个坑**（已写入本节 D6）：① 原文档把 `Plugins` 错写成在 `XLua` **里面** → 漏掉原生库；
      ② 加原生库后**必须重启 Unity**，否则 `Generator..cctor` 会被 `DllNotFoundException` **永久毒死**，刷新无效
      —— 📌 版本：源码为 GitHub `master`（`CHANGELOG.txt` 最新条目 **v2.1.15**）；原生库取自**同一份源码树**（`LIB_VERSION_EXPECT = 105` 版本握手）
      —— 📌 **DLL 本身已用 `LoadLibraryW` 实测无问题**（`err=0`、导出表含 `xlua_get_lib_version`、`Machine=Amd64`、只依赖 `KERNEL32`/`WS2_32`）
- [x] **D7** 序列化库就位：**Google.Protobuf 3.21.1 + `System.Runtime.CompilerServices.Unsafe` 6.0.0（共 2 个 DLL）**，`.proto` 与生成代码就位，编译符号 `NBC_HAS_PROTOBUF` 生效
      —— ✅ **2026-09-20 换库后完成**：`Client\Assets\ThirdParty\GoogleProtobuf\`（2 个 DLL，实测程序集 `Google.Protobuf` **3.21.1.0** / `Unsafe` **6.0.0.0**）；`Client\Assets\_Project\Protocol\NbcProbe.cs`（`<auto-generated>`，`protoc` 3.21.1 生成）
      —— 🔄 **本步原是 protobuf-net 4 个 DLL**（`protobuf-net` / `.Core` / `System.Collections.Immutable` / `Unsafe`），已随换库全部移除；原因与证据见本节 D7 正文与 `Docs\09-IL2CPP验证清单.md` §五
- [x] **D7b** 运行 `Tools\Verify-PluginDlls.ps1`，确认依赖闭包齐全、`link.xml` 条目全部匹配
      —— ✅ **2026-09-20 换库后重跑通过**（依赖闭包 **2/2** `[OK]`：`Google.Protobuf` + `System.Runtime.CompilerServices.Unsafe`；`link.xml` 只剩 1 条 `Assembly-CSharp` 且匹配；**Google.Protobuf 已登记为"无需 link.xml 条目"的豁免项**）
      —— 📌 换库前该步是"闭包 4/4 + 3 条 protobuf 条目匹配"，已随换库作废
- [x] **D8** Behavior Designer **经典版**（离线包 `1.7.13 u2022.3`）导入成功，示例可 Play
      —— ✅ **2026-09-20 实测通过**（你确认菜单 **Tools → Behavior Designer → Editor** 可开、示例可 Play；AI 磁盘核对 **1149 个文件**）
      —— ⚠️ **两点实测事实**（已写入 `Docs\05-依赖清单.md` C4）：① DLL 的 `FileVersion` 是 `0.0.0.0`，**版本只能以离线包名 `1.7.13` 为准**；② 纯预编译 DLL、**无 asmdef**，后续按程序集名 **`BehaviorDesigner.Runtime`** 引用
      —— ⚠️ **路径多了一层**：实际在 `Client/Assets/Behavior Designer/Behavior Designer/Runtime/…`（包内 pathname 是 `Assets/Behavior Designer/Runtime/…`，606 条已核对 → **是导入产生的嵌套**，非包本身结构）。**功能不受影响**；可选清理方式见 C4 的 📌
- [x] **D9** A\* Pathfinding Project **Pro**（离线包 `5.4.7`）导入成功，Grid Graph 可 Scan
      —— ✅ **2026-09-20 实测通过**（你确认 `AstarPath` + Grid Graph 可 Scan；AI 核对 **4178 个文件**、`source=embedded`、三个依赖 `burst 1.8.21` / `collections 1.5.1` / `mathematics 1.2.6` 全部就位）
      —— ✅ 那条 `2022.3.21` 告警已在 Unity 升级到 `2022.3.62f3c1` 后消失
      —— 📌 程序集名 **`AstarPathfindingProject`**（源码形态 421 个 `.cs`）
- [x] **D10** DOTween（离线包 `1.3.030`）导入 + **Utility Panel → Setup DOTween** 完成
      —— ✅ **2026-09-20 实测通过**（你确认 Utility Panel 可开；AI 核对 45 个文件，且 **`DOTWEEN` 编译符号已写入所有平台** —— Setup 的产物）
      —— 📌 当时的符号串是 `Standalone: NBC_HAS_PROTOBUF_NET;DOTWEEN`；**换库后 protobuf 那个符号已改名为 `NBC_HAS_PROTOBUF`**（DOTween 不受影响）
      —— 📌 `DOTween.dll` 无版本戳（`FileVersion=1.0.0.0`），版本以包名 `1.3.030`（免费版）为准
- [x] **D11** 所有版本号回填 `Docs\05-依赖清单.md`（**版本冻结完成**）
      —— ✅ **2026-09-20 实测完成**（D6 完成后补齐最后一项）：Unity / UPM 包读 `packages-lock.json`、插件读离线包 `package.json`/`readme.txt`、DLL 读 `FileVersionInfo`，**全部实测值**
      —— ✅ **C3 xLua 已回填 `v2.1.15`**，并新增 **C3a（原生库 `xlua.dll`）**行与版本握手说明
      —— 📌 取值口径与逐项结果见 `Docs\05-依赖清单.md` **§五**
- [x] **D12** MySQL `nbc_db` 四张表建立，测试账号（密码 `123456`）可查
      —— ✅ **2026-09-20 实测通过**（AI 用**免凭据的物理核验**确认：`C:\ProgramData\MySQL\MySQL Server 8.0\Data\nbc_db\`
      下恰好 4 个 `.ibd`：`account` / `player_profile` / `battle_record` / `battle_player_detail`，创建时间 11:38:49~52。
      记录见本节末「D12 实测记录」）
- [x] **D13** `dotnet build` 成功，`dotnet run` 打印出 `[Check] NBC.Shared 可引用`
      —— ✅ **2026-09-16 实测通过**（记录见本节末「D13 实测记录」）
- [x] **D14** 插件边界实验完成，**报错原文已记录**
      —— ✅ **2026-09-20 实测完成**（探针工程 `Server\_unity-ref-probe\`，9 级探针 **3 级失败**；
      报错原文 `System.Security.SecurityException: ECall methods must be packaged into a system module.`；
      完整记录见需求文档 **§5.4.6**）
- [x] **D15** 目录结构（14 个目录）、asmdef、验证脚本就位，且用 §1.3 第四步的脚本**逐项验证过 23 项**
      —— ✅ **2026-09-20 实测通过**（23 项全就位；6 个 asmdef 均已被 Unity 识别并生成 `.meta`；`NBC.Editor.dll` 8.5KB 已编译出来 → 证明 asmdef 生效；Console 无红色报错）
      —— 附带：**AnkleBreaker Unity MCP 2.39.5 已装入 Client**，bridge 已注册在端口 7890（`instances.json` 记录 projectName=`NBC`）
- [x] **D16** `git init` + 首次提交完成，`git status` 中**无** `Library/`/`bin/`/`obj/`
      —— ✅ **2026-09-20 实测通过**（提交 `f83771d`，47 个文件；已跟踪清单中**无** `Library/`/`Temp/`/`obj/`/`bin/`/`Builds/`/付费插件/`Client/`。完整记录见本节末「D16 实测记录」）
- [x] **D17** **IL2CPP 出包可运行，protobuf 与 xLua 探针均"通过"**
      —— ✅ **2026-09-20 第 3 次出包通过**（Unity `2022.3.62f3c1` / WindowsPlayer / **IL2CPP** / **Release**）
      —— **protobuf（Google.Protobuf）通过**：**49 字节**；`Id=1001 Name=联网战斗Demo Hp=3200`、`SkillIds=8 个（首个=2001）`、
      `id/name/hp/ratio/flag` 全 True、**`列表=True/True`**（← 这正是前两次让 protobuf-net 必炸的**集合字段**形状）
      —— **xLua 通过**：Lua 求和 1..10 = **55**
      —— 📌 **达成路径（这是本步的价值所在，共三次出包）**：
      ① **第 1 次** → 出包/运行成功、xLua ✅，但 **protobuf-net ❌**（`GetTypeModifiers` icall 未实现）
      ② **第 2 次** → 用 **4 条路矩阵**证明 protobuf-net 的"默认自动发现"与"显式 TypeModel"**两条都失败**，
      且触发条件是**集合字段**；并确认**不是代码裁剪**（加 `link.xml` 无效）
      ③ **决策换库** → `Google.Protobuf 3.21.1`（`protoc` 生成纯 C#、零反射）；DLL 4 个 → **2 个**、`link.xml` 少 **3** 条
      ④ **第 3 次** → **一次通过**（protobuf + xLua 双双通过）
      —— 📌 完整记录（三次出包 + 逐字堆栈 + 回填结论）见 `Docs\09-IL2CPP验证清单.md` **§五**

### ⚠️ 额外检查项（2026-09-16 实测新增，不阻塞 M0 但阻塞 M1）

- [ ] **N-1** 解决 **NuGet 还原被 TLS/凭证阻塞**的问题（见 `Docs\05-依赖清单.md` §三·补）
      —— M0 骨架不含 NuGet 包所以能编译，但 M1 一引包就会失败

### D16 实测记录（2026-09-20 通过）

**提交**：`f83771d (HEAD -> master) chore(m0): 初始化项目仓库与文档基线`
**规模**：**47 个文件，10327 行新增**；无远端（未 push，符合规则 7）

**审计结果（AI 只读核对，命令见下）**：

| 检查项 | 结果 |
| --- | --- |
| 提交内容里有无 `Library/` / `Temp/` / `obj/` / `bin/` / `Builds/` | ✅ **无** |
| 提交内容里有无付费插件（Behavior Designer / `com.arongranberg.astar` / Demigiant） | ✅ **无** |
| 提交内容里有无实验脚手架（`_unity-ref-probe` / `_nupkg-verify`） | ✅ **无** |
| `Client/` 是否被提前提交（本次应"否"） | ✅ **未被提交** |
| 已跟踪的 47 个文件有无"被修改/删除"（换行符归一化抖动） | ✅ **无** |

**关键证据 —— `.gitignore` 在真实 Unity 工程上真的生效**：
`Client/` 未跟踪清单只有 **106 条**（`Assets` 78 / `ProjectSettings` 26 / `Packages` 2），
里面**完全没有 `Library/`**。如果 `Library/` 没被忽略，条目会是**几万条**。

**审计用的只读命令（可复现）**：

```powershell
cd "E:\U3D Projects\0_MyFile\3D联网战斗Demo"
git log --oneline --decorate --all
git ls-files | Measure-Object -Line          # 已跟踪文件数：应为 47
git -c core.quotepath=false status --porcelain -uall
git add --dry-run .                          # 地面真值：哪些文件"将被加入仓库"
```

#### 本次审计发现并修掉的 3 个问题

| # | 问题 | 处理 |
| --- | --- | --- |
| 1 | **`.dsh-tmp/` 有 445 个未跟踪文件**（DSH 运行环境在本仓库内落下的 pnpm/corepack 缓存，非项目内容）。一旦谁执行 `git add -A`，就会把一整个 pnpm 安装提交进去 | ✅ `.gitignore` 新增 `.dsh-tmp/` 与 `.dsh-*/`；实测未跟踪条目从 **553 → 109** |
| 2 | **7 个空目录会在 clone 后消失**（git 不跟踪空目录）：`Assets/Art`、`Assets/LuaScripts`、`_Project/Art`、`_Project/Configs`、`_Project/Game/{Controller,Service,View}` | ✅ 各放一个 `.gitkeep` 占位（Unity 会忽略以 `.` 开头的文件，**不会生成 `.meta`**，也不进 Console） |
| 3 | 根目录两个**需求来源文件**未提交：`用户技术需求大纲.md`、`20260107-F唐老师Unity框架相关笔记LYF.docx` | ✅ **已决定：两个都不提交**，并**写进 `.gitignore`**（避免长期以"未跟踪"形式挂在 `git status`、也防止 `git add -A` 误提交）。理由：后者是唐老师的课程资料，公开仓库等价于再分发。需求文档 §1 仍以**文字**引用它们 |

> ⚠️ **因为上述修复，需要补一次提交**（只改动了 3 个文件）：
> ```powershell
> cd "E:\U3D Projects\0_MyFile\3D联网战斗Demo"
> git add --dry-run .        # 复核：应只剩 Client/ 未跟踪 + 这 3 个已修改文件
> git add .gitignore Docs/01-项目需求文档.md Docs/10-M0手动操作手册.md
> git commit -m "chore(m0): 忽略 DSH 临时目录与需求来源文件；补 D16 审计记录"
> ```
>
> 7 个 `.gitkeep` 占位文件在 `Client/` 下，**跟着以后那次「提交 Client/」一起走**即可，不必现在单独提交。

#### 可选优化（不影响 M0 收关）

| 项 | 现状 | 建议 |
| --- | --- | --- |
| `core.quotepath` | 未设置（默认 `true`）→ 中文路径在 `git status` 里显示成 `\xxx` 转义，**很难读** | `git config core.quotepath false` |
| `core.autocrlf` | 本机为 **`true`** | 本项目已用 `.gitattributes` 统一换行符，建议 `git config core.autocrlf false`，让 `.gitattributes` 成为**唯一**判据，避免以后出现"整文件 diff" |

### D13 实测记录（2026-09-16 通过）

**环境**：.NET Runtime **8.0.21**（`dotnet --list-sdks` 显示 SDK 有 8.0.204 / 9.0.306，运行时取了 8.0.21）
**命令**：`cd Server` → `dotnet build` → `dotnet run --project NBC.Server.Host`
**结果**：`dotnet build` 0 错误；`dotnet run` 打印：

```
==============================================
  NBC Server (Networked Battle Combat)
  Version : 0.1.0.0
  Runtime : 8.0.21
  OS      : Microsoft Windows NT 10.0.2620x.0
  Machine : <本机名>
==============================================

[Check] NBC.Shared 可引用，共享层版本标识：NBC.Shared
```

**关键验证点**：`[Check] NBC.Shared 可引用` 出现了 —— 这证明 `netstandard2.1` 的共享层能被 `net8.0`
服务端引用并加载。**这是"双端共享同一份战斗逻辑"的第一半证据**（另一半等 Unity 侧 D17）。

#### ⚠️ 过程中修复的一个真实缺陷（值得记住的排错案例）

首次 `dotnet build` 失败：

```
error NETSDK1005: 资产文件 "Builds\Server\obj\project.assets.json" 没有 "netstandard2.1" 的目标。
```

**根因**：`Server\Directory.Build.props` 早期版本为了"统一输出目录"，覆盖了

```xml
<BaseIntermediateOutputPath>$(RepoRoot)..\Builds\Server\obj\</BaseIntermediateOutputPath>
```

该属性**在项目文件之前求值**（连 `$(MSBuildProjectName)` 都不可用），导致 5 个工程算出**同一个 `obj` 目录**，
它们的 `project.assets.json` 互相覆盖 —— 某个工程读到的是**别人的**资产文件，里面自然没有它需要的目标框架。

**判据（可复用）**：

| 属性 | 能否多工程共享 |
| --- | --- |
| `BaseOutputPath`（`bin`） | ✅ 可以（但要按工程名分子目录：`$(MSBuildProjectName)`） |
| **`BaseIntermediateOutputPath`（`obj`）** | ❌ **绝对不能**（`project.assets.json`、`*.AssemblyInfo.cs` 都是按工程的） |

**修复**：删除这两个路径覆盖，回归 .NET 默认（每工程自己的 `obj`/`bin`）。
**修复后**：5 个工程各有独立 `project.assets.json`，编译通过；`NBC.Shared.dll` 落在
`NBC.Shared\bin\Debug\netstandard2.1\`，其余工程落在 `...\bin\Debug\net8.0\`。

**教训已写入** `Server\Directory.Build.props` 的注释，避免以后重犯。

### 排期红线（需求文档 §18.1）
| 时间 | 红线 | 降级动作 |
| --- | --- | --- |
| 第 1 周末 | D17 未通过 | **不进入 M1**，先解决 AOT 问题。这是唯一不能跳过的关卡 |
| 第 1 周末 | D12 未通过 | 可先用 SQLite/内存库占位继续 M1，但需在 M3 前补齐 |
| **M1 开工前** | **N-1 未解决** | 无法引入任何第三方包。可先走"手动 `.nupkg` 取 DLL"路线（对客户端无影响，主要影响服务端） |

---

## 十二、需要你反馈给我的信息

按优先级排列（**第 1、2 项最急**，因为它们决定我下一步能不能动）：

| # | 反馈内容 | 对应 | 我要用它做什么 |
| --- | --- | --- | --- |
| **1** | **D13 的 `dotnet build` 与 `dotnet run` 结果**（成功输出或完整报错） | §6 / D13 | 确认服务端骨架可用；有编译错误我立刻修 |
| **2** | ~~**D14 的完整编译报错信息**~~ ✅ **已由 AI 代跑完成（2026-09-20）** | §7 / D14 | 已写入需求文档 **§5.4.6**（含逐字输出与判读），§5.4.5「事实 1」已升级为实测 |
| 3 | **D17 的打包与运行结果**（屏幕截图或 `Player.log`） | §9 / D17 | 判定 M0 最终关卡是否通过；通过则收关 M0 |
| 4 | **各依赖的精确版本号**（Unity / URP / YooAsset / Cinemachine / Behavior Designer / A\* Pathfinding Project / DOTween / xLua / **Google.Protobuf**） | §4 / D11 | 回填 `Docs\05-依赖清单.md`，完成**版本冻结** |
| 5 | **N1 联机范围**：同机多开 / 局域网 / 都要（见 §10） | §10 | M3 按它定服务端监听与客户端连接策略 |
| 6 | 是否还有**其他已购插件/资源包**（Q14 待定项） | — | 把插件已提供的能力从 P0 任务里划掉，省时间 |

> 收齐 1、3、4 后我会：回填依赖清单 → 输出 `Docs\09-IL2CPP验证清单.md` 的实测版 → 按规则 1 写当日知识点总结 → 判定 M0 是否收关。
> （**第 2 项（D14）已完成**，不再需要你提供。）
>
> **卡住时不要自己试太久**：把现象（截图/日志/报错原文）贴给我，我按排查表定位。手册每步都写了"如果失败看什么"。

---

## 十三、M0 收关判定标准

M0 算完成，必须**同时**满足：

| 判定项 | 证据 | 谁给 |
| --- | --- | --- |
| ① 工程可打开可编译 | Unity Console 无红色报错 | 你 |
| ② 依赖冻结 | ✅ **已达成**：C1~C14（含 C3 xLua `v2.1.15` / C3a 原生库）全部回填实测版本，见 `Docs\05-依赖清单.md` §五 | 你提供 → 🤖 已回填 |
| ③ 数据库可用 | ✅ **已达成**：`nbc_db` 下恰好 4 个 `.ibd`（表名与脚本一致）；测试账号由脚本末尾的验证查询打印 | 你执行 + 🤖 只读核验 |
| ④ 服务端可编译可运行 | `dotnet build` 成功 + `[Check] NBC.Shared 可引用` 出现 | 你 |
| ⑤ **插件架构边界已验证** | ✅ **已达成**：D14 探针 9 级 3 失败 + `ECall` 报错原文（需求文档 §5.4.6） | ~~你~~ → 🤖 已代跑 |
| ⑥ **IL2CPP + AOT 已验证** | ✅ **已达成（2026-09-20 第 3 次出包）**：protobuf（Google.Protobuf，49 字节，含集合字段往返正确）+ xLua（1..10=55）**双双通过** | 你执行 + 🤖 记录 |
| ⑦ Git 基线 | ✅ **已达成**：`git log` 有首次提交 `f83771d`（47 文件）；已跟踪清单中无 `Library/`/`bin/`/`obj/`（D16 实测记录） | ~~你~~ → 你执行 + 🤖 只读审计 |

> **⑥ 是唯一不能跳过的一项**（需求文档 §18.1 排期红线）。它没过就不进 M1 —— 因为 AOT 问题会污染后面所有里程碑。

---

## 附：本手册引用

- YooAsset 安装（OpenUPM 作用域注册表）：[YooAsset 快速开始](https://yoo-asset-doc.vercel.app/docs/2.3.x/guide-editor/QuickStart)
- YooAsset 接入实战流程：[实战接入 YooAsset](https://www.cnblogs.com/jzssuanfa/p/20047747)
- **（已换库，仅作历史）** protobuf-net 在 Unity 下的预编译必要性：[Protobuf-net 预编译：解决 IL2CPP 序列化崩溃](https://blog.csdn.net/weixin_31479991/article/details/161280276)
  —— ⚠️ 本文讲的是被我们放弃的 **protobuf-net** 路线；**现用 Google.Protobuf**，其 `protoc` 生成命令见本手册 **D7** 与 `Docs\11-环境配置说明.md`
- Unity asmdef 文件格式（`references` 可按程序集名或 GUID，二者不可混用）：[Assembly Definition File Format — Unity 2022.3](https://docs.unity.cn/Components/AssemblyDefinitionFileFormat.html)
- Behavior Designer 版本差异与网络方案：需求文档 §5.4.5 已引用的 Opsive 官方文档
