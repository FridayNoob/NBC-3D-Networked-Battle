# 本项目工作规则（裁剪版）

| 项 | 内容 |
| --- | --- |
| 文档版本 | v1.9.7（2026-09-23 **新增 W12 场景物体命名规范** `类型_物体名`，**类型前缀首字母大写**（例 `Button_开始`）；M2 收关，已讲 → 21 篇）<br>历史：**W3a/W11**（09-22：`Get-Content` 行数不可信；生成代码注释密度）→ v1.9（16 篇）· **v1.9.1** 阶段总复习（→ 17）· **v1.9.2** M2-A（→ 18）· **v1.9.3** M2-B（→ 19）· **v1.9.4** M2-C（→ 20）· **v1.9.5** M2-D（→ 21） |
| 生效日期 | **2026-09-16**（已核对系统时间） |
| 来源 | `C:\Users\李云峰\.pi\agent\skills\project-rules\SKILL.md`（pi 版项目规则，触发范围为 RPG_Git / RPG1） |
| 为什么有这个文件 | 上述规则文件的触发范围是 **RPG_Git 项目**，本项目（3D联网战斗Demo）不在其内，**自动化不会加载它**。为保证本项目也遵循同一套工作方式，特此把规则裁剪成本项目适用版本，并把"有意不适用的条款"与"经批准的例外"写明，避免漏规则或误用规则 |
| 强制性 | 本文档对本项目**全程有效**；新增条款须经项目负责人确认后写入 |

> **重要**：本文档**不是**在修改你的全局 skill，而是本项目内的落地副本。全局 skill 位于 `~/.pi/` 下，AI 不改动它。

---

## 一、逐条适用性对照

| 原规则 | 内容摘要 | 本项目 | 说明 |
| --- | --- | --- | --- |
| 1 | 每次对话后总结知识点到 `DeepSeekOutput\YYYY-MM-DD-知识点总结.md`（先核对系统日期；同日追加） | ✅ **适用（路径已改）** | 落盘目录改为 `E:\U3D Projects\0_MyFile\3D联网战斗Demo\DeepSeekOutput\`（项目负责人 2026-09-16 确认） |
| 2 | 修改文件/代码前必须先问（要改哪些文件、每处怎么改），读取文件直接做 | ✅ **适用** | — |
| 3 | 回答问题时提供最多 3 个方案 + 优缺点 + 适用场景 | ✅ **适用** | — |
| 4 | 非代码操作（Unity Editor 配置、命令行、第三方工具安装）必须给详细操作步骤（菜单路径/按钮名/输入内容） | ✅ **适用** | — |
| 5 | 代码直接完整输出代码块（标注文件路径）；已改文件仍要给改动摘要 | ✅ **适用** | — |
| 6 | 默认中文回答 | ✅ **适用** | — |
| 7 | `git push` 前必须展示推送内容 + 目标，得到明确同意才执行；缺信息就直接问，不臆测 | ✅ **适用** | 本项目目前**尚未初始化 Git** |
| 8 | 允许 AI 直接改项目文件，但必须先说明改动并确认；**中文注释不得乱码**（按原编码 + 原 BOM 状态回写，GBK 文件禁写 UTF-8 字节） | ✅ **适用** | 本项目当前只有 UTF-8 新文件，尚未触及 GBK/带 BOM 文件；一旦触及按本条执行 |
| 9 | 先充分沟通、不推测用户意图；有歧义直接提问 | ✅ **适用** | — |
| 10 | **新会话首次实质回复必须声明参考了哪些资料、逐个列出加载了哪些 Skill**；未参考也要明说 | ✅ **适用** | 本项目此前遗漏多次，已重点整改 |
| 11 | **新需求先中文复述确认**（含理解、范围边界、默认方案），确认后再进入实施 | ✅ **适用** | — |
| 12 | **不重复造轮子，优先现成方案**；禁止自写同类基础能力（资源加载框架、热更、网络、对象池、寻路、加密、UI 框架） | ✅ **适用，方案 B** | 见下方「二、规则 12 的落地：5 项书面例外」 |
| 13 | **工具边界：AI 只读写文件；Unity 编辑器点击 / 测试 / 打包由用户手动**（标注"最高优先级"） | ✅ **适用（最高优先级）** | 见下方「三、规则 13 在本项目的具体边界」 |
| 14 | **边做边教**：新概念三段式「是什么 → 为什么 → 不这么做会怎样」；每轮 1~3 条；知识点总结固定加「🎓 本次新概念」小节 | ✅ **适用** | — |
| **15** | **本项目的目的包括培养用户的操作能力、知识与技能**；**尽量避免使用一键脚本**（即使它能提高效率） | ✅ **适用（优先级高于效率）** | 见下方「二之二、规则 15 的落地」 |

> **规则 15 是项目负责人于 2026-09-16 新增的全局规则**，它把"提升用户开发能力"正式列为项目目标之一。
> 由于它与"提高效率"直接冲突，下面专门用一节明确冲突时的裁决，以及"什么算一键脚本"的判据。

**原规则的「环境速查」段不适用本项目**（那是 RPG_Git 的路径），本项目环境速查见本文档第五节。

---

## 二、规则 12 的落地：5 项书面例外

规则 12 的原文要求"**禁止自行新写同类基础能力**"，但本项目同时受技术需求大纲约束
（需求 11 点名用指定的自研框架底座、需求 5 明确热更新"这部分不用框架"），两者存在张力。
**2026-09-16 经项目负责人确认，采用"方案 B"**：

> **原则：兜底基础能力一律优先用现成方案；仅下列 5 项例外自研，且每项必须有书面理由。**

| # | 例外项 | 理由 | 规则 12 对应点名 |
| --- | --- | --- | --- |
| **E1** | **唐老师框架底座**（单例/事件中心/对象池/MonoMgr/音效/UI 框架） | 需求 11 点名指定；弃用它改用现成库将直接违背需求 11 | 对象池、UI 框架 |
| **E2** | **网络传输层**（长度前缀/粘包/心跳/重连 + 三种同步策略） | 需求 11 已排除 Mirror 等完整网络框架；需求 2 要三种同步模式 + 预测回滚，无现成方案可套 | 网络 |
| **E3** | **Lua + AB 的业务侧热更管理**（更新流程/CUI/强制更新/原子性切换/回滚） | 需求 5 明确"这部分不用框架"。注意：**AB 构建与下载底层已改用 YooAsset** | 热更 |
| **E4** | **定点数学 `Fix64`/`FixVector3`/`FixMath`** | 无现成可用方案；浮点在帧同步下必然不确定 | （未点名） |
| **E5** | **确定性 A\* / 流场寻路** | 已购 A\* Pathfinding Project Pro 是浮点 + 异步多线程，不满足帧同步确定性；Unity NavMesh 同理 —— 属"现成方案确实不满足需求" | 寻路 |

**其余一律使用现成方案**（符合规则 12）：

| 能力 | 采用方案 |
| --- | --- |
| 资源加载 / AB 构建 / 下载 / 版本清单 | **YooAsset** |
| Lua 绑定 / 热更加载器 | **xLua** 及配套封装 |
| 行为树（客户端） | **Behavior Designer 经典版** |
| 客户端寻路 | **A\* Pathfinding Project Pro** |
| 补间 / 相机 | **DOTween** / **Cinemachine** |
| 序列化 | **Google.Protobuf**（`protoc` 生成代码；2026-09-20 由 protobuf-net 换库，原因见需求文档 R4） |
| 数据访问 | **Dapper** + **MySqlConnector** |
| Excel 读写 | **EPPlus / NPOI** |

### 新增例外的流程（不得口头例外）

1. 先评估现成方案为什么不满足（要有具体技术理由，不能是"我想自己写"）
2. 在需求文档 §5.4.0 例外表追加一行（含理由 + 对应点名项）
3. 在需求文档 §17 记录该决策
4. 更新 `Docs/05-依赖清单.md`

### 代码书写附加条款（2026-09-16 新增，源于一次真实缺陷）

| # | 条款 | 原因 |
| --- | --- | --- |
| **W1** | **`.csproj` / `.props` / `.xml` 等 XML 文件的注释内禁止出现连续两个连字符 `--`** | XML 规范硬性规定。用 `------` 画分隔线会导致 **工程文件直接无法解析**（`MSB4025` / `not well-formed`）。分隔线请改用 `=======` 或 `~~~~~~~` |
| **W2** | 生成文件后**必须做一次只读合法性校验**再交付 | 本项目 M0 就靠这一步抓出了上面那个缺陷。校验方式：`[xml]` 解析、`json.load`、括号配平、编码/BOM 检查 |
| **W3** | **不要用 PowerShell 的 `Get-Content -Raw` 校验 UTF-8 中文文件** | 本机 PowerShell 默认按 GBK 解码，会把正常 UTF-8 文件读成乱码，造成"JSON 校验失败"之类的**误报**。校验中文文件请用 Python 显式 `encoding="utf-8"`，或 PowerShell 显式 `-Encoding UTF8` |
| **W3a** | ⚠️ **同一根因的第二个症状：`Get-Content` 连"行数"都是错的，`Select-String` 报的"行号"也不可信**（2026-09-22 实测）。<br>**实测数据**：`Docs\16-M1开工清单.md` 真实 **1488** 行（`[System.IO.File]::ReadAllLines` 与 ripgrep **一致**），而 `Get-Content` 报 **1094** 行 —— **静默吞掉 394 个换行**（内容同时还是乱码）。`Select-String` 同源，行号随之偏移。<br>**后果（W9 的又一次现形）**：我拿 `Select-String` 给的行号去**切一段文本**做"Markdown 表格完整性检查"，切到的是**完全不相干的段落**（`## 十一` 而不是 `§10.2.19`），于是那次校验**一个表格都没检查**，却报"0 问题" —— **假绿**。<br>**本环境下的正确读法**：`[System.IO.File]::ReadAllText/ReadAllLines($path, [System.Text.Encoding]::UTF8)`，或直接用编辑器的 `read` / `grep` 工具。**凡是要行号、要计数、要按行切段落，一律不用 `Get-Content` / `Select-String`**；后者只允许用于"看一眼大概内容"，且必须显式 `-Encoding UTF8` | 这是**静默**错误：不像 W3 那样一眼就是乱码，而是"看着像正常文本、数字却是错的"。**用错的行号去切片，校验会转向一个不相干的对象，而结果永远是"通过"** —— 比不校验更危险 |
| **W4** | 写文件一律 **UTF-8 无 BOM + LF** | 与规则 8 的编码安全一致；已有 GBK/带 BOM 文件按原编码回写 |
| **W5** | **`.gitignore` 写法的三条要点**（2026-09-20 用真实 git 仓库实测校正）：<br>① **`#` 只在行首是注释 —— `.gitignore` 不支持行尾注释**。写 `!**/Packages/   # 说明` 会让**整行**（含说明文字）变成一个永远匹配不到的取反模式，**静默失效、不报错**<br>② **需要忽略目录时，写「目录名 + 斜杠」**，例如 `Client/Library/`、`bin/`、`obj/`<br>③ **不要靠取反去救回被误伤的同名目录，正解是收窄作用域**：Windows 上 `core.ignoreCase = true`，`**/packages/` 会连 Unity 的 `Packages/` 一起忽略；把该规则放进 `Server\.gitignore`（该目录下没有 Unity 工程）即可，不需要 `!` | ① 是**静默失效**，最危险 —— 直到某天发现 Unity 的 `Packages/` 被忽略、工程拉下来跑不起来。③ 是"用作用域代替取反"，语义更清晰、不易出错 |
| **W5a** | ⚠️ **更正**：本条原写"`[Ll]ibrary/` 只匹配目录本身、目录内文件会被误提交"、"`**/bin/` 不如裸 `bin/` 可靠"—— **这两条结论都是错的**，2026-09-20 已用 `git check-ignore -v` 实测推翻。真实语义是：**已被忽略的目录，git 不会向下遍历**，所以目录内文件自动一并忽略；`[Ll]ibrary/` 与 `Library/`、`**/bin/` 与 `bin/` **都有效**。本项目仍统一用精确大小写的写法（`Client/Library/`、裸 `bin/`），理由是**可读性**，不是因为另一种写法有缺陷 | 错误结论会让人做无谓的"改写规则"工作，并误导后续排查。**规格说明要经得起实测检验** |
| **W6** | 改完 `.gitignore` 必须用**地面真值**验证，判据是「**这些文件最终会不会进仓库**」，而不是某条规则"看起来有没有写对"：<br>① 在临时目录 `git init` 一个最小仓库，摆出真实的目录骨架；<br>② **地面真值命令**：`git add --dry-run .`（列出"将被加入仓库"的文件）或 `git status --porcelain -uall`；<br>③ 逐个核对：该保留的**必须出现**，该忽略的**必须不出现**。<br>⚠️ **不要只依赖 `git check-ignore -v`**：它会**把"被取反规则命中"的路径也打印出来且退出码为 0**，容易被误读成"已忽略" | 2026-09-20 一天之内**两次**验证翻车：先是用"自己写的模拟脚本"得出两条与真实 git 相反的结论（见 W5a）；改用 `git check-ignore -v` 后，又把 `.gitignore` 末尾 `!Server/**/*.csproj` 这类**取反保命规则**误判成"被忽略"。换成 `git add --dry-run` 后，22 项保留 + 17 项忽略全部符合预期。**判据选错，比不验证更危险** |
| **W7** | **`.ps1` 脚本的编码分两种情况**（见下）：**① 正文全 ASCII** → 无 BOM；**② 必须含非 ASCII**（如中文文件名/路径）→ **必须 UTF-8 with BOM** | 本机是 **Windows PowerShell 5.1**，它**按 ANSI(GBK) 读取无 BOM 的脚本源码**；含中文的无 BOM `.ps1` 会因字符串字面量被破坏而报一堆 `Unexpected token`，而文件本身是合法 UTF-8（普通编辑器看完全正常），**排查成本极高**。详见 `Tools/README.md` |
| **W7a** | **`.csproj` / `.props` / `.xml` 等由 MSBuild 读取的文件不受此限** | MSBuild 按 UTF-8 读，无 BOM 即可 |
| **W7b** | ⚠️ **本项目所有 `.ps1` 都不能含非 ASCII —— 除非该路径是运行所必需的** | 2026-09-16 实测：`Check-DocLinks.ps1` 需要真实中文路径 `Docs\01-项目需求文档.md`，此时**必须带 BOM**。写完后用下面命令自检 |
| **W7c** | 用编辑工具改过带 BOM 的 `.ps1` 后，**必须补回 BOM** | 编辑工具按无 BOM 回写，会让脚本重新变成"坏"的（本次踩过两次） |
| **W8** | 若某个脚本/文件**必须**含非 ASCII（如中文注释的 `.cs`），需确认其**消费方**用什么编码读取 | 例如 `.cs` 由 Roslyn/Unity 按 UTF-8 读，所以无 BOM 也没问题；但 `.ps1` 由 PowerShell 引擎按 ANSI 读，就必须带 BOM。**判据是"谁来读"，不是"我写了什么"** |
| **W9** | **自研校验器/模拟器必须先被校验**：① 能用官方工具时**优先用官方工具**（真实的 git / `dotnet build` / 编译器）；② 确实必须自己写时，先用**一个已知答案的用例**校准它，通过了再去判定别的文件；③ 校验器给出"异常结论"时，**先怀疑校验器**，别急着改被检查的文件；④ **选对判据**比"有没有验证"更重要 —— 要验证的应该是**最终效果**（文件会不会进仓库），而不是中间产物"看起来对不对" | 2026-09-20 真实事故：一个自己写的路径模拟脚本给出两条与真实 git 相反的结论，被当成事实写进了 `.gitignore` 注释；随后 `git check-ignore -v` 又把取反保命规则误判为"已忽略"。**"验证工具"本身是代码，也会有 bug；把它的输出当真理是最容易犯的错** |
| **W11** | **生成代码的注释密度**（2026-09-22 由项目负责人定，**同日改过一次，下面是最终版**）：生成出来的代码文件里 —— ① **文件开头**必定有注释；② **每个成员变量（字段）在声明处**必定有行尾注释；③ **类名 / 方法名前**必定有注释；④ **其他变量（局部变量 / for 计数器）看实际情况加**，**其余地方非必要不加注释** | **中途那版**要求"每个变量名都要加注释"，实测下来局部变量会写出一堆**照抄代码本身**的废话（`string[] lines = ... // 按行拆开`、`for (int i...) // 逐项解析`），信息量为零却让 diff 变脏。收紧后的判据是：**注释要加在"读的人会卡住"的地方，而不是"有变量的地方"**。**这条由自测机械检查**（`CodeGen_CommentDensityRule`，只强制成员变量） |
| **W12** | **场景物体命名规范**（**2026-09-23 由项目负责人定**，同日补充"首字母大写"）：`类型_物体名` —— **类型前缀首字母大写**，例如**开始按钮命名为 `Button_开始`**；物体名部分中英文皆可（英文也用大写开头） | ① **在 Hierarchy 里一眼看出"这东西是什么类型"**（`Title` / `Text (1)` / `Button (2)` 这类名字看不出类型，也看不出用途）；② 我们的 UI 框架是**按物体名索引控件**的（A9 的 `BasePanel`），于是"控件名"就是"物体名"——**命名规范直接决定代码里写什么字符串**；③ 前缀大写也与 Unity 自带物体的习惯一致（`Main Camera` / `EventSystem` 都是大写开头）。<br>**约定用到的类型前缀**（首字母大写）：`Button_` / `Text_` / `Image_` / `Panel_` / `Layout_` / `Slider_` / `Toggle_` / `Scroll_` / `Root_`（不够再加，但**同类只用同一个前缀**）。<br>⚠️ **不强制一次性重命名已有物体**：新搭的按规范来；老物体改动到时顺手改。<br>⚠️ 代码侧配套：**控件名应当可配**（别把物体名硬编码在代码里）—— 项目里的样例是 `QuestPanel` 的 5 个控件名（`[SerializeField]` 字段，默认值即规范名：`Text_Title` / `Text_Message` / `Layout_OfferList` / `Layout_TrackingList` / `Button_RowTemplate`）。 |

### PowerShell 脚本编码自检（W7 系列，建议每次改完脚本就跑）

```powershell
# 对每个 .ps1 检查：非 ASCII 字节数 与 BOM 状态
Get-ChildItem Tools\*.ps1 | ForEach-Object {
    $b = [System.IO.File]::ReadAllBytes($_.FullName)
    $na = ($b | Where-Object { $_ -gt 127 }).Count
    $bom = ($b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF)
    $verdict = if ($na -eq 0 -and -not $bom) { 'OK  (pure ASCII, no BOM)' }
               elseif ($na -gt 0 -and $bom)  { 'OK  (has non-ASCII, BOM present)' }
               else                          { 'BAD (would be mis-decoded by PowerShell)' }
    "{0,-24} 非ASCII={1,-4} BOM={2,-6} {3}" -f $_.Name, $na, $bom, $verdict
}

# 再检查语法（0 个错误才算通过）
Get-ChildItem Tools\*.ps1 | ForEach-Object {
    $e = $null
    $null = [System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$null, [ref]$e)
    "{0,-24} 语法错误数 = {1}" -f $_.Name, $(if ($e -and $e.Count) { $e.Count } else { 0 })
}
```

**判据（两条必须同时成立）**：

| 非 ASCII 字节数 | BOM | 结论 |
| --- | --- | --- |
| 0 | 无 | ✅ 正确（回归无 BOM 的统一约定） |
| \> 0 | **有** | ✅ 正确（PS 5.1 能正确解码） |
| 0 | 有 | ➖ 可接受（但不统一） |
| **\> 0** | **无** | ❌ **脚本会崩**，必须补 BOM |

补 BOM 的一行命令：

```powershell
$p = "Tools\Check-DocLinks.ps1"
$t = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($p))
[System.IO.File]::WriteAllText($p, $t, (New-Object System.Text.UTF8Encoding($true)))
```

### 代码改动边界（**W10**，2026-09-21 由项目负责人明确）

> 原文：**"后续要尽量避免修改框架代码或者插件源码"**。

| 对象 | 要求 | 理由 |
| --- | --- | --- |
| **第三方插件 / 包源码**（YooAsset、xLua、Google.Protobuf…） | 🚫 **一律不改** | 改了就没法升级；别人 clone 下来行为和包不一致；出问题时分不清是自己写错还是包被改过 |
| **已收关框架模块的既有代码**（测试全绿 + 已留档） | ⚠️ **尽量避免改**。优先"**新增**"（新文件 / 新模块 / 新程序集），而不是"**改动**" | 每改一次都要重跑全部测试；已验收的东西被动过，就得**重新证明一遍** |
| **框架的公共接口**（ADR 里冻结过的） | ⚠️ 改之前**必须先问** | 见 §五「已冻结的关键技术决策」 |

**「新增」不算「修改」**：加新文件、新模块、新程序集都属于新增，**不受本条限制**。

**确实非改不可时，四件事一件都不能少**：

1. 先说清**为什么非改不可**（不能是"这样写更顺手"）
2. 说明**影响面**（哪些已验收的模块会被牵动）
3. **补测试或改测试**，并说明改的是哪一条、为什么改
4. 在 `Docs\06-框架改造记录.md` 的变更记录里**记一笔**

⚠️ **不许为了让自己新写的代码通过，去改已有的守卫 / 测试。**

> **已有一次正确做法的实例**（2026-09-21）：A9 需要 UGUI，而 UGUI 必须显式引用 ——
> 这和"`NBC.Framework` 的 `references` 必须为空"那条守卫冲突。
> 处理顺序是：**先查官方手册确认事实 → 把冲突报给负责人 → 拿到决定后才动结构**。
> 结果是 **UI 单独成 `NBC.Framework.UI` 程序集，守卫一个字没改**（`Docs\02` 决策 7）。

### 依赖登记要求（规则 12 附带）
新增任何第三方插件/开源库，必须在 `Docs/05-依赖清单.md` 登记：
**名称 / 版本 / 来源（商店或仓库地址）/ 开源许可 / 用途 / 是否属于例外清单**。

引用第三方代码须注明来源与版本；**自研代码只做"业务胶水"（封装/接线/适配），不做"重造基础能力"**。

### 采用新插件前的快速评估清单

是否活跃维护 · 社区规模 · 开源许可 · 与 Unity 2022.3.62f3c1 兼容性 · 是否有中文文档

---

## 二之二、规则 15 的落地：以培养用户能力为目标（2026-09-16 新增）

### 规则原文

> 本项目的目的也包括教会用户相关操作和知识以及技能，提升用户开发能力，**尽量避免使用一键脚本，即使它能够提高工作效率**。

### 15.1 目标定位（写在最前面，因为它裁决一切冲突）

| 目标 | 相对优先级 |
| --- | --- |
| **提升用户的开发能力（操作 / 知识 / 技能）** | **高** |
| 项目交付速度与自动化程度 | 低（为前者让路） |

**冲突裁决**：当"某个做法能提高效率"与"该做法会让用户少学到东西"冲突时，**选择前者（教学优先）**。
这与规则 14（边做边教）方向一致，但更强硬：规则 14 管"讲不讲原理"，规则 15 直接管"**要不要替用户做**"。

### 15.2 「一键脚本」的判据（什么禁止、什么允许）

判据不是"是不是脚本"，也不是"用没用 PowerShell"，而是：

> **这个工具是在"替用户完成流程"，还是在"帮用户观察与判断"？**
> 把**多个步骤打包成一次执行**、使中间动作与中间结果不再出现在用户眼前 → **属于禁止的一键脚本**。
> 只是**输出信息供用户判断**、不代替用户做任何修改动作 → **不属于**，可以保留。

| 类别 | 例子 | 判定 |
| --- | --- | --- |
| ❌ **一键式流程自动化** | 建目录 + 搬文件 + 初始化 Git + 建库"一次跑完" | **禁止**。用户看不到中间动作，也不知道每步在做什么 |
| ❌ 把多步配置合成一个"Setup"脚本 | 装包 + 改设置 + 生成配置一把梭 | **禁止** |
| ⚠️ 便捷别名/包装（把一条命令包成短命令） | 如 `mk` 代替 git 三步 | **不鼓励**。要写就得讲清它展开了什么，且优先直接给原命令 |
| ✅ **只读诊断工具** | 分层探测网络（DNS/TCP/TLS/代理）并打印结果 | **允许**。用户必须自己读结果、自己判断、自己动手修 |
| ✅ **只读验证工具** | 读 DLL 元数据核对程序集名、比对配置条目 | **允许**。它只是把"结论"呈现给用户，不改任何东西 |
| ✅ **生成对照物** | 打印一份应放的 DLL 清单供手工放置 | **允许**。放置动作仍由用户完成 |

> **一句话**：**观察与判断可以自动化，动手与理解不可以。**

### 15.3 今后每一步对话的教学要求

每涉及一个需要用户动手的步骤，必须同时给出：

| # | 要给的东西 | 说明 |
| --- | --- | --- |
| 1 | **逐步操作** | 精确到菜单路径 / 按钮名 / 命令原文（规则 4 已要求） |
| 2 | **每步在干什么** | 一句白话，说明这一步改变了什么 |
| 3 | **验证方式** | 怎么知道成功了（看什么信号、什么数值） |
| 4 | **失败时先看什么** | 常见现象 → 可能原因 → 判据（不只给结论） |
| 5 | **为什么这样做** | 设计权衡与替代方案，不堆术语 |
| 6 | **让用户决策** | 列**选项 + 判据 + 后果**，由用户拍板，并记录其选择与理由 |

**禁止的做法**：说"我已经帮你搞定了"就结束；或在用户没看懂的情况下继续往下推进。

### 15.3.1 ⚠️ **下轮开工前必须先讲上一轮的模块**（2026-09-20 由项目负责人明确）

> 原文：**"要记住在进行下轮工作前，你得教会我"**。

**硬性要求**：每完成一个模块（A1、A2、B2、A5 …），在**开始下一个模块之前**，
必须先用大白话把**刚完成的那个模块**完整讲一遍，再动下一轮的代码。

| 项 | 约定 |
| --- | --- |
| **时机** | **模块收关（测试全绿）当场把课写进 `Docs\教学\`**，**并在同一条回复里继续下一个模块的工作** —— **教学不再阻塞进度**（2026-09-21 由负责人调整） |
| **粒度** | **一次讲完整一个模块，不打断**（负责人 2026-09-20 选定） |
| **对话里给什么** | **一段短导读（5~10 行）**：这东西干嘛的 / 不修会怎样 / 改变了什么，外加文档链接。<br>⚠️ **完整内容在文档里**，负责人自己打开看；**不要在对话里重复整篇**（2026-09-21 由负责人调整） |
| **结构** | ① 这东西解决什么问题（不用代码）② 一个最小例子 ③ 原版错在哪（配原代码） ④ 改造后的做法 ⑤ **自测 5 问** ⑥ 你 30 秒能做的验证 ⑦ 面试一分钟版本 |
| **留档**（**2026-09-20 由项目负责人新增**） | 讲过的内容**必须同时落成独立 md**，放在 `Docs\教学\`，**一个模块一篇**（命名 `A7-输入管理.md`），供负责人随时复习。⚠️ **留档是"讲"的补充，不是替代**：先讲、后留档；只写文档不讲，仍然违反本节 |
| **留档写法** | 沿用七段式的七个小标题，**正文照抄对话原话**（保留"你 / 我"的口吻），**不做二次精炼** —— 精炼过的版本又会变成"看不懂的文档"。开头一行写清"对应代码 / 对应测试 / 对应文档"三跳转 |
| **禁止** | 用文档的密度写对话（500 行代码 + 一整套 ADR + 四五个新术语一起甩）；**把教学写进 `Docs\` 当附录就当讲过了**（**新增留档要求后，这一条依然有效**） |
| **已讲** | **M1**：A1 ✅ A2 ✅ A3 ✅ A4 ✅ A5 ✅ A7 ✅ A8 ✅ A9 ✅ A10 ✅ B2/B3 ✅ C2/C3 ✅ C4 ✅ D1 ✅ D2 ✅ E1 ✅ E2 ✅ + M1 总复习 ✅<br>**M2**：**A 条件系统与任务系统 ✅**（`M2-A-条件与任务.md`）、**B 单机 PVE 战斗内核 ✅**（`M2-B-战斗内核.md`）、**C 装配与端到端 ✅**（`M2-C-装配与端到端.md`，端到端**已在 Unity 里真跑通**）、**D 任务 UI ✅**（`M2-D-任务UI.md`）<br>—— 全部有独立留档（`Docs\教学\`，共 **21 篇** = 16 M1 单模块 + 1 篇 M1 总复习 + M2-A/B/C/D）。<br>2026-09-21 负责人要求"把 `Docs\教学\` 中没有的教程也补上"，已补写 **A1 / A2 / A3 / A4 / A5 / B2-B3 / D2** 七篇。<br>2026-09-22 M1 收关，补写 `M1-总复习.md`（模块地图 + 16 篇索引 + 8 条母题 + 实测数字 + 面试话术 + 犯错清单）。<br>2026-09-22 M2-A 收关（EditMode 489 全绿）→ `M2-A-条件与任务.md`；M2-B（530 全绿）→ `M2-B-战斗内核.md`；2026-09-23 M2-C（545 全绿 + 端到端在 Unity 跑通）→ `M2-C-装配与端到端.md`；M2-D（567 全绿）→ `M2-D-任务UI.md`。<br>⚠️ **E1 与 C 组的留档写在"测试还没跑完"的阶段**：设计结论确定，凡"实测"字样都注明了出处；跑完若与文中不符会回填更正（E1、C2/C3、C4 都已回填）。<br>待讲：**A11**（分层断言已在 A2/A9 的课里随讲）、**B4/B5**（YooAsset 打包，属负责人操作编辑器）。 |
| **待补** | 无（2026-09-21 已全部补齐；2026-09-22 补 E1）。只剩"还没做到"的模块：A11 / C 组 / F 组 |
| **阶段总复习**（**2026-09-22 新增**） | 一个**里程碑收关时**，除单模块留档外再补一篇**总复习**：`Docs\教学\M1-总复习.md`。内容 = 模块地图 / 单篇索引 / **跨模块的方法论母题** / 实测数字 / 面试话术 / **本项目犯错清单**。<br>⚠️ 它的定位是**复习入口**，不替代任何单篇（单篇有完整代码与排错复盘）。已讲清单随之变为 **17 篇**（16 单模块 + 1 总复习）。 |

#### 15.3.1.1 ⚠️「说人话」硬约束（**2026-09-21 由项目负责人第二次指出**）

> 原文：**"教我的时候尽量说人话，太多专业术语混在一起我看不懂了"**。
>
> ⚠️ **这是同一个问题的第二次**。第一次是 2026-09-20 的"我看不懂了"，
> 当时立的 §15.3.1 只写了"不许甩术语"，**没给可执行的判据** ——
> 于是我又用"术语堆叠 + 破折号套破折号 + 跨模块类比"讲了一遍 A8，负责人**再次没看懂**。
>
> **教训：光说"要讲清楚"没有用。必须给能当场自查、能当场判违规的硬规则。**

| # | 硬规则 | 我自己犯过的反例 |
| --- | --- | --- |
| 1 | **先讲具体场景，再讲道理。** 每节开头必须是"玩家做了什么 / 屏幕上发生什么" | A8 第①节我上来就讲"音效是高频、同构、可复用的东西" —— 这是**结论**，不是**场景** |
| 2 | **专业词可以随便用，但每个词第一次出现必须当场解释。** 解释要落在"它解决什么问题"或"打个比方"，不是背定义 | ⚠️ 我第一版**走偏了**：把"术语太多"理解成"禁用术语"，定了"一次最多 2 个新词"，结果讲得干巴巴、反而更难懂。**负责人 2026-09-21 纠正："说明的时候可以有专业词，但是要对专业词进行解释"** |
| 3 | **代码示例尽量给、越详细越好。** 宁可多贴几段真实代码，也不要一句抽象概括带过。每段代码旁边配一句话说它在干嘛 | ⚠️ 同上，我第一版给单个代码块定了"不超过 10 行"的上限 —— **纯属自缚手脚**。**负责人 2026-09-21 纠正："代码可以放，而且示例越详细越好"** |
| 4 | **一句话别塞三件事。** 破折号 / 括号里再套一个从句，读的人就断了 | "音效是高频、同构、可复用的东西 —— 恰好是对象池最典型的场景" |
| 5 | **跨模块类比可以用，但不能默认对方记得那个模块。** 要先一句话把被类比的东西说清 | A8 里我插了"顺手对照 A7：`InputActionId` 的 0 是合法索引…" —— 对方当时根本没在想 A7 |
| 6 | **多说"看得见的东西"**：屏幕上的数字、Console 里的字、能点的按钮、玩家的体感 | 我讲了半天"ABA 问题"，却没说"玩家会听到两首歌同时停" |
| 7 | **语言自然点**，像平常说话。别写成电报体，也别写成说明书。**"你""我""咱们"都能用** | ⚠️ 我第一版把句子砍得太短，读起来一句一顿像在下命令。**负责人 2026-09-21 纠正："语言不要太生硬"** |

**篇幅**：**不设硬上限**。考核指标不是"讲了多少行"，而是"讲完对方能不能复述"。
（我第一版定过"80 行以内"，结果为了压行数把话说得像电报 —— **已被负责人纠正**。）
但**每一段都要有用**：不要为了显得全面而堆砌，**也不要把该展开的细节压掉**。

**讲完的自查（必做，不许跳）**：让负责人回答两个问题 ——
**① 这东西是干嘛的？ ② 不修会怎样？** 答不上来 → **重讲，不许进下一个模块**。
（这比"你听懂了吗"有效：那个问题对方出于礼貌会说懂了。）

> **违反本节的判定方式**：把讲课里出现的**每一个专业词**圈出来，
> 逐个问一句"它**第一次**出现的地方，有没有当场解释？"
> —— 有没解释的，就是违规，回去补。

> **为什么写进规则**：2026-09-20 之前，我连续几个模块只交代码 + 甩文档，负责人直接说
> **"我看不懂了"**。规则 15 的原始条文其实已经覆盖这件事（"在用户没看懂的情况下继续往下推进"是被禁止的），
> 但**没有把"讲"绑定到"下一轮开工之前"这个时间点上**，于是就被效率压过去了。
> 现在把它变成**前置门**：讲完才开工。

### 15.4 对既有产出的处置

规则 15 生效后，需要回头审视此前产出的内容：

| 产出 | 与新规则的关系 | **已执行的处置（2026-09-16 经负责人确认）** |
| --- | --- | --- |
| `Tools\Prepare-M0.ps1`（一键准备脚本：建 14 目录 + 搬 9 文件） | ❌ **属于 15.2 禁止的"一键式流程自动化"** | ✅ **已删除**。对应步骤改写为 `Docs\10-M0手动操作手册.md` **§1.3 手工教学版**：逐步命令 + 14 个目录各自的作用 + 通用途验证手法 + 失败排查表 |
| `Tools\Test-Network.ps1`（只读分层网络诊断） | ✅ 属于允许的"只读诊断工具" | ✅ **保留**，并补教学：`Tools\README.md` 新增「为什么分层 / **不用脚本你自己怎么用 6 条命令得出同样结论**」 |
| `Tools\Verify-PluginDlls.ps1`（只读校验程序集名） | ✅ 属于允许的"只读验证工具" | ✅ **保留**，并补教学：`Tools\README.md` 新增「为什么读元数据 / **一行 PowerShell 自己读程序集名**的对照方法」 |
| `Docs\10-M0手动操作手册.md` | ✅ 本身是逐步手册，方向正确 | ✅ **已加强**：§1.3 改为教学版；分工表与验收清单去掉对脚本的推荐 |

> **注意**：保留诊断/验证脚本不等于"仍可写脚本"。它们只允许做**只读观察**；
> 任何会**修改工程状态**的脚本，都按 15.2 禁止处理。
>
> **今后新增工具时的自检问题**：*它是在替用户做事，还是在帮用户看清事情？* 前者不写。

---

## 三、规则 13 在本项目的具体边界（最高优先级）

### AI 可以做

| 类别 | 具体 |
| --- | --- |
| 读写文件 | 读取任意文件；新建/修改项目内文件（需遵守规则 2 的"先说明再改"） |
| 只读命令行分析 | `git status/log/diff`、`grep`、`find`、编码探测、环境探测、目录列举 |
| Unity MCP 只读查询（若配置） | `unity_editor_state`、`unity_get_compilation_errors`、`unity_console_log`，以及读取场景/预制体/组件属性、搜索资产等**纯查询** |

### AI 一律不得代替执行（必须由项目负责人手动完成）

| 类别 | 具体示例 |
| --- | --- |
| Unity 编辑器内的按钮/菜单点击 | 创建工程、`File > Build Settings`、YooAsset Bundle Builder 的 Build、任何编辑器窗口操作 |
| 运行测试 / 进入 Play 模式 / 双端联机实测 | Play Mode、Unity Test Runner、多开客户端联机验证 |
| 打包与部署 | 打 exe、打内容包、上传/部署到服务器或真机 |
| 任何长时间占用编辑器或改变编辑器/运行时状态的操作 | — |

> **禁止**把"只读查询"当借口去代跑操作。只要涉及点击/构建/测试/打包，一律交负责人手动执行，或先经明确同意。
> 不确定某操作该不该做时，**先问，不擅自执行**；宁可少做，不可越界。

### AI 的正确做法

对需要手动执行的操作，**给出详细步骤**（规则 4）：菜单路径、按钮名称、输入内容、预期结果、以及"如果失败看什么"。

---

## 四、知识点总结规范（规则 1 + 14）

| 项 | 规定 |
| --- | --- |
| 落盘目录 | `E:\U3D Projects\0_MyFile\3D联网战斗Demo\DeepSeekOutput\` |
| 文件名 | `YYYY-MM-DD-知识点总结.md` |
| 日期来源 | **必须用系统时间核对**（`Get-Date -Format 'yyyy-MM-dd'`），不凭记忆推断 |
| 同日多次 | 在当天文件**末尾追加**，不覆盖 |
| 结构 | 按主题分章节；每个知识点含 **背景/问题 → 根因分析 → 解决方案/代码模式 → 踩坑记录** |
| 固定小节 | 末尾必须有 **「🎓 本次新概念（复习用，1~3 条）」**，只写本次真正用到过的概念 |
| 教学口径 | 新概念三段式「**这是什么 → 为什么这么做 → 不这么做会怎样**」；一次最多 1~3 条；术语首次出现给一句白话解释；**先给结论/可执行动作，再补原理小段**；优先用表格 + 本项目真实数字举例 |

---

## 五、本项目环境速查

| 项 | 值 |
| --- | --- |
| 项目根目录 | `E:\U3D Projects\0_MyFile\3D联网战斗Demo` |
| 需求文档 | `Docs\01-项目需求文档.md` |
| 知识点总结 | `DeepSeekOutput\` |
| Unity | **2022.3.62f3c1**（`C:\Program Files\Unity\Hub\Editor`；旧版 2022.3.17f1c1 **保留**给 `RPG_Git` 与 D14 探针） |
| 渲染管线 | URP |
| .NET SDK | 8.0.204 / 9.0.306 可用（服务端用 **.NET 8**） |
| MySQL | MySQL80 服务运行中 |
| Node.js | v24.18.0 |
| Git | 2.55.0（**本项目尚未初始化仓库**） |
| Python | 3.13（`D:\Python313`，可用于文档/表格处理脚本） |
| **AI 代跑构建** | ⚠️ **必须加 `-m:1`**：`dotnet build NBC.sln -m:1`。DSH 沙箱下多节点并行构建会**静默失败**（`0 个错误` + 退出码 1），根因与最小对照实验见 `Docs\11-环境配置说明.md` §四之二。**你在自己终端里构建不受此限** |

### 已冻结的关键技术决策（改前须经负责人确认）

Unity **2022.3.62f3c1** + URP **14.0.12** · C# 9 · **Google.Protobuf 3.21.1**（`protoc` 生成代码）· xLua · Behavior Designer **经典版**（禁换 Pro） ·
A\* Pathfinding Project **Pro** · **YooAsset** · .NET 8 + MySQL 8 · 自研定点数学 · 只做 PC

---

## 六、本文件的维护

- 规则条款变化、例外增删，**必须更新本文件并记录日期**，同时在需求文档 §17 记录决策。
- 本文件与 `Docs\01-项目需求文档.md` 冲突时，**以需求文档为准**（需求文档是本项目的唯一需求基准），
  但规则性条款（工作方式、工具边界）以本文件为准。
- **规则 15（教学优先）与需求文档中任何"效率 / 工期"表述冲突时，以规则 15 为准** ——
  因为它是项目目的层面的要求，而工期是手段层面的约束。

---

## 七、变更记录

| 日期 | 版本 | 变更 |
| --- | --- | --- |
| **2026-09-23** | **v1.9.7** | **W12 补充"首字母大写"**（负责人同日明确："A 吧，而且首字母要大写"）：类型前缀改为**首字母大写** —— **开始按钮叫 `Button_开始`**（原来是 `button_开始`）。好处之一是**与 Unity 自带物体的习惯一致**（`Main Camera` / `EventSystem` 都是大写开头）。<br>代码同步：`QuestPanel` 的默认控件名改为 `Text_Title` / `Text_Message` / `Layout_OfferList` / `Layout_TrackingList` / `Button_RowTemplate`。 |
| **2026-09-23** | **v1.9.6** | **新增 W12「场景物体命名规范」**（由项目负责人定）：`类型_物体名`，类型在前 —— 例：开始按钮叫 **`button_开始`**。理由两条：① Hierarchy 里一眼看出类型与用途；② **我们的 UI 框架按物体名索引控件**（A9 的 `BasePanel`），"控件名 = 物体名"，所以命名规范**直接决定代码里写什么字符串**。<br>⚠️ 配套要求：**控件名应当可配**（别把物体名硬编码在代码里）—— 样例是 `QuestPanel` 的 5 个控件名（`[SerializeField]` 字段，默认即规范名）。<br>⚠️ 同日还记一条 UI 事实：**项目现在两种文字组件都在用**（框架既有面板用 UGUI `Text`，负责人新搭的界面用 **`TextMeshProUGUI`**）→ 新写的面板**两种都要支持**（用基类 `Graphic` 取控件，再 `GetComponentInChildren<TMP_Text>()` 兜底到 `Text`）。 |
| **2026-09-23** | **v1.9.5** | **M2-D 收关 + M2 整体收关**，已讲清单 → 21 篇（补 `Docs\教学\M2-D-任务UI.md`；M2 收关报告见 `Docs\23-M2收关报告.md`）。<br>新增两条教训：<br>① **能测的界面**：面板拆"纯 C# 视图模型 + MonoBehaviour 摆放"，规则全在 EditMode 里（**不需要预制体**）；UI 两条专属坑 —— **动态 `Instantiate` 的按钮不会带上模板的点击监听**、**重建式刷新会让所有旧引用失效**。<br>② ⚠️ **闸门绿不等于 Unity 绿**：编译闸门是把全部代码编进**一个**程序集，**抓不到 asmdef 级的程序集边界错误**（这次 `NBC.Boot` 用到 `QuestPanel`，而 `Initialize()` 是基类 `BasePanel` 的成员、在 `NBC.Framework.UI`，没引用就 CS0012）。<br>⇒ 规则收紧：**改 asmdef、或跨程序集使用类型时，必须有一次 Unity 编译兜底**；闸门只能证明"API 与语法没问题"。 |
| **2026-09-23** | **v1.9.4** | **M2-C 收关，已讲清单 → 20 篇**（补 `Docs\教学\M2-C-装配与端到端.md`）。端到端闭环**已在 Unity 里真跑通**（不是只有测试绿）。<br>新增两条母题（都由这一轮的"卡住"逼出来）：<br>① **"读了源码"还不够，要读对代码路径** —— 同一功能两个入口（窗口 UI 一条、运行时反射一条）**输出目录不一样**；只读一个入口会得出"看起来有据可查"的错结论。判据落在 `Docs\16` §10.4.2 的更正里。<br>② **"手工步骤 + 没有提示" = 一定会在某天以别的 bug 的面目出现** —— 配置资产是快照、模拟清单也是快照，"忘了点第二步"**不报任何错**。→ 补了**资产过期检查菜单**（编辑器比文件时间）+ **运行时数据预检**（查 id，打包后也有效）。<br>③ 另记一条真缺陷修复：`ConfigImporter` 原来按"任意程序集里第一个同名类型"找类型 → 撞上测试里的同名假类型会**随机挑错**；判据收紧为"同名 **且** 真的有 `LoadFromTsv`"，有歧义**报出来不猜**。 |
| **2026-09-22** | **v1.9.3** | **M2-B 收关，已讲清单 → 19 篇**（补 `Docs\教学\M2-B-战斗内核.md`：共享层伤害结算 + 战斗世界 + 输入→技能）。<br>同时记下三条新母题（都由"红"逼出来）：① **一个走不到的分支比没有分支更糟** —— 判据是"**当你写不出某个状态的测试时，先怀疑那个状态是否真的存在**"；② **注释写着 A、代码做的是 B，比没有注释更危险**（`Bind` 注释说"装配时就报错"，实际只查了一半；**红的是用例，根因是代码**）；③ **规则一处实现、两个调用点 = fail early + fail loud**（技能归属：装配期早检查 + 结算期兜底）。<br>另记："断言里的数字必须算出来"**第 4 次**（这次不是阈值，是**我算错了用例里的伤害**：300 血的怪挨两发 120 不会死）。 |
| **2026-09-22** | **v1.9.2** | **M2-A 收关，已讲清单 → 18 篇**（补 `Docs\教学\M2-A-条件与任务.md`：条件系统 + 任务运行时）。<br>同时记一条**机械检查的新形态教训**（前 6 次都是"命中注释"，这次不同）：**阳性对照写的是小写 `quest`，而检查器大小写敏感 → 对照红了；但它暴露的是检查器有个洞**（`questId` 这类驼峰写法会溜过去）。<br>⚠️ 由此新增一条做法：**阳性对照必须覆盖"最可能漏掉的形态"，而不是最容易写的形态**；且**真实检查与阳性对照必须走同一个判定函数**（两处各写一份匹配逻辑，迟早不一致）。详见 `Docs\教学\M2-A-条件与任务.md` §⑧ |
| **2026-09-22** | **v1.9.1** | **§15.3.1 新增「阶段总复习」**：里程碑收关时，除单模块留档外再补一篇 `Docs\教学\M1-总复习.md`（模块地图 / 单篇索引 / **跨模块方法论母题** / 实测数字 / 面试话术 / **本项目犯错清单**）。定位是**复习入口**，不替代单篇。已讲清单 → **17 篇** |
| **2026-09-22** | **v1.8** | **新增 W3a**：`Get-Content` 在**行数**上也是错的（实测 1488 行报 1094 行，静默吞掉 394 个换行），`Select-String` 的行号随之偏移。**后果是一次"假绿"校验**：拿错行号去切段落做 Markdown 表格完整性检查，切到了完全不相干的段落，**一个表格都没检查**却报"0 问题"。规则改为：**凡是要行号/计数/按行切片，一律用 `[System.IO.File]::ReadAllText/ReadAllLines(..., UTF8)` 或 `read`/`grep` 工具**。② **§15.3.1 已讲清单补 E1**（`Docs\教学\E1-性能看板.md`，共 14 篇） |
| 2026-09-16 | v1.0 | 建立本项目规则裁剪版：14 条规则的适用性对照、规则 12 的 5 项书面例外、规则 13 的工具边界、知识点总结规范、环境速查 |
| 2026-09-16 | v1.0+ | 新增「代码书写附加条款」W1~W8（XML 注释禁 `--`、交付前只读校验、`.ps1` 正文全 ASCII、含非 ASCII 文件的"消费方编码"判据等） |
| **2026-09-16** | **v1.1** | **新增规则 15「以培养用户能力为目标」**：明确"教学优先于效率"的冲突裁决、**「一键脚本」的判据**（替用户完成流程 = 禁止；帮用户观察判断 = 允许）、每一步对话的教学要求，以及对既有产出（`Prepare-M0.ps1` 等）的处置方向 |
| **2026-09-20** | **v1.2** | ① **重写 W5/W6**：`.gitignore` 不支持行尾注释（静默失效）、`core.ignoreCase=true` 会误伤 Unity 的 `Packages/`（正解是收窄作用域而非取反）、必须用**真实 git** 验证规则；② **新增 W5a 更正条**：推翻原 W5 关于 `[Ll]ibrary/` 与 `**/bin/` 的两条错误结论（实测证明二者**都有效**）；③ **新增 W9**：自研校验器/模拟器必须先被校验，能用官方工具就用官方工具；④ 记录 **D14 由 AI 代跑**这一例外授权的边界（仅在规则 13 范围内做 .NET 编译与运行，不碰 Unity 编辑器）；⑤ **环境速查新增"AI 代跑构建必须加 `-m:1`"**（DSH 沙箱下多节点并行构建会静默失败，已用最小对照工程证明与本项目无关，见 `Docs\11-环境配置说明.md` §四之二） |
| **2026-09-20** | **v1.3** | ① **序列化库由 `protobuf-net` 换为 `Google.Protobuf`**（D17 实测：protobuf-net 在 IL2CPP 下含 `repeated` 字段必崩）→ 同步更新能力选型表与「已冻结的关键技术决策」行；② **M0 全部 18 项完成（18/18）**，D17 第 3 次出包通过（protobuf 49 字节含集合字段 + xLua 1..10=55）；③ 相关证据链见 `Docs\09-IL2CPP验证清单.md` §五、`Docs\05-依赖清单.md` §2.1/§2.2 |
| **2026-09-20** | **v1.4** | **新增 §15.3.1「下轮开工前必须先讲上一轮的模块」**（由项目负责人明确要求）。起因：M1 期间我连续几个模块只交代码 + 甩文档，负责人直接说**"我看不懂了"**——规则 15 原文其实已覆盖此事，但**没把"讲"绑定到"下一轮开工之前"这个时间点**，于是被效率压过去了。现在改为**前置门**：模块收关后先完整讲一遍，讲完才动下一轮。同时补记教学结构（七段式）与"已讲/待补"清单 |
| **2026-09-21** | **v1.7** | ① **新增 W10「代码改动边界」**（负责人："后续要尽量避免修改框架代码或者插件源码"）：第三方插件/包源码**一律不改**；已收关框架模块的既有代码**尽量避免改**，优先"新增"而非"改动"；非改不可时四件事一件不能少；**不许为了让自己代码通过而改守卫**。② **§15.3.1 教学改为不阻塞**（负责人："你教我的时候可以把知识放在 md 文档中，我打开文档看，然后你继续工作，不因为教我而停止工作"）：模块收关当场写 `Docs\教学\`，**同一条回复里继续下一个模块**；对话里只给 5~10 行导读 + 文档链接 |
| **2026-09-21** | **v1.6** | **新增 §15.3.1.1「说人话」硬约束**（由项目负责人**第二次**指出："教我的时候尽量说人话，太多专业术语混在一起我看不懂了"）。**这是同一个问题的第二次**：2026-09-20 的"我看不懂了"催生了 §15.3.1，但它只写了"不许甩术语"、**没给可执行判据**，于是我又用术语堆叠讲了一遍 A8。本次补上 **7 条可当场判定违规的硬规则**。⚠️ **同日稍后又被负责人纠正**：我把"术语太多"理解成了"禁用术语 + 限制代码块长度"，属**矫枉过正**；已按"专业词可以用但必须当场解释、代码示例越多越详细越好、语言要自然、篇幅不设上限"重写 |
| **2026-09-20** | **v1.5** | **§15.3.1 新增「留档」要求**（由项目负责人明确要求）：讲过的内容须**同步落成 `Docs\教学\` 下的独立 md**（一模块一篇，命名如 `A7-输入管理.md`），供负责人随时复习。**关键边界**：留档是"讲"的**补充而非替代**——§15.3.1 原有的"把教学写进 `Docs\` 当附录就当讲过了"这条禁止**继续有效**；留档正文**照抄对话原话、不做二次精炼**（精炼后又会退化成看不懂的文档）。同步补讲并留档 **A7 输入管理** |

