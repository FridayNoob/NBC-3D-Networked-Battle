# M3 开工清单 · 网络框架 + 房间 + 副本组队

| 项 | 内容 |
| --- | --- |
| 文档版本 | v1.6（2026-09-23 建立；同日回填 S1~S5b + S6a 证据，见 §七、§八） |
| 对应里程碑 | **M3 · 网络框架 + 房间 + 副本组队**（`Docs\20-M2M5重排期草案.md` §三） |
| 用途 | 把 M3 拆成**可执行、可验收**的切片；每个切片**独立提交** |
| 前置 | **M2 已收关**（`Docs\23`）；**B4/B5 打包已完成**；protoc/运行时已配对（见 §二） |
| 适用规则 | 规则 12（成熟方案 + 例外 E2 自研传输）、规则 13（AI 不代跑 Unity）、规则 15（教学优先） |

---

## 一、M3 要达成什么

**一句话**：**两个客户端**组队进副本，**打死 1 个 BOSS**，**掉落一致**。

### 1.1 验收标准（源自 `Docs\20` §三 M3 行）

| # | 完成标准 | 验证方式 |
| --- | --- | --- |
| V1 | 自研传输层跑通（`Google.Protobuf` 载荷，**两端同一份协议代码**） | 一个 echo/心跳协议端到端 |
| V2 | 房间与席位：2 人能进同一个房间，各自收到**同样的世界状态** | 两个客户端各自的画面/日志一致 |
| V3 | **状态同步（服务器权威）**：服务端 30 Hz 跑战斗、下发快照 | 服务端日志与客户端表现对得上 |
| V4 | 副本实例 + BOSS（复用 A10 状态机） | 打死 BOSS |
| V5 | **掉落一致**：服务端权威掉落，两边看到一样的东西 | 两边收到同一份掉落 |

### 1.2 明确不做（写清楚，免得以后以为漏了）

| 不做 | 什么时候做 |
| --- | --- |
| **匹配队列** | **维持不做**（`Docs\01` §260–261 的非目标） |
| `HostPlayMode`（热更） | M5 |
| 多阶段 / 多 BOSS / 难度分级 / 随机词缀 | 作品集 Demo 不做（`Docs\20` §二） |
| 观战 / 回放 | 不做 |
| **客户端预测 + 回滚** | **M4**（PVP 帧同步时才需要；M3 先正确、后手感） |
| AOI / 分线 / 分服 | ✅ **已按 Q3（2~4 人）砍掉** |

---

## 二、现状盘点（**每一项都实测过**，不是"应该有"）

| 起点 | 状态 | 对 M3 的意义 |
| --- | --- | --- |
| **协议工具链** | ✅ `protoc.exe` = **libprotoc 3.21.1**（`E:\U3D Projects\TeachNet\Protobuf\`），与运行时**配对** | ⚠️ 离线包里那份 `protoc-35.1` **不能用**（要配 3.35.1 运行时）；`Docs\11` §5.2 有命令与坑 |
| **协议运行时** | ✅ `Client\Assets\ThirdParty\GoogleProtobuf\Google.Protobuf.dll` = **3.21.1** | 两端同一份生成代码 |
| **协议唯一来源** | ✅ 仓库顶层 `Protocol\`（现只有 M0 探针 `nbc_probe.proto`） | M3 新增正式协议文件 |
| **生成物落点** | ✅ `Client\Assets\_Project\Protocol\`（命名空间 `NBC.Protocol`，**入库**） | 服务端用 `<Compile Include>` 引同一批文件 |
| **服务端骨架** | ✅ `NBC.Server.Core` / `.Game` / `.Host` / `.Tests`（.NET 8） | M3 往里填 |
| **双端共享层** | ✅ `Client\Assets\_Project\Shared\`（条件系统 + **伤害结算**），服务端 `<Compile Include>` 引同一份 | ⭐ **M3 的服务端可以直接用 M2 的伤害结算**（当时那条判据换来的） |
| **框架底座** | ✅ A 组：`MonoManager`（帧宿主/定时器）、事件中心、场景、输入 | 网络 tick 挂在宿主上跑 |
| **装配层** | ✅ M2 的 `BattleSession`（**不含逻辑**，纯装配） | 服务端与客户端**共用同一套装配形状** |
| **BOSS 行为** | ✅ A10 的 `StateMachine` / `HierarchicalStateMachine`（`FW-M14`） | 服务端跑 BOSS AI ⚠️ **2026-09-23（S6b）实测更正**：那 5 个文件确实是纯 C#，**但它引的 `EventId` 不是**（里面有一句 `Debug.LogWarning`）→ 看**整条依赖链**才准（见 §8.12） |
| **怪物/技能/掉落表** | `Monster`/`Skill` 已有；**`Dungeon`/`DropTable` 要在 M3 加** | C 组工具加表**不改一行工具代码** |

### 2.1 环境约束（实测，会直接影响写法）

| 约束 | 应对 |
| --- | --- |
| **沙箱里 NuGet restore 不可用**（`NU1301`） | 服务端**只允许 BCL**；第三方（protobuf）走仓库里的 DLL |
| `dotnet build` 必须 **`-m:1`** | 所有构建命令都带 |
| `dotnet run` 必须 **`--no-build`**（否则沙箱下构建 stdio 被挡） | 先 build 再 run |
| **AI 不能开 Unity、不能点菜单**（规则 13） | 客户端的场景/预制体由负责人操作 |

---

## 三、关键决策（**请负责人过目**；推荐已标注）

| # | 决策 | 推荐 | 理由 | 别的选项 |
| --- | --- | --- | --- | --- |
| D1 | **传输层** | **TCP + 4 字节长度前缀 + protobuf 载荷** | M3 是**状态同步**，"可靠 + 有序"最重要；自研（需求 12 的例外 E2）；**不引第三方**；几十行就能跑通 | 直接上 UDP+KCP 式重传：M3 用不上，且调试成本高 |
| D2 | **服务端 tick** | **30 Hz**（需求 §6.1 已定） | 客户端 60 fps、服务端 30 Hz 下发，2~4 人完全够 | —— |
| D3 | **权威边界** | **服务端算伤害/位置/掉落**；客户端只发**意图**（输入命令） | 这是"服务器权威"的定义；也让 M4 的 PVP 能直接复用 | 客户端自算 + 服务端校验（更复杂，M3 不需要） |
| D4 | **要不要客户端预测** | **M3 不做**（**有意**的取舍） | 先正确、后手感；预测+回滚属 M4 的帧同步 | 现在做 → M3 会变成"手感 + 网络"两件事一起调 |
| D5 | **同步内容与粒度** | 每 tick **全量实体快照** + 事件（伤害/死亡/掉落） | 2~4 人、实体数少 → 全量最省心；**差分/压缩留 M5** | 增量同步：现在做会先优化后正确 |
| D6 | **房间与副本** | **一个房间 = 一个副本实例**；进程内 `RoomRegistry`，每房 2~4 席位 | 最简单又能演示组队；也天然支持"打完退出" | 独立副本服：M5+ |
| D7 | **共享什么** | 协议（`.proto`）+ **M2 已有的伤害结算/条件系统**；**位置移动的确定性逻辑暂不共享** | M3 不是帧同步，位置由服务端权威下发即可；共享层已经在，白拿 | 现在就把移动也做成确定性的：M4 帧同步时再做 |
| D8 | **两个客户端怎么跑** | **一个 Editor + 一个打包版**（需要打一次包） | Unity 里同时 Play 两个实例做不到；这是最省事又真实的方式 | ParrelSync 之类第三方工具：多一个依赖，且是编辑器插件 |

---

## 四、切片顺序（每片自成闭环）

| 片 | 内容 | 产出 | 验收 |
| --- | --- | --- | --- |
| **S1** | **协议定义**：`Protocol/nbc_m3.proto`（握手/进房/席位/输入/快照/伤害事件/死亡/掉落）+ protoc 生成两端 C# | `.proto` + 生成物入库 | 两端都能编过（客户端走编译闸门，服务端走 `dotnet build -m:1`） |
| **S2** | **传输接缝 + 实现**：框架侧只定义 `ITransport`（**不认识 TCP**）；新增适配程序集装 TCP 实现（BCL `System.Net.Sockets`，不算第三方） | 接缝 + 实现 + EditMode 用例（假传输） | 假传输上跑通"发送→分帧→回调" |
| **S3** | **端到端回环**：服务端监听 + 客户端连接 + 心跳/echo | 服务端 `NBC.Server.Host` 能起、客户端能连 | **本机两个进程互通**（这是"网络真的通了"的第一证据） |
| **S4** | **房间与席位**：`RoomRegistry` + 加入/离开 + 席位广播 | 房间模块 + 用例 | 2 个连接进同一房间、各自收到席位表 |
| **S5** | **状态同步**：服务端跑战斗（**复用共享层伤害结算**）+ 30 Hz 快照下发 + 客户端按快照更新 | 同步模块 + 用例 | 客户端表现与服务端日志一致 |
| **S6** | **副本 + BOSS**：`Dungeon`/`DropTable` 配置表 + 怪物 AI（复用 A10 状态机）+ BOSS 技能 | 配置表 + AI + 用例 | 打死 BOSS |
| **S7** | **掉落一致**：服务端权威掷掉落并广播 | 掉落模块 | 两边收到**同一份**掉落 |
| **S8** | **网络模拟器**（`NET-07`，⚠️ **面试关键道具**）：可注入延迟/丢包/抖动 | 模拟器 + 用例 | 注入 200ms 延迟后仍能打完 |
| **S9** | **端到端验收 + 收关**：两个客户端进副本打死 BOSS、掉落一致 | `Docs\26-M3收关报告.md` + 教学留档 | 负责人亲手跑通 |

> 📌 顺序的理由：**先把"能通"证明掉（S2/S3），再谈同步逻辑**。
> 反过来做的话，一旦不通，你分不清是"同步写错了"还是"根本没连上"。

---

## 五、新增/改动的程序集与文件（预告）

| 位置 | 内容 |
| --- | --- |
| `Protocol\nbc_m3.proto` | 正式协议（**唯一来源**） |
| `Client\Assets\_Project\Protocol\` | protoc 生成的 C#（**入库**） |
| `Client\Assets\_Project\Framework.Net\`（新程序集） | **接缝 + TCP 实现**：`ITransport`/`NetTypes`/`TcpTransport`（BCL，不引第三方，**引擎无关**）⚠️ S2b 实测后调整：接缝没有放进 `NBC.Framework`，理由见 §7.1 |
| `Client\Assets\_Project\Game\Net\` | 客户端会话/装配（把网络接进 `BattleSession`） |
| `Server\NBC.Server.Core\` | 传输/分帧/房间注册表 |
| `Server\NBC.Server.Game\` | 战斗权威（**引共享层伤害结算**） |
| `Server\NBC.Server.Host\` | 入口（已有 `Program.cs`） |
| `Configs\Design\Dungeon.csv` / `DropTable.csv` | 副本与掉落（C 组加表**不改工具代码**） |

---

## 六、风险（如实记）

| # | 风险 | 应对 |
| --- | --- | --- |
| 1 | **两客户端联调麻烦**（要打包一次） | S8/S9 才需要；前面所有片都能"单进程 + 假传输"验证 |
| 2 | 沙箱 NuGet 不可用 | 服务端只用 BCL；protobuf 用仓库里的 DLL |
| 3 | **状态同步的手感**（没有预测 → 操作有延迟） | **有意**的取舍（D4）；M4 做预测+回滚时再解决，而且那时正是"帧同步"上场 |
| 4 | 协议一经下发就难改 | 协议里**留 version 字段**（握手时校验），两端不一致直接拒绝并说清 |
| 5 | 服务端与客户端的**装配重复** | 共用 `BattleSession` 的**形状**（构造参数一致），但服务端不依赖 UnityEngine → 装配代码分两处，**共享的是它下面那层**（伤害结算/条件系统） |

---

## 七、进度与证据（逐片回填）

> 规矩：这一节只写**能复核**的东西（文件路径 / 命令 / 实测输出）。
> "应该有"不算证据 —— 这是 `Docs\00` 里"先测量再说"那条。

| 片 | 状态 | 证据 |
| --- | --- | --- |
| **S1** | ✅ | `Protocol\nbc_m3.proto`（16 个消息，顶部 `// CONTRACT_VERSION: 1`）+ `Client\Assets\_Project\Protocol\NbcM3.cs`（protoc 3.21.1 生成，入库）；`ProtocolTests` 8 条**直接读 .proto 文件**比对版本 |
| **S2a** | ✅ | `Shared\Net\NetContract.cs` + `Shared\Net\FrameCodec.cs`（双端共享，`FrameCodec` + `FrameDecoder`）；`FrameCodecTests` 14 条；探针 7 条分帧用例 —— 提交 `561db9b` |
| **S2b** | ✅ | `Framework.Net\` 新程序集（`ITransport.cs` / `NetTypes.cs` / `TcpTransport.cs` + asmdef + 3 个 `.meta`）；`TcpTransportTests` **12 条**（真开回环端口）+ `FakeTransport` + `FakeTransportTests` **6 条**；编译闸门 **0 错 0 警告**；探针 **37 -> 45 全绿**（比 S2a 多 8 条真 socket 用例）。**负责人 2026-09-23 在 Unity 里实跑：610 全绿** ✅ |
| **S3** | ✅ | **服务端**：`INetTransport` 补上"收到消息"的出口（M0 少了半边）+ `TcpServerTransport` + `ServerMessageRouter`（握手门 + 显式注册表）+ `ServerMessagePump`；`NBC.Server.Host` 真起监听 + 30Hz 主循环 + Ctrl+C 优雅退出。**端到端探针 17/17 全绿**；**两进程互通实测通过**。<br>**客户端**：`Game\Net\NetSession`（握手/心跳/RTT/收包分发，时间注入）+ 编辑器窗口 `Tools/NBC/网络/网络调试窗口`；`NetSessionTests` **17 条**（全用 `FakeTransport`，不开端口）；**探针"会话层模式"对着真服务端 6/6 全绿**（细节见 §8.3、§8.6） |
| **S4** | ✅ | 协议：追加 `LeaveRoomRequest` + `ErrorResponse`（**兼容改动，`CONTRACT_VERSION` 保持 1**）+ protoc 重新生成；服务端：`RoomRegistry`（纯规则）+ `RoomService`（注册处理器 / 整份广播 / 断线自动退房）+ `ClientSession.PlayerName`；客户端：`NetSession` 房间状态与 `JoinRoom/LeaveRoom` + 编辑器窗口的房间区。**探针 17 → 31 条全绿**（+6 房间规则、+8 走网线），会话层模式 **8/8**（含真服务端进房/离开）。细节见 §8.7 |
| **S5** | ✅（下行） | **快照下行完成**：`DungeonBattle`（服务端权威世界：整数毫米位置 + 巡逻 + 复用共享层 `DamageMath`）+ `RoomBattleService`（每逻辑帧推进世界 + **序列化一次、发多份**全量快照）+ Host 按 tick 驱动；客户端 `SnapshotView`（**只由快照决定的世界**，含"旧快照丢弃"分支）+ 编辑器窗口的世界区。**探针 31 → 43 全绿**（+12 条状态同步）、EditMode +8。**负责人 2026-09-23 实跑：643 全绿 + 窗口世界区确认** ✅ 见 §8.9 ⚠️ **上行（PlayerInput）未做**，见 §8.5.1 |
| **S5b** | ✅ | **输入上行完成**：`NetSession.SendInput`（沿用 A7 的 `InputCommand`，量化约定同一套）+ 服务端 `RoomBattleService.HandleInput`（**权威校验** `player_id` / **移动是状态**、**动作是事件** / 输入保鲜期 6 帧 / 轴钳位）+ `ReconcileHeroes`（**一个席位一个英雄**，缺的补、走的删）+ `DungeonBattle` 的移动与普攻判定（射程/冷却/不能打自己人）。**探针 43 → 57 全绿**、EditMode +4。**负责人 2026-09-23 实跑：647 全绿 + 窗口「输入」区驱动英雄成功** ✅ 见 §8.10 |
| **S6a** | ✅ | **副本与数值改由配置表决定**：新增 `Configs\Design\Dungeon.csv`（ConfigKit **0 错 0 警告 / 32 文件 / 退出码 0** —— 加表**没改一行工具代码**）；服务端新增 `ServerTables`（读**源 CSV**）+ `DungeonBattle.FromDungeon`（阵容/血量/移速/普攻伤害全来自表）+ Host 启动时读表（读不到**不启动**）。**探针 57 → 64 全绿**（含"副本不在表里必须开不了"的负向对照）。见 §8.11 |
| **S6b** | ✅ | **BOSS AI 完成**：`BossBrain`（**用 A10 的 `StateMachine<S>`**，同一份源码编进服务端）驱动 Idle/Chase/Attack 三态；`DungeonBattle` 补上"每单位自己的攻击力/移速"（顺手修掉一个隐藏 bug：原先 BOSS 打人用的是**英雄的**伤害）、找最近英雄、够不够得着、朝目标走。**探针 64 → 70 全绿**。见 §8.12 |
| **S7** | ✅ | **掉落一致完成**：新增 `Configs\Design\DropTable.csv`（概率用**万分比**，ConfigKit 0 错 0 警告）+ 服务端**确定性**掷骰（`BattleRandom`：**不用 `System.Random`**，它的算法在 .NET 版本间变过）+ 怪**从活着打到死**那一下才掷 + `RoomBattleService` 把掉落事件**序列化一次发给全房**。**探针 70 → 74 全绿**。见 §8.13 |
| **S8** | ✅ | **网络模拟器（NET-07）完成**：SimulatedTransport（**也是一个 ITransport**，包在任意实现外面）+ NetSimProfile（延迟/抖动/丢包，概率用万分比）；⚠️ 时间入口是 Advance(deltaMs)（**不污染接缝**）；M3 走 TCP 所以探针**只注入延迟/抖动**，丢包留给 EditMode 与 M4。**EditMode +8、探针 74 → 77 全绿**。见 §8.14 |

### 7.1 S2b 里三个值得记的决定

| # | 决定 | 为什么（**实测出来的，不是审美**） |
| --- | --- | --- |
| 1 | **接缝 `ITransport` 和 TCP 实现住同一个程序集** `NBC.Framework.Net`（`noEngineReferences: true` + 只引 `NBC.Shared`），**不放** `NBC.Framework` | 放 `NBC.Framework` 的话，网络层就挂上了 UnityEngine 依赖 → 我在沙箱里**一行都跑不了**（只能等负责人开 Unity）。挪出来之后，`Server\_condition-probe`（.NET 8）能把这批源码直接编进去**真开端口跑**：回环、粘包、拆包、对端关闭、超限帧当天就有证据。代价：网络层用不了框架的日志/事件中心（报错走 `Closed` 的人话原因，够用） |
| 2 | **`NoDelay = true`（关 Nagle）** | Nagle 会攒小包，最多压到 ~200ms 才发 → 对 30Hz 动作战斗就是"操作忽然一顿一顿的"。我们包本来就小、频率也低，值得 |
| 3 | **收到 0 字节 = 对端关了，必须当断开** | `Poll(SelectRead)` 为真但 `Available == 0` 就是 FIN。漏掉它的表现是"对方早退了，本端一直以为自己还连着" —— 又一个只在联机里才看得见的静默失败（已写成用例 `Receive_PeerCloses_ReportsClosed`） |

### 7.2 探针这一版**实测**到的两条（写下来，免得以后当成"应该是那样"）

| 场景 | 本机实测原因（探针原样打印） |
| --- | --- |
| 连一个没人监听的端口 | `连接 127.0.0.1:10027 失败：对端没有在监听（连接被拒绝）` |
| 连不可达地址 `10.255.255.1:9`（超时设 300ms） | `连接 10.255.255.1:9 超时（300ms）`，耗时 **309ms** → **走的是超时分支**（不是"网络不可达"），所以那条 Unity 用例确实覆盖到了超时这条代码路径 |

### 7.3 顺带修掉/补上的一处（诚实记）

`Tests\EditMode\Framework\AsmdefReferenceGuardTests.cs` 的映射表原来只有 11 行，且是**按命名空间全名精确匹配**的 —— 于是：

* `using NBC.Framework.Net.Adapter;`（用了实现、却只引了接缝程序集）**从表里漏过去**
* `using NBC.Shared.Net;` 同理

本片补了 4 行（`NBC.Framework.Net` / `NBC.Framework.Net.Adapter` / `NBC.Framework.Asset.Adapter` / `NBC.Shared.Net`）并加了一条阳性对照。

⚠️ 因为沙箱里跑不了 Unity 的 NUnit，我**用脚本把这条守卫的逻辑重放了一遍**（映射表直接从那个测试文件里解析出来，避免出现第二份会漂移的副本）：12 个 asmdef、**0 处违规**。
重放**不等于**Unity 里那条用例通过（它自己还要编过、还要在 Unity 里跑）—— 如实记。
另外记一个重放脚本自己踩的坑：第一版把整个测试文件都扫了，于是用例里 `new List<string> { "NBC.Framework", "NBC.Shared" }` 这种**样本**被当成了表项，冒出 3 条假违规（"机械检查的范围没界定好" —— 本项目栽过的第 N 次）。

### 7.4 待办（写下来，免得静默丢掉）

| # | 待办 | 什么时候 |
| --- | --- | --- |
| 0 | **ParrelSync 落位**：`Client\Assets\ParrelSync\` → `Client\Packages\ParrelSync\`（**需要先关 Unity**，再由 AI 挪或负责人自己挪；理由与坑见 §8.8）。⚠️ 克隆目录的 `.gitignore` 已提前补好并实测 | 随时（S5 联调前） |
| 1 | **教学留档《M3-A-网络层》**：S2a（分帧）+ S2b（接缝/拉模式泵/真 socket）+ S3（服务端、会话层、握手）当成**一课**写成 `Docs\教学\M3-A-网络层.md`（规则 15） | ✅ **已完成**（2026-09-23，已讲清单 → 22 篇） |
| 2 | 负责人跑一次 Unity：`Tests_EditMode` 应为 **643 条** ✅ **已完成（643 全绿）**；窗口「加入房间 / 离开房间 / 世界区」也已实跑确认 ✅ | ✅ 已验 |
| 3 | `NBC.Framework.Net` 的 asmdef 引用越界由 `AsmdefReferenceGuardTests` 盯着（已补 5 行映射：Net / Net.Adapter / Asset.Adapter / Shared.Net / Protocol） | 已就位 |
| 4 | **S5 开工第一件事**：定下 `SharedInfo.SnapshotSendRate=20` 与"每 tick 全量快照"的冲突（见 §8.5） | S5 |
| 5 | **教学留档《M3-B-房间与状态同步》**：S4（房间表 + 席位广播）+ S5 上下行（权威世界、快照、输入）合起来当一课 | ✅ **本片收关时交付**（见 `Docs\教学\M3-B-房间与状态同步.md`） |
| 6 | **S5b（输入上行）** ✅ 已完成（见 §8.10） | ✅ |
| 7 | 负责人跑一次 Unity：`Tests_EditMode` 应为 **647 条**；窗口里点「加入房间」后用「输入」区驱动英雄（持续发送 → 世界区里自己的英雄坐标在变） | S6 开始前 |
| 8 | **Unity 里点一次配置表导入**（新增了 `Dungeon` 表，**手工步骤 + 没有提示**）：菜单 `Tools/NBC/配置表/导入 TSV → ScriptableObject` → 生成 `DungeonConfig.asset`。⚠️ 现在客户端还没用到副本表，所以**不点也不影响联机**；但"表加了资产没导"正是那条会某天以别的 bug 面目出现的坑 | 有空时 |
| 9 | **S6b：BOSS AI**（A10 状态机驱动追击/攻击）—— 已确认 A10 的 5 个文件**全是纯 C#**，服务端可直接 `<Compile Include>` 复用 | 下一步 |

---

## 八、S3 预备：服务端骨架**盘点结果**（2026-09-23 读源码，不是猜的）

开工之前先把 `Server\` 现有的东西读了一遍 —— 结果有三条会**直接改变 S3 的写法**：

| # | 盘点到的事实 | 对 S3 的影响 |
| --- | --- | --- |
| 1 | `NBC.Server.Core` 里**已经有** `INetTransport` 骨架，注释里就写着「`TcpTransport` —— 真实联机（M3 实现）」；还有 `ClientSession`（含 `SessionPhase`：Connecting/InLobby/InRoom/InBattle/Closed）和 `TickScheduler`（累加器切定长帧、`maxTicksPerUpdate` 防补帧风暴） | ⭐ **不新造轮子**：S3 就是**实现这两个骨架** + 接上 `TickScheduler`。M0 当时留的坑现在填 |
| 2 | 但 `INetTransport` **只有** `IsRunning/Start/Stop/Send(sessionId,…)/Disconnect(sessionId,…)`，**没有"收到消息"的出口** | ⚠️ 必须**扩接口**（加"消息到达/会话建立/会话关闭"三个事件）。M0 的骨架少了半边 —— 这是要改的，不是能用就行的 |
| 3 | `NBC.Server.Tests` 里**没有任何测试框架**（`PackageReference` 那段被注释掉了，因为沙箱还原不了 NuGet，见 `Docs\11` §四之三②） | ⚠️ 服务端**没有 xUnit/NUnit 可用** → S3 的验收只能靠：① `dotnet build Server\NBC.sln -m:1`；② 一次性探针（`Server\_condition-probe` 那一套）真跑一遍；③ `NBC.Server.Host` 真启动 |

### 8.1 S3 的形状（下一步照这个做）

```
服务端                                         客户端
──────                                         ──────
NBC.Server.Core\TcpServerTransport              NBC.Framework.Net\TcpTransport（S2b，已完成）
  · TcpListener + 接受循环（非阻塞）               · 非阻塞连接 + Pump
  · 每个连接一个 ServerSession（复用 ClientSession）
  · 收到的字节 → FrameCodec.FrameDecoder → 载荷 ── 两端**同一份** Shared\Net\FrameCodec
  · Send(sessionId, payload) → FrameCodec.Encode → 那条连接的发送队列
  · 事件：MessageReceived / SessionOpened / SessionClosed（**要扩 INetTransport**）
                ↓ 载荷（protobuf）
NBC.Server.Core\ServerMessageRouter（显式注册，不用反射 —— Core csproj 里 NET-02 的要求）
  · 握手门：未握手只许发 Handshake（其余直接拒 + 说明原因）
  · Handshake → 版本校验（`NetContract.Version`）→ HandshakeAck{accepted, reason}
  · Ping → Pong{client_time 原样回}
                ↓
NBC.Server.Host\Program.cs
  · 起监听 → 用 TickScheduler 跑 30Hz 主循环 → Ctrl+C 优雅退出
  · 循环里只做：推进 tick + 处理队列里的消息（**网络 IO 回调只入队**，M0 就定下的规矩）
```

### 8.2 S3 的验收（我自己能拿到的证据）

| # | 证据 | 怎么拿 |
| --- | --- | --- |
| 1 | `dotnet build Server\NBC.sln -m:1` 0 错 0 警告 | 我跑 |
| 2 | 新探针：起真服务端（端口用 0）+ 用**客户端 `TcpTransport`**（S2b 那个）连上去 | 我跑 |
| 3 | **握手成功** → `HandshakeAck.accepted == true` | 探针断言 |
| 4 | **版本不一致被拒** → `accepted == false` 且原因是人话、连接被断开 | 探针断言（负向对照，必须有） |
| 5 | **Ping → Pong**，且 `client_time` 原样回来 | 探针断言 |
| 6 | 两个客户端同时连（Q3 的 2~4 人），各自收到自己的 `Pong`（**不许串台**） | 探针断言 |
| 7 | 负责人：`NBC.Server.Host` 真启动一次 + 客户端能连 | 需要负责人跑（规则 13） |

### 8.3 S3 实跑结果（2026-09-23，**数字都是跑出来的**）

**端到端探针**（`Server\_net-probe`，一次性脚手架，不进 Git —— 服务端用**生产代码**，客户端用 Unity 侧那份 `TcpTransport` 源码，真 socket + 真 protobuf）：

```
通过 17，失败 0。  结果：✅ 全绿
```

17 条覆盖：握手成功（玩家号/版本回显/tick 率）+ 会话进 InLobby + 拿到 player_id、
心跳往返、**版本不一致先收到说明再断开**（3 条，同时验证"先回包后断开"的顺序）、
空 payload 判违规、**没握手就发别的被断**、还没实现的消息只记一句不断开、
**两个客户端不串台**、服务端同时持有 2 会话、客户端拔线被认出并释放席位、
**心跳超时主动断开**（2 条）、撒谎长度前缀判违规、`FrameCodec` span 版与 byte[] 版一致。

**两进程互通**（S3 的验收本体 —— 进程内用例再多也证不了跨进程边界）：

| 步骤 | 实测输出 |
| --- | --- |
| 起 `NBC.Server.Host.exe --port=7799` | `[就绪] 已监听 7799` / `路由已注册 2 种消息：Handshake（内建）+ Ping` |
| 另一个进程跑 `NetProbe --connect=7799` | `✅ 连上服务端进程` / `✅ 握手成功（跨进程）` / `✅ 心跳往返（跨进程）` / `✅ 连接全程没被断开` |
| 服务端那侧的日志 | `[接入] 会话 1 来自 127.0.0.1:5245`<br>`[提示] 会话 1 握手成功 → 玩家 1（客户端 v1，tick 30Hz）`<br>`[断开] 会话 1（玩家 1）：对端关闭了连接（收到 0 字节）` |

**30Hz 主循环实测**：`--ticks=600` 自己退出，整个进程 **20562ms**；扣掉 .NET 启动（约 700ms）≈ **30.2 Hz** ✓（D2 定的 30Hz 不是"配了就算"，是量出来的）。

### 8.4 S3 里撞到的三条（都值得留档）

| # | 撞到什么 | 结论 / 处理 |
| --- | --- | --- |
| 1 | **CS0012：`MessageParser<>` 在未引用的程序集中定义**（`NBC.Server.Host`） | 就是"**引用不传递**"那条规律，**今天在 csproj 上撞了两次**（Host、探针）：`NBC.Server.Core` 引了 Google.Protobuf，但 `<Reference>`（裸程序集引用）**不传递** —— 用到 `ClientMessage.Parser` / `ToByteArray()` 的工程必须自己引一份。**遇到 CS0012 先读它的原文**（它会点名哪个程序集），别猜 |
| 2 | **CS1061：`ServerMessage` 未包含 `ToByteArray`**（同一个文件） | 缺 `using Google.Protobuf;`（`ToByteArray` 是扩展方法）。报错原文里就带"是否缺少 using 指令"，**读原文**即可 |
| 3 | **CS0162：无法访问的代码**（我在 Host 里写的"tick 率不一致就警告"） | `SharedInfo.LogicTickRate` 与 `NetContract.TickRate` **都是 const**，编译器在编译期就算出条件恒假 → 那段运行时检查**永远不执行**。这正是"**走不到的分支比没有分支更糟**"：看着很负责，其实是摆设。**改成测试**（`ProtocolTests.TickRate_MatchesSharedLayerLogicRate`），并把它从 Host 里删掉 |

### 8.6 S3 客户端半边（2026-09-23 同日完成）

| 产出 | 说明 |
| --- | --- |
| `Client\Assets\_Project\Game\Net\NetSession.cs` | 会话状态机（未连接 → 连接中 → 握手中 → 在线 / 失败）：发握手、按节奏发心跳、算 RTT、收包分发、**握手超时**与**心跳超时**判定。**不认识 socket**（只认 `ITransport`），**时间由调用方喂**（`Pump(deltaMs)`） |
| `Client\Assets\_Project\Editor\NetDebugWindow.cs` | 编辑器窗口 `Tools/NBC/网络/网络调试窗口`：连接 / 断开 / 发一次心跳 + 状态区（玩家 id、RTT、心跳收发、传输统计）+ 日志区。⚠️ `EditorApplication.update` 的 delta **夹到 250ms** —— 编辑器卡顿会让 delta 变成好几秒，照实喂进去会判出**假掉线** |
| `Tests\EditMode\Game\NetSessionTests.cs` | **17 条**，**全部用 `FakeTransport`**（S2b 那个替身终于派上用处）：握手成功/被拒/超时、心跳节奏（**算出来的**：1+3 次）、Pong→RTT、心跳超时**且真的断开**、有 Pong 就不超时（负向对照）、收包转发、0 长度帧判违规、垃圾字节判违规、重复 Ack 忽略、未上线 Send 抛异常、断开幂等、**失败后重连要清干净** |

**验证（都在同一个线程上、真 socket）**：

| 证据 | 实测输出 |
| --- | --- |
| 探针**会话层模式**对着真服务端进程 | `通过 6，失败 0。结果：✅ 全绿` —— 连上/握手/player_id/服务端版本/RTT/1.2 秒内自动心跳 ≥2 次/全程无失败/主动断开后回 Disconnected |
| 服务端侧日志（同一次） | `[提示] 会话 1 握手成功 → 玩家 1（客户端 v1，tick 30Hz）` |
| 端到端探针（进程内 17 条） | 仍全绿（新增的 `Game\Net` 编译没有影响它） |
| 编译闸门 | **0 错 0 警告**（顺带把编辑器窗口的 Editor API 也过了一遍编译器） |
| EditMode 用例数 | 611 → **628**（+17） |
| asmdef 引用守卫 | 重放 **0 处违规**；顺手补了一行 `NBC.Protocol`（原来表里没有它 —— `using NBC.Protocol;` 的缺失**查不出来**） |

⚠️ **负责人实跑待办**：`Tests_EditMode` 应为 **628 条**；编辑器窗口的"点着连"还需要在 Unity 里点一次
（规则 13：AI 不开 Unity）。窗口与菜单路径已写进 `Docs\教学\M3-A-网络层.md` §⑥。

### 8.7 S4 房间与席位（2026-09-23）

**协议（两处兼容追加，`CONTRACT_VERSION` 保持 1）**

| 追加 | 为什么 |
| --- | --- |
| `LeaveRoomRequest` + `ClientMessage.leave_room = 5` | 不给"离开"一条消息的话，只能靠**断线**离开房间 —— 演示里"换个房"就得重连。加消息是兼容改动（proto3 忽略未知字段） |
| `ErrorResponse{code, message}` + `ServerMessage.error = 6` | 进房失败（满 / 重复 / 房间不存在）如果"什么都不回"，客户端只能显示"点了没反应" —— 这正是本项目的判据"**fail loud，还要说人话**"要挡的东西。错误码常量在 `Shared\Net\NetErrors.cs`（双端共享，和 `NetContract` 同一条判据） |

**分工（写下来免得混）**：`RoomState` = 你的房间**现在长什么样**；`ErrorResponse` = 你刚发的请求**被拒了**。
其中 **"空房号状态"是一条明确约定**：`room_id` 为空 = "你不在任何房间"（用它回答"离开成功"，就不必再为成功造一种消息）。

**服务端**

| 文件 | 职责 | 判据 |
| --- | --- | --- |
| `RoomRegistry.cs`（含 `Room`/`RoomSeat`） | **纯规则**：谁能进、满没满、房主是谁、房空了要删 | ⚠️ 里面出现 `Send` 就是放错了地方 |
| `RoomService.cs` | 接线：注册 `JoinRoom`/`LeaveRoom`、**整份广播**席位表、`SessionClosed` 时**自动退房** | 断线不退房 → 4 人房被 3 个僵尸占住，新的人进不来 |

房主规则（**确定性**，所以能写用例钉住）：第一个进房的人当房主；房主走了由**进房顺序的下一个人**接替。

**验证（数字都是跑出来的）**

| 证据 | 实测 |
| --- | --- |
| 探针【四】房间规则（纯逻辑，不碰网络）**6 条** | 房主是第一个 / 满员拒绝（1001，且不偷偷另开一间）/ 重复进房拒绝（1002）/ 房号不存在拒绝（1003，且房间数仍为 0）/ 留空房号优先同副本 / 房主移交 + 空房删除 + 离开幂等 |
| 探针【五】走网线（真 socket）**8 条** | 两客户端同房各收到 **2 人**席位表（房号一致、容量 4、房主=第一个、名字对得上）/ 满员时第三个收到 **1001 + 人话**且**一份含自己的席位表都没收到** / 主动离开 → 离开者收到"空房号"、留下的人收到 1 人表 / **拔线（不发离开请求）→ 席位自动释放** / 最后一人离开 → 空房被删 |
| 探针总数 | **17 → 31 全绿** |
| 探针"会话层模式"（客户端 `NetSession` 对真服务端进程） | **8/8**：连上 / 握手 / player_id / RTT / 自动心跳 / **进房收到 `r1（副本 1001）1/4 人：探针会话[房主]`** / **离开后"你不在任何房间"** / 主动断开 |
| 服务端日志（同一次） | `[房间] 玩家 1（探针会话）进入房间 r1（副本 1001，1/4 人），广播 1 份席位表`<br>`[房间] 玩家 1（探针会话）离开房间 r1（房间已空，已删除）` |
| 编译闸门 | 0 错 0 警告；`dotnet build Server\NBC.sln -m:1` 0 错（1 个警告来自 `NBC.Server.Lab`，M0 的实验文件，不是本片的） |
| EditMode 用例 | 628 → **635**（+6 房间、+1 协议往返） |
| asmdef 守卫重放 | 0 处违规 |

⚠️ **一条用例被自己的功能改红了（值得记）**：探针原来有一条"**还没实现的消息只记一句、不断开**"，用的是 `JoinRoom`。
S4 把 `JoinRoom` 实现之后，那句"还没实现"自然就不出现了 —— **用例当场变红**。
这不是坏事，是"用例跟着功能走"该有的样子：功能变了，旧断言就得改（已改用 `PlayerInput`，S5 的消息）。

### 8.8 ParrelSync 放哪儿（2026-09-23，负责人手动导入后问的）

**现状（2026-09-23 已落位）**：`Client\Packages\ParrelSync\`（**1.5.3**，`com.veriorpies.parrelsync`，MIT），
`Client\Packages\packages-lock.json` 里已登记为 `"com.veriorpies.parrelsync": { "version": "file:ParrelSync", "source": "embedded" }`。
实测读到两条事实：它有 `package.json`（**它本来就是个 UPM 包**，不是普通资产），
它的 asmdef `projectCloner.asmdef` 是 `includePlatforms: ["Editor"]`（**纯编辑器代码，永远不进包体**）。

**落位时顺带实测到的两件事（都写下来，免得下次重新踩）**：

| # | 实测 | 处理 |
| --- | --- | --- |
| 1 | 克隆目录的真实名字是 **`Client_clone_0`**（不是 `Client_clone`）—— 读源码：`ClonesManager.CloneNameSuffix = "_clone"`，实际路径再拼 `_<序号>`，所以第二个叫 `Client_clone_1` | 我第一版 `.gitignore` 写的 `Client_clone/` **实测没命中**（`git status` 里那行 `?? Client_clone_0/` 就是证据）→ 已改成 `Client_clone*/`、`*_clone*/`、`*_Clone*/`，并重新用 `git check-ignore -v` + `git add --dry-run` 验过 |
| 2 | 克隆目录**不是全量拷贝**：`Assets` 与 `ProjectSettings` 是**符号链接**、`Packages` 是真拷贝（与它那个按 MD5 同步 `Packages` 的 `ValidateCopiedFoldersIntegrity` 对得上）；`Library`/`Temp`/`Logs` 是它自己的 | 所以克隆的代价主要是 `Library` —— 但**照样必须忽略**（GB 级 + 上千个 `.csproj`/`.sln`） |
| 3 | `Client\Assets\Plugins\ParrelSync\ScriptableObjects\ParrelSyncProjectSettings.asset`（450 B）**不是重复的包**，是 ParrelSync **自己生成的项目设置资产** | 留着并**入库**（它的 `ParrelSyncProjectSettings.cs` 读它） |

| 位置 | 结论 | 理由 |
| --- | --- | --- |
| **`Client\Packages\ParrelSync\`**（UPM **嵌入式包**） | ✅ **推荐** | ① 它本来就是包，Unity 按**包**处理：不会被编进 `Assembly-CSharp`、不进我们的 asmdef 守卫、不进构建；② **跟着仓库走** —— 本机 git 直连 github 是被拦的（`Docs\11` §四之一），manifest 里写 git URL 每次解析都要靠代理，离线/换机器就断；③ **与既有先例一致**：A* 就是 `Client\Packages\com.arongranberg.astar\`（`.gitignore` §第三方插件 那段写了理由）；④ ⭐ **它自己会同步 `Packages/`**：`ValidateCopiedFoldersIntegrity`（`[InitializeOnLoad]`）在克隆工程启动时**对 `Packages/` 做 MD5 比对**，不一致就 `FileUtil.ReplaceDirectory` 从原工程拉过来 —— 放 `Packages/` 正好落在这条自动同步的覆盖范围内（实测：该文件只校验 `Packages`，`Assets` 走它自己的 `AssetModBlock`） | 
| `Client\Assets\ParrelSync\`（现状，与 `Behavior Designer` / `XLua` 同级） | ⚠️ 能用，但次选 | 把"包"当"资产"：每个文件一个 `.meta` 进资产库，且克隆工程时会作为**资产**再复制一份。真要用这个位置，就**保持 `Assets` 根目录**、**别挪进 `_Project`**，并把 `.meta` 一起入库 |
| `Client\Assets\_Project\...` | ❌ **不要** | ① `AsmdefReferenceGuardTests` 扫的是 `_Project` 下**所有** asmdef —— 第三方编辑器插件的 asmdef 会被卷进我们的引用规则里；② 编译闸门 glob 的是 `_Project\**`，会把**编辑器插件代码**编进闸门；③ `_Project` 是业务层，第三方工具进去是层级污染 |

**⚠️ 一个会咬人的坑（已提前堵上）**：ParrelSync 的工作方式是**把整个 Unity 工程克隆到同级目录**，
默认形如 `Client_clone\` —— 也就是落在 `E:\...\3D联网战斗Demo\Client_clone\`，**在 git 仓库里面**。
不忽略的话：`git status` 里会冒出成千上万个文件，而且一旦用 `git add -A <目录>` 提交
（我 2026-09-23 正是这么漏掉 `Server` 的，见 `Docs\00` **W13**），一个完整工程副本（含它自己的 `Library\`）
会以 GB 计进仓库。

⇒ 已在根 `.gitignore` 增加 `Client_clone/`、`*_clone/`、`*_Clone/`，并**用地面真值验过**
（`git check-ignore -v` 命中 + `git add --dry-run .` 里不出现该目录），不是"看起来写对了"。

**对 D8 的影响（决策要更新一句）**：D8 原来定的是"**一个 Editor + 一个打包版**"（当时把 ParrelSync 列为"别的选项"）。
现在它装上了，于是"**两个 Editor**"也能跑。建议的用法是：
· **日常联调用 ParrelSync**（S5 的快照对账、S8 的网络模拟器，改一行就能两边都看到，省掉打包时间）；
· **S9 的最终验收仍然要有一个打包版客户端** —— 它才是"真客户端"的证据（IL2CPP、无编辑器、无 Editor 程序集）。

### 8.9 S5 快照下行（2026-09-23）

**服务端**

| 文件 | 职责 |
| --- | --- |
| `NBC.Server.Game\DungeonBattle.cs` | **权威世界**：单位（实例号/配置号/血/毫米位置/朝向/存活）、巡逻推进、`ApplyDamage`（**复用共享层 `DamageMath`**）、`ToSnapshot()`。位置是**整数毫米**、速度是"毫米/帧" —— 和 A7 输入量化、D2 定点数同一条判据（要过网线的东西不许是浮点） |
| `NBC.Server.Game\RoomBattleService.cs` | 每逻辑帧：**收拾**（房没了就回收世界）→ **推进**（`Step`）→ **下发**（`ToSnapshot().ToByteArray()` **只序列化一次**，同一份字节发给房里每个人）。⚠️ 判据：全量 ≠ 每人算一遍 |

⚠️ 与 M0 的 `BattleInstance` 的分工写进了文件头：`DungeonBattle` 是**一个世界**，`BattleInstance` 是**同步策略宿主**（三种模式是 M4 的事）。

**客户端**：`Game\Net\SnapshotView.cs` —— 客户端这边**唯一**的世界状态来源（"客户端没有自己那份血条"）。
它有一条"旧快照直接丢掉"的分支：TCP 下正常走不到，防的是重连/换连接后迟到的旧包（画面倒退）。
⚠️ 按本项目"走不到的分支比没有分支更糟"，它**必须能被测**才算存在 → `SnapshotViewTests.Apply_StaleSnapshotIsIgnored` 就是它的存在理由。

**验证（数字都是跑出来的）**

| 证据 | 实测 |
| --- | --- |
| 探针【六】状态同步 **12 条** | 两客户端最后一张快照**帧号相同、逐字段一致** / 客户端内容 == 服务端 `ToSnapshot()` / 1 英雄 + 2 怪 / **位置随帧变化**（世界真在跑）/ 巡逻确定性（不越界）/ 伤害走 `DamageMath`（`applied=120`、`overkill=9879`、血不为负）/ 打死之后**两边看到的血与存活一致** / **推 60 帧 → 每边收到 60 张**（快照率 = tick 率）/ 帧号单调 / 房空后世界被回收 |
| 探针总数 | **31 → 43 全绿** |
| EditMode 用例 | 635 → **643**（+8 条 `SnapshotViewTests`）；asmdef 守卫重放 0 违规 |
| **负责人实跑（2026-09-23）** | **643 全绿** ✅（第一次跑是 642 通过 + 1 红 —— 那条红是**我算错的期望值**，不是被测代码错，已修并记进教学母题"断言里的数字必须算出来"第 5 次）；窗口「世界」区确认**服务端帧号逐帧推进、两只狼在 1500~2500mm 之间来回巡逻** ✅ |
| 编译 | 服务端解 0 错 0 警告；编译闸门 0 错 0 警告 |

**今天第四次撞上"引用不传递"**（如实记、也再次证明这条值得写进规则）：
`NBC.Server.Game` 生成快照时用到 `ToByteArray()`，报 `CS0246 … "Google"` ——
`NBC.Server.Core` 引了 protobuf **不会传过来**，必须自己引一份。今天这一条已在
Host（CS0012）、探针（CS0246）、Game（CS0246）三处各撞一次。

⚠️ 另记一个**构建失败的假象**：`dotnet build` 报 `MSB3027/MSB3021 文件被锁` ——
根因是**上一次跑的 `NBC.Server.Host` 进程还开着**（负责人测试房间时留下的），
不是代码错。重启服务端的命令：`Server\NBC.Server.Host\bin\Debug\net8.0\NBC.Server.Host.exe --port=7777`

### 8.10 S5b 输入上行（2026-09-23）

**协议**：只改了 `PlayerInput.action_bits` 的**注释**（第 0 位 = 技能1/普攻，与 A7 的
`InputActionId.Declare(0, "Skill1")` 对齐）→ **`CONTRACT_VERSION` 不动**（注释不是协议变更）。

**服务端**（`RoomBattleService` + `DungeonBattle`）

| 规则 | 内容 | 不这么做会怎样 |
| --- | --- | --- |
| **移动是状态** | 每个玩家只留**最新一条**输入，每 tick 应用一次 | 客户端发 60 次/秒 就比发 30 次/秒 走得快 —— "速度取决于网速"这种经典 bug |
| **动作是事件** | `action_bits` 收到**立刻判定**（射程/冷却/目标活着） | 把动作也当状态 → "按住攻击每秒打 30 下"（冷却挡住了，但语义是错的） |
| **输入保鲜期 6 帧** | 超过 200ms 没收到新输入 → 当**没有输入** | "客户端发一次往前然后崩了"→ 英雄**永远往前走** |
| **权威校验** | `player_id` 必须是这个会话的玩家，否则回 `ErrorResponse{1004}` | 客户端能驱动**别人的**英雄（作弊/串台） |
| **轴钳位** | 客户端给的轴一律钳到 -1000..1000 | 不信任客户端的第二道门（第一道是 protobuf 的 int32） |
| **一个席位一个英雄** | `ReconcileHeroes` 每帧对齐：缺的补（出生点按席位序号）、走的删 | 2 人房里多一个没人控制的幽灵英雄；离房的人留在场上当靶子 |

**两种"拒绝"分开处理**（S5b 定的判据，值得记）：
· **权威被违反**（冒名 `player_id`）→ 回 `ErrorResponse`（响亮、可测）
· **由世界状态决定的拒绝**（射程外/冷却中/目标死了）→ **只记日志**。理由：客户端自己那份世界
  （`SnapshotView`）看得到这些，回错误会在"按住攻击键"时变成**每帧一条错误** —— 那不是响亮，是刷屏。

**客户端**：`NetSession.SendInput(InputCommand)` / `SendInput(轴, 轴, 位, 位, 目标)`；
`client_tick` 在 M3 填的是"第几条输入"（**没有客户端逻辑帧**，见 `NextInputSeq` 注释；M4 做预测时才变成真帧号）。
编辑器窗口加了「输入」区：两个轴滑条 + 持续发送 + 目标编号 + 「普攻目标」按钮（能自动选一只活怪）。

**验证（数字都是跑出来的）**

| 证据 | 实测 |
| --- | --- |
| 探针【七】输入上行 **14 条** | 输入只动自己的英雄（10 帧 = 1500mm，A 到 500、B 原地不动）/ **一帧收 10 条输入只走一格**（限速）/ 停发后最多滑 6 帧然后彻底停（**保鲜期契约**）/ 冒名输入 → `Error{1004}` 且谁都不动 / 走近后普攻打中掉 30 血 / **冷却中再按不掉血** / 冷却走完又能打中 / 射程外不掉血且**不回错误**（拒 1 次）/ **一个席位一个英雄**（2 人 2 英雄、出生点不同）/ 离房后英雄被移出世界 |
| 探针总数 | **43 → 57 全绿** |
| EditMode | 643 → **647**（+4 条 `NetSessionTests`：字段映射 / `InputCommand` 转换 / `client_tick` 递增 / 未上线抛异常） |
| 编译 | 服务端解 0 错 0 警告；编译闸门 0 错 0 警告 |

**这一版红过 9 条，两个根因都值得记**（都不是被测代码错，而是**探针自己**错）：

| # | 我写错了什么 | 教训 |
| --- | --- | --- |
| 1 | 探针夹具**忘了 `Battles.RegisterHandlers(router)`**（Host 里注册了、夹具里没注册） | **输入用例全部静默失效**（服务端根本没处理 Input）→ 9 条一起红。判据：**"处理器注册"这种接线，凡是第二处调用点都要有**；漏了不会报错，只会"输入像丢了一样" |
| 2 | `TickBattles` 写成了"**先 Tick 再收包**"，而 Host 主循环是"先收包派发、再推进逻辑" | 于是"这一帧发的输入"要等下一帧才处理（离房的人英雄没被移出、输入像丢了）。**探针的推帧顺序必须和主循环一致，否则验的不是生产里跑的那个东西** |
| 3 | 断言写错两条：多推了一帧却仍期望 10 步（实际 11 步）；把保鲜期当成"停发立刻不动" | 前者是算术；后者是我对自己设计的语义断言错了 —— **保鲜期是有意的**（容忍抖动），正确契约是"最多滑 6 帧然后必须停" |
| 4 | 一条用例**退役**：`还没实现的消息只记一句` 用 `PlayerInput`，而 S5b 把 Input 实现掉了 | 与 S4 时同一件事（那次换的是 `JoinRoom`）。至此**五种 `ClientMessage` 全部有处理器**，"还没实现"没有对象可测了 ⇒ 用例退役（函数体留作样板，S6 加新消息时补），服务端那条 `Warn` 分支保留 |

### 8.11 S6a 副本与数值来自配置表（2026-09-23）

**加表实测**：`ConfigKit.Cli --source Configs\Design --out Client\Assets\_Project\Game\Config\Generated`
→ **0 错 0 警告、写出 32 个文件、退出码 0**，`Dungeon` 的四个产物（`Config_Dungeon.cs` / `DungeonConfig.cs` /
`Dungeon.tsv` / `Dungeon.json`）都出来了，其中 `public int[] monsters;`（`ref:Monster[]`）与 `public int bossId;`
是**照我在 CSV 里写的类型生成的** ⇒ **"加表不改工具代码"这条成立**（`Docs\17` 的类型系统直接支持数组外键）。

**新表** `Configs\Design\Dungeon.csv`（列：`id / name / maxPlayers / spawnRadiusMm / monsters / bossId`）；
普通怪**写重复就是刷多只**（`"6001,6001"` = 两只狼）—— 这条来自 `Docs\17` §五"数组允许重复"。

**服务端怎么读表**（这一步有个必须写下来的判断）：

| 事实（实测） | 结论 |
| --- | --- |
| `Config_<表>.cs`（生成物）**只有数据字段**，是纯 C# | 可以用，但它没有解析逻辑 |
| **解析逻辑在被生成到 Unity 侧**的 `<表>Config.cs` 里（`using UnityEngine`，SO 容器 + `LoadFromTsv`） | 服务端**没法直接复用** |
| 三条路：① 让 ConfigKit 生成纯 C# 解析器（**要改工具**）② 读 JSON（得手写 JSON 解析）③ **读源 CSV** | **选 ③**：源表才是**唯一真源**，生成物只是它的投影；格式由 `Docs\17` §三/§四**冻结** |

⚠️ **如实记代价**：这是**第二份读表实现**（Unity 侧的 SO 导入器是第一份）。
缓解办法不是"祈祷两份一致"，而是**探针读真表**：`NetProbe` 会断言"副本 1001 里就是 2 只狼 + 1 只 BOSS，
血量分别是 300/300/2000、BOSS 在对面"—— 格式一旦漂移，探针当场红。

⚠️ **部署期提醒**（写在 `ServerTables` 文件头）：现在靠"从 exe 所在目录往上找 `Configs\Design`"，
是**开发期**的做法；M6 应当把表随进程发出去。

**顺带修掉一个真隐患**：`Hero.csv` 说剑士 **hp 1200 / 移速 5000 毫米每秒**，
而服务端此前**写死** 300 / 150 毫米每帧 —— **同一个东西两套数**。现在全部来自表：
血量 1200、每帧 `5000 ÷ 30 = 166` 毫米、普攻伤害来自 `Skill` 表（英雄第一个技能 = 100 点）。

**验证**

| 证据 | 实测 |
| --- | --- |
| 探针【八】配置表 **7 条** | 四张表都从源 CSV 读到了 / 副本 1001 的怪 = 表里写的（2 普通怪 + 1 BOSS）/ 每只怪血量来自 `Monster.hp`（含 BOSS 2000）/ 出生点用 `Dungeon.spawnRadiusMm`（BOSS 在对面）/ **副本 9999 不在表里 → 开不了世界且说明原因**（负向对照）/ 英雄血量移速来自 `Hero` 表 / 普攻伤害来自 `Skill` 表 |
| 探针总数 | **57 → 64 全绿** |
| 探针里所有期望值 | 全部**从表里算**（`Hero.moveSpeed ÷ 30`、`Skill.damage`、`Dungeon.monsters.Length + 1`）—— 不再手抄数字，见下方"这一版红过 1 条" |
| 服务端解 / Host 编译 | 0 错 0 警告（Host 用 `-t:Compile` 单独验，避开"服务端正开着"的文件锁） |

**这一版红过 1 条**（又是"期望值手抄"）：`输入上行：A 一直往右按` 的断言写死了 `10 帧 × 150mm = 1500mm`，
而移速已经改成从表来（166）→ 实际走了 1660mm。**这正是本项目"断言里的数字必须算出来"的第 6 次**
（前 5 次：E1 ×3、M2-B、M3-S5）。处置同上：把期望值改成**由同一个约定算出来**
（`Hero.moveSpeed ÷ TickRate`），而不是再抄一个 660。

⚠️ **顺带记一个反复出现的环境坑**：`dotnet build Server\NBC.sln` 又撞 `MSB3027/MSB3021 文件被锁` ——
根因还是**负责人正开着 `NBC.Server.Host`**（这次第三次）。
**处置办法（已定型）**：我平时只构建 `Server\_net-probe\NetProbe.csproj`（它编译 Core+Game，**不碰 Host 的 bin**），
Host 单独用 `dotnet build ...\NBC.Server.Host.csproj -m:1 -t:Compile` 做编译检查 —— 两者都不需要你关服务端。

### 8.12 S6b BOSS AI（2026-09-23）

**做法**：`BossBrain` 用 **A10 的 `StateMachine<EBossState>`**（同一份源码由 `NBC.Server.Game.csproj`
`<Compile Include>` 编进服务端），三个状态：**Idle（没目标）/ Chase（超出射程就追）/ Attack（够得着就打）**。
状态里**只做判定与调用**，世界改动一律回到 `DungeonBattle`（规则一处实现）。
时间用**逻辑帧号**（`_fsm.Tick(frame)`，A10 的硬要求，不许用 `deltaTime`）→ AI 行为可复现（M4 帧同步的前提）。

**⚠️ 一处必须更正的判断（"纯 C#"要看整条依赖链）**

`Docs\25` §二 原来写的是"A10 的状态机是纯 C# → 服务端跑 BOSS AI"。**真去编的时候发现只对了一半**：

```
StateBase.OnEvent(EventId id)  →  EventId.cs  →  using UnityEngine;
                                                  ↑ 唯一用法：Debug.LogWarning("[EventId] 建议事件名带模块前缀…")
```

= **A10 状态机通过 `EventId` 间接依赖 UnityEngine**。这是 **D14 结论的第二层**：
D14 证明的是"**依赖引擎对象**的东西一碰就 `ECall`"；这里证明的是"**看似纯 C# 的东西，
也可能通过一句日志把引擎拖进来**"。
⇒ **判据升级：判"能不能进服务端"要看整条依赖链，不是看那个文件自己的 `using`。**

**处置**（两条都记着）：① 加 `UnityEngineDebugShim.cs`（**10 行、只垫 `Debug.LogWarning`**，
红线与"更干净的长期解"写在那个文件头里）；② 那 5 个 FSM 文件 + `EventId.cs`
按 `Shared\` 的老规矩加 `#nullable disable`（服务端开了可空、Unity 没开，否则 38 条 nullable 噪音）。
⚠️ **没有**用 `NoWarn` 一刀切 —— 那会把本工程自己的真实可空缺陷也屏蔽掉（csproj 里写了理由）。

**顺手修掉一个隐藏 bug**：`TryBasicAttack` 原来用的是 `BasicAttackDamage`（**英雄**的伤害，来自 `Skill` 表）——
BOSS 打人也会用英雄的伤害值。现在改成**每单位自己的** `AttackDamage`（英雄来自 `Skill` 表，怪来自 `Monster.attack`）、
`MoveSpeedMmPerTick`（英雄来自 `Hero` 表）。

**验证**

| 证据 | 实测 |
| --- | --- |
| 探针【九】BOSS AI **6 条** | 英雄跑远 → BOSS **追上来（距离变小）** / 追击时状态是「追击」或「攻击」/ 站射程里**会掉血且掉的是 `Monster.attack` 的整数倍** / **冷却生效**（1 秒最多挨 3 下）/ 英雄死了 → 回「站桩」**且不再移动** / 大脑确实挂在状态机上（有状态名、发生过切换） |
| 探针总数 | **64 → 70 全绿** |
| 编译 | 服务端 0 错 **0 警告**（含那 6 个双端文件） |

**我这一轮犯的两个错（如实记）**
① 探针的"**通过 N，失败 M**"那一行被我**自己替换锚点时误删了**（S6a 那次），
   而我没注意 —— 直到搜"通过 "搜不到才发现。**改替换锚点时必须回读输出**（同 W13 的精神）。
② BOSS/普通怪的**移速**暂时用常量（`Monster` 表还没有 `moveSpeed` 列）—— 已写成 `TODO(S6+)` +
   常量注释，**不许变成"没人知道的魔法数"**。

**行为树的落点（顺带定下来，免得面试答不出）**

| 线 | 用什么 | 依据 |
| --- | --- | --- |
| **权威 AI**（决定血量/位置/掉落的） | **纯 C# 状态机（A10）**，永远 | D14 实测：行为树/NavMesh 依赖引擎对象，**进不了服务端**；`Docs\10-M0` 裁决 A1~A3"AI 分两套实现" |
| **表现/客户端 AI**（巡逻表演、BOSS 演出、阶段切换的表现编排） | **Behavior Designer**（已购插件） | 需求 Q3/Q11 要求"投入使用"；它只能在客户端跑，正好落在表现层 |

⇒ 所以行为树**不是"用在哪"的问题，而是"只能用在客户端表现层"**；M3 的验收（打死 BOSS）用不到它，
**落点定在 M5 的表现/演出**（到那时再真接一次，并补一条"客户端表现 AI"的留档）。

### 8.13 S7 掉落一致（2026-09-23）

**加表**：`Configs\Design\DropTable.csv`（`id / monsterId / itemId / countMin / countMax / chancePerTenThousand`），
概率用**万分比**（`Docs\17` §4.1 的约定：`5000` = 50%）；BOSS 两条写 **10000 = 必掉**（方便验收与对账）。
ConfigKit 生成 **0 错 0 警告 / 36 文件 / 退出码 0**。⚠️ M3 **还没有 `Item` 表**（背包/物品是 M5+），
所以 `itemId` 先用裸编号，表注释里写明了。

**服务端三个决定**

| 决定 | 内容 | 不这么做会怎样 |
| --- | --- | --- |
| **自己写 PRNG**（`BattleRandom`，32 位 xorshift） | **不用 `System.Random`** | 它的算法**在 .NET 版本间变过**（.NET 6 换过实现），Mono/IL2CPP 又是另一套 → 同种子给不出同一串数 → 掉落在单端测试全绿、联机时"两边掉的不一样"（静默失败） |
| **只有"从活着打到死"那一下掷** | `ApplyDamage` 里判 `wasAlive && !Alive` | 打尸体重复掉（M2 已经在共享层钉过"打 0 血不判致死"，这里是同一条思路） |
| **序列化一次、发全房** | 与快照同一条规矩 | "两边收到同一份掉落"靠的是**同一份字节**，不是"两边各掷一次、结果正好一样" |

**`winner_player_id`**：M3 的规则是"**击杀者获得**"（协议注释里就是这么写的）→ 从攻击者单位取 `PlayerId`。

**验证（探针【十】4 条）**

| 用例 | 实测 |
| --- | --- |
| BOSS 必掉 | 表里 2 条（都是 10000）→ 实掉 2 条，且**数量都落在表里区间内** |
| 两端同一份 | 甲、乙收到的掉落**逐字段一致**（itemId / count / winnerPlayerId） |
| 归属 | `winner_player_id` = 击杀者的 player id |
| **确定性** | 同一房号 + 同样击序列 → **同样的掉落**；顺带验了 PRNG"同种子 → 同序列" |

**我这一轮又踩了同一个坑（第 2 次，值得单列）**：用脚本替换代码锚点时**没检查返回值**，
于是【十】的调用**没插进去**、探针还是 70 条 —— 而输出看着"全绿"，差点就漏过去了。
⇒ 判据再收紧：**脚本替换必须检查命中数**（本轮前面几次我是检查了的，这两次偷懒）。
这和 `Docs\00` W13（`git add` 白名单静默漏文件）、以及"改替换锚点要回读输出"是同一族：
**"看起来成功"从来不等于"真的成功"**。
### 8.14 S8 网络模拟器（NET-07，2026-09-23）

**它是什么**：`Client\Assets\_Project\Framework.Net\Sim\SimulatedTransport.cs` ——
**它本身也是一个 `ITransport`**，所以能插在任意实现外面（这就是当初留接缝的回报）：

```
真实： NetSession → SimulatedTransport → TcpTransport → 网线
测试： NetSession → SimulatedTransport → FakeTransport（纯内存、时间精确可控）
```

**一个接口上的取舍**：`ITransport` **故意没有时间参数**（那样两端同源、可复现），
而延迟必须有时间轴 ⇒ 模拟器**多加一个入口** `Advance(deltaMs)`，用法是
**先 `Advance` 再 `Pump`**（顺序反了，"这一帧该到的包"要等下一帧）。**没有污染接缝。**

**⚠️ "丢包"在 M3 的真实含义（这条必须写清，否则会误用）**：
M3 走 **TCP**，可靠性由 TCP 保证 ⇒ **应用层丢一条就是"这条消息永远不到"**（连握手都会失败）。
所以：**延迟/抖动**可以直接用来验"抗抖动"（探针里就是这么用的）；
**丢包**的正确用途是 **M4 的帧同步（UDP 式）**与客户端容错 —— M3 只在 EditMode 用例里验"模拟器真的在丢"。

**随机数**：同样**不用 `System.Random`**（算法在 .NET 版本间变过 → 测试会偶尔红），
用 xorshift；但**故意不共用 `BattleRandom`**：那份是**战斗规则**（双端要对账），
这份只是"本端什么时候丢一个包"（只影响本端）—— 两者会朝不同方向演化，硬合并会让"改模拟器"顺手改到"战斗随机"。

**验证**

| 证据 | 实测 |
| --- | --- |
| EditMode `SimulatedTransportTests` **8 条** | 不模拟时**完全透明**；固定延迟**两个方向都延迟**（99ms 不到、100ms 到）；抖动会产生延迟/乱序；**全丢**（万分比 10000）一条都不到且丢包计数 = 上行+下行；**部分丢包同种子同结果**（把随机钉成确定序列，不 flaky）；断开**不延迟**透传；状态/描述透传；关闭会清掉"路上"的包 |
| 探针【十一】**3 条**（真 socket + 真服务端） | **注入 200ms 延迟后仍能完成握手**；**测出的 RTT ≥ 200ms**（延迟真的生效了，不是"设了但没用上"）；延迟下**进房 + 收快照照样工作** |
| 探针总数 | **74 → 77 全绿**；EditMode **647 → 655** |

**我这一轮又栽在两个"低级但真实"的地方**（如实记）：
① 用例里写中文串时**又用了半角引号**（`（不是"连上就完事"）`）→ 编译错误。
   这是同一个错误的**第 3 次**（前两次在服务端）⇒ 规矩收紧：**中文串里的引用一律用「」**。
② 探针里用 `SimulatedTransport` 但**忘了补 `using NBC.Framework.Net.Sim;`** —— 编译器当场点名（CS0246），
   一改就好。这也说明"**读报错原文**"这条判据一直有效。
### 8.5 快照率与速率常量（S5 开工第一件事，已定案）

**原问题**：`SharedInfo.SnapshotSendRate = 20`（共享层写着"快照 20Hz"）与 D5 的"**每 tick 全量快照**"（= 30Hz）矛盾。

**定案：M3 的快照 = 每 tick 一张（30Hz），并删掉 `SnapshotSendRate`。**理由三条：

| # | 理由 |
| --- | --- |
| 1 | M3 **不做插值、也不做预测**（D4）→ 发得越勤画面越顺。20Hz 只会让动作更顿，没有任何好处 |
| 2 | 代价可以忽略：2~4 人、十几个单位，一张快照几百字节 → 每秒十几 KB |
| 3 | 那个 `SnapshotSendRate` **一行代码都没用到** —— 它唯一的作用是让启动日志显示一个**错数字**（这就是"两份数据一定会漂移"的最好例子） |

**顺带把根挖了**（同一类隐患一次清掉，不止这一处）：

| 改前 | 改后 | 为什么 |
| --- | --- | --- |
| `ContractVersion = 1`、`LogicTickRate = 30`、`SnapshotSendRate = 20` **各写一个数** | `ContractVersion = NetContract.Version`、`LogicTickRate = NetContract.TickRate`（**派生**），`SnapshotSendRate` 删除 | 从"两处要对齐"变成"**只有一处**" —— 漂移在编译期就不可能发生 |
| `SharedInfo.Describe()` 里印着 `Snapshot=20Hz` | 只印 `LogicTick=30Hz` | 日志里不该出现第二个、还没被兑现的速率 |
| 测试 `TickRate_MatchesSharedLayerLogicRate`（断言两个常量相等） | **删掉**，换成 `SharedInfoDescribe_MentionsOnlyOneRate` | 派生之后原断言**恒真** = 死检查（"走不到的分支比没有分支更糟"的测试版）；新测试防的是"有人再加一个速率" |
| `SkeletonSelfCheck` 断言字面量 `30 / 20` | 断言 `SharedInfo.LogicTickRate == NetContract.TickRate` | 断言真正的不变量，而不是两个魔数 |

### 8.5.1 S5 两侧都完成了（2026-09-23 同日收关）

| 半 | 内容 | 状态 |
| --- | --- | --- |
| **下行（快照）** | 服务端权威世界 → 每 tick 全量快照 → 客户端 `SnapshotView` | ✅ 完成（证据见 §8.9） |
| **上行（输入）** | 客户端发 `PlayerInput`（只发**意图**）→ 服务端驱动英雄移动/普攻 | ✅ 完成（证据见 §8.10） |
