# 教学留档索引

> 本目录按 `Docs\00-项目工作规则.md` **§15.3.1「留档」**要求建立：
> **每讲完一个模块，就把它落成这里的一篇独立 md**，方便随时复习。
>
> ⚠️ **两条边界**（写在最前面，免得走偏）：
> 1. **留档是"讲"的补充，不是替代。** 只读文档不等于上过课 —— 规则里"把教学写进 `Docs\` 当附录就当讲过了"这条禁止**依然有效**。
> 2. **正文照抄讲课原话，不做二次精炼。** 精炼过的版本又会退化成"看不懂的文档"（这正是 §15.3.1 诞生的起因）。

## 课程清单

| 模块 | 篇目 | 讲 / 留档日期 | 关键一句话 |
| --- | --- | --- | --- |
| **A10** | [`A10-状态机.md`](A10-状态机.md) | 2026-09-21 | **把"现在处于什么状态"变成显式的值，把"能不能换"变成集中检查的规则**；纯 C#、帧驱动、分层约束 |
| **A9** | [`A9-UI框架.md`](A9-UI框架.md) | 2026-09-21 | 前两个毛病最要命，因为它们**出错的时候不报错** —— 目标不是"检测到错误"，而是**让错误自己说出来** |
| **A8** | [`A8-音频管理.md`](A8-音频管理.md) | 2026-09-21 | 音频要解决的不是"怎么播声音"，而是**播几百次不出事、切场景不崩、音量能分开调** |
| **A7** | [`A7-输入管理.md`](A7-输入管理.md) | 2026-09-20 | **怎么把键盘，变成一个可传输、可重放、可测试的值** —— `int` 能过网线，`KeyCode` 不能 |
| A1 | ⏳ 待补 | — | 单例与注册表 |
| A2 | ⏳ 待补 | — | 对象池：注入工厂、无中心管理器 |
| A3 | ⏳ 待补 | — | 事件中心：`readonly struct EventId` + 同步派发 + 写时复制快照 |
| A4 | ⏳ 待补 | — | `MonoManager`：框架自己的"驱动器" |
| A5 | ⏳ 待补 | — | `SceneLoader`：Tick 驱动 + 超时可测 |
| B2 适配层 / B3 | ⏳ 可补 | — | 三段式初始化（排错过程中已随讲） |

> **"待补"是什么意思**：这些模块在收关时**已经口头讲过**（当时是边做边讲，没有留档要求），
> 但没留下成篇的文字。你要是想复习，说一声我就补写 —— 顺手也能发现自己当时哪块没讲透。

## 每篇的标准结构（七段式）

按 `Docs\00` §15.3.1：

1. **① 问题是什么** —— 不用代码，讲清这东西解决什么问题
2. **② 最小例子** —— 两小段：错的写法 + 对的写法，一眼看出差别
3. **③ 原版错在哪** —— 配**原代码**，并说清"每条错误会导致什么现象"
4. **④ 改造后的做法** —— 分层 / 关键决定，每条都带"不这么做会怎样"
5. **⑤ 自测 5 问** —— 带折叠答案，先自己答
6. **⑥ 30 秒验证** —— 用户自己能跑的验证步骤（含**负向对照**，证明测试真的在测东西）
7. **⑦ 一分钟面试版** —— 可以直接背出口的那段话

## 怎么用这些文档

| 场景 | 建议 |
| --- | --- |
| **复习** | 先只看 ① 和 ⑦，能复述就说明记住了框架；细节翻 ③④ |
| **自检** | 做 ⑤ 的 5 道题，**不看答案**。答不上来的那题，对应的 ④ 那段就是你的知识缺口 |
| **面试前** | 直接读 ⑦。要更细就往上翻 ③（"我改掉了什么"是面试官最爱追问的部分） |
| **验证环境还在不在** | 跑 ⑥，应该得到文中写的那个数字 |
