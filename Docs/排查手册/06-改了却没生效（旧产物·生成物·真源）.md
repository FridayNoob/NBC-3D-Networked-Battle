# 06 · 改了却没生效（旧产物 / 生成物 / 真源）

> 定位日期：2026-09-26（M3-S9 收尾）
> 现象一句话：**代码改了、提交了、也重启了，新加的那行日志就是不打印 —— 而且不报任何错。**

---

## ① 现象（负责人原话）

> 「重启服务端后，没有打印：`掉落：怪 6001 → 物品 9001×1~2@10000、9002×1~1@10000`。」

当时的处境很容易让人往错的方向查：

- 代码里**确实有**这段打印（`Server\NBC.Server.Host\Program.cs`，`foreach (int monsterId in tables.MonsterIdsWithDrops)`），而且**已经提交**（`95ddc05`）；
- `Configs\Design\DropTable.csv` 里 6001 的两行**确实**是 `10000/10000`；
- 「重启服务端」这个动作也**确实做了**；
- 但输出里连**一行红字都没有** —— 没有异常、没有警告、没有"跳过"。

> 📌 **"改了却没生效"这一类 bug 的第一特征就是：没有报错。** 所以它必须靠"证据"定位，不能靠读代码。

---

## ② 先确认是哪一层

拿第二节的分层排查图对照，这个现象**不是**③托管运行时、也不是④引擎运行时 —— **逻辑代码压根没被执行**。

正确的第一问**在分层图之前**：

> **第 0 问：我现在跑的 / 看的，是最新那一份吗？**

这一问能解释掉绝大多数"改了却没生效"。本项目到 2026-09-26 已经撞过**三次**，每次的"那一份"都不一样：

| 你改的地方 | 运行时**真正读/执行**的那一份 | 一眼看出来的判据 |
| --- | --- | --- |
| 服务端源码 `Server\**\*.cs` | `Server\NBC.Server.Host\bin\Debug\net8.0\NBC.Server.Host.dll`（**编译产物**） | 启动横幅的 `Built :`（本次新加） |
| Unity 侧配置资产 `DropTableConfig.asset` | 服务端读的是 `Configs\Design\DropTable.csv`（**真源**） | 启动逐条念「掉落：怪 … → …」 |
| `Configs\Design\*.csv` | Unity **运行**读的是**生成物**（`Config_*.cs` / `.tsv` / `.asset`） | 跑 ConfigKit → Unity 导入菜单 → 资产过期检查菜单 |

> ⚠️ 三行的共同点：**改的和读的不是同一个文件**，而**没有任何东西会提醒你**。

---

## ③ 定位步骤（照着做，每步都能得出"是/不是"）

### 第 1 步：比修改时间（最快的铁证）

```powershell
Get-ChildItem -Recurse -Filter "NBC.Server.Host.exe" Server | Select-Object FullName, LastWriteTime
Get-ChildItem Server\NBC.Server.Host\Program.cs, Configs\Design\DropTable.csv | Select-Object FullName, LastWriteTime
```

本次实测：

| 文件 | 时间 |
| --- | --- |
| `bin\Debug\net8.0\NBC.Server.Host.dll` | **2026-09-26 08:56:12** |
| `Server\NBC.Server.Host\Program.cs`（源码） | **2026-09-26 10:35:31** |
| `Configs\Design\DropTable.csv` | 2026-09-26 10:35:31 |

⇒ **产物比源码早了 1 小时 39 分** → 结论：**跑的是旧 exe**。到此已经可以收工，不需要再看任何代码。

### 第 2 步：直接跑一次产物，看它到底会打什么

```powershell
& "Server\NBC.Server.Host\bin\Debug\net8.0\NBC.Server.Host.exe" --port=7801 --ticks=1
```

`--ticks=1` 是特意留的"跑一帧就退出"，**不用起真服务端、不占端口、不改状态**，最适合做这种取证。

旧产物输出里**连 `[Check] 配置表：…` 这一行都没有** —— 说明那份构建比"打印配置表"这个改动还早。
（⇒ 反过来推：负责人看到的"其他日志都有"，不代表"这份是最新的"。）

### 第 3 步：重新编译，再跑一遍（阳性对照）

```powershell
dotnet build "Server\NBC.Server.Host\NBC.Server.Host.csproj" -m:1
```

结果（实测）：`0 个警告 0 个错误`，再跑 `--ticks=1` → 该有的三行都出来了：

```
  Built   : 2026-09-26 10:52:35
[Check] 配置表：副本 2、怪 3、英雄 3、技能 3、掉落 5（来自 …\Configs\Design）
        掉落：怪 6001 → 物品 9001×1~2@10000、物品 9002×1~1@10000
        掉落：怪 6002 → 物品 9001×1~3@7000
        掉落：怪 6003 → 物品 9003×1~1@10000、物品 9001×2~5@10000
```

**阳性对照成立**：同一份 CSV、同一份源码，只把产物换成新的，行为就从"没有"变成"有" ⇒ 变量锁定为**产物新旧**。

---

## ④ 根因（为什么会长这样）

**是我（AI）留下的坑，如实记：**

1. 前一天我改完 `Program.cs`（加掉落自述）时，负责人的服务端**正开着** → 编译到 `bin` 会撞 `MSB3027/MSB3021（文件被占用）`；
2. 为了不打扰他，我当时的验证只做了 **`-t:Compile`（只编译、不写输出目录）** —— 它**只证明"能编过"，不产生任何新产物**；
3. 于是 `bin` 里留着的还是几小时前的那份；
4. 负责人「重启服务端」时，重启的是**同一个旧 exe** —— 动作没错，**对象错了**。

> 📌 **一句话讲清根因：加了代码 ≠ 生效；编译器把输出写过去才算生效。**
> `-t:Compile` 是**语法体检**，永远不能当成"部署完成"。

---

## ⑤ 解决方案

给别人（或给未来的自己）的指令里，**"重启"两个字必须带上"先编译"**：

```powershell
# 1) 先关掉正在跑的服务端（否则 bin 被占用 → MSB3027）
# 2) 编译（把新代码写进 bin）
dotnet build Server\NBC.Server.Host\NBC.Server.Host.csproj -m:1
# 3) 再启动
Server\NBC.Server.Host\bin\Debug\net8.0\NBC.Server.Host.exe --port=7777
```

> 💡 `dotnet run --project ... --no-build` 里的 `--no-build` 同样是这个坑的表亲：**它明确表示"别编译，跑现成的"**。

> ⚠️ **本项目的实际启动方式是「双击 `bin\Debug\net8.0\NBC.Server.Host.exe」** ——
> 双击**永远不会编译**。⇒ 「改完服务端代码」这一步必须有人**真的编一次**：
> 要么我编完再告诉你，要么你手跑上面第 2) 条。**"重启服务端"这个动作本身不含编译。**

---

## ⑥ 防复发（已经落成代码，不是"下次注意"）

### 1. 被动：启动横幅打印 `Built :`（上一次编译的时间）

```
  Built   : 2026-09-26 10:56:27
```

拿它和"我刚才改代码的时间"一比即可判定。**判据是四个 dll 里最新的那个时间戳**，不是本程序集自己的
—— 因为只改 `NBC.Server.Game` 时，MSBuild **只重编 Game.dll，`NBC.Server.Host.dll` 可以一动不动**
（实测：10:56:03 改源码、编完后 Host.dll 仍是 10:55:56）。

> ⚠️ **同一事实只能有一处判据**：横幅与下面的警告共用 `LastBuildTime()`，
> 免得出现"横幅说新、警告说旧"。

（⚠️ 不能改用程序集里的 `Version`：它常年是 `0.1.0.0`，**不会因为改了代码而变** —— 这正是它当年没拦住这个 bug 的原因。）

### 2. 主动：源码比产物新 → **大声报警**（因为"双击"这个用法永远不会编译）

```
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
[警告] 你双击的是**旧的 exe**：源码比产物新，改的代码没有生效！
        最新源码：…\Server\NBC.Server.Game\DungeonBattle.cs
                  2026-09-26 10:56:03
        产物时间：2026-09-26 10:56:23
        先编译再启动：dotnet build Server\NBC.Server.Host\NBC.Server.Host.csproj -m:1
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
```

**边界（每一条都是实测出来的，不是想当然）：**

| 规则 | 为什么 |
| --- | --- |
| 只比 `Server\**\*.cs`，**不比 `Configs\Design\*.csv`** | 表是**运行时读**的，表比产物新是**正常**的 → 比了就天天误报 |
| 只扫 Host 真正依赖的**四个工程**，排除 `_net-probe`/`_condition-probe`/`NBC.Server.Lab` | 这些目录"随手改、编了也不进 bin" → 比了就误报 |
| 产物时间取**最新**，不取最旧 | 没改动的工程 MSBuild **跳过编译与拷贝**，其 dll 停在很久以前（实测 `NBC.Shared.dll` 一直停在 10:55:38）→ 取最旧会**每次改代码都误报**；**一个天天误报的警告等于没有警告** |
| 旁边没有源码（部署环境）**静默跳过** | 那是正常情况，不该在客户机器上喊"你编译一下" |
| 整个检查包在 `try/catch` 里，**取不到时间就跳过** | **诊断信息绝不该把服务端拦在门外** |

**实测四个对照**（不是"应该没问题"）：

| 场景 | 期望 | 实测 |
| --- | --- | --- |
| 刚编译完就跑 | 无警告 | ✅ `Built : 10:56:23`，无警告 |
| 改完源码没编译 | **报警** | ✅ 报警，且"产物时间"与横幅**同一个数**（10:56:23） |
| 报警后再编译一次 | 立刻恢复 | ✅ 无警告，`Built : 10:56:27` |
| 把 `bin` 整个拷到别处（模拟部署，无源码） | 静默跳过 | ✅ 未误报 |

### 3. 启动时自述它**真正读到的配置**

掉落逐条念出来（上一条 bug 已经加过，本次保留）。它的价值在这次事件里再次体现：
**旧产物连念都不念**，直接暴露"这份根本没有这段代码"。

### 4. 顺手删掉一个走不到的分支

我原本还想加"掉落表 0 行就警告"，做**阴性对照**时发现走不到 ——
把 `DropTable.csv` 砍成只剩表头（4 行），加载器直接：

```
[致命] 读配置表出错：DropTable.csv 少于 5 行（规范要求：表头 4 行 + 至少 1 行数据）
```

退出码 `3`（**根本不启动**）⇒ "0 行"在启动前就被挡住了，兜底分支是**死代码**（看着像有保护）。
已删，并把结论写成注释留在原地。事后校验：文件已还原，行数 = **9**（原样）。

---

## ⑦ 一句话讲清（面试版）

> 「改了却没生效」这类问题的特征是**全程没有任何报错**，所以不能读代码、只能拿证据：
> 我第一步是**比文件时间**（产物 08:56 vs 源码 10:35，早了 1 小时 39 分），一分钟锁定；
> 根因是我为了避开文件占用只做了 `-t:Compile`（只编译不写输出），`bin` 里仍是旧 exe，
> **加代码 ≠ 生效，编译器写过去才算**。
> 防复发我不写"下次注意"，而是**让产物自报身份 + 主动报警**：启动横幅打出上一次编译时间，
> **源码比产物新就直接喊"你跑的是旧的 exe"**（因为这个项目是双击 exe 启动的，双击永远不编译），
> 同时让它把**真正读到的配置值**逐条念出来 —— 以后这类问题从"猜"变成"看一眼"。

---

## ⑧ 同一个坑的**第二次**（2026-09-26）：狼王血量改了没用

### 现象（负责人原话）

> 「为什么我直接在 SO 中修改了狼王的 HP 为 1000，测试时狼王还是 2000？」

### 先把三份摆出来（**这次我不靠记忆，直接读文件**）

| 文件 | 6003 狼王 `hp` | 谁读它 |
| --- | --- | --- |
| `Configs\Design\Monster.csv`（**真源**） | **2000** | **服务端**（`ServerTables` → `CsvSheet.LoadMany` 读源 CSV） |
| `Game\Config\Generated\Monster.tsv`（生成物） | 2000 | Unity「导入 TSV」菜单的输入 |
| `Configs\MonsterConfig.asset`（**改的就是它**，mtime 12:49） | **1000** | Unity 侧 `ConfigMgr`（客户端数值/UI） |

⇒ **服务端根本看不到 SO**，所以它的狼王永远是 2000。**不是缓存、不是没重启、不是 bug。**

### 为什么会长成这样（不是设计失误，是取舍）

服务端是**纯 .NET**、复用不了 Unity 的资产：生成的 `<表>Config.cs` 里 `using UnityEngine`
（继承 `ScriptableObject`）⇒ 进不了服务端（与 D14 那条"看整条依赖链"同源）。
所以服务端**读源 CSV**，Unity 侧**读生成物 SO** —— 这份"两份读表实现"是**有意**的，
代价就是**改了其中一份、另一份不会变，而且不报错**。

### 正确流程（两条路，按你想影响谁选）

| 你想改的是 | 改哪里 | 之后要做什么 |
| --- | --- | --- |
| **服务端行为**（血量、攻击、移速、掉落、AI 数值） | `Configs\Design\*.csv` | **重启服务端即可**（CSV 是**运行时读**的，**不用重新编译**） |
| **Unity 侧数值**（客户端读的配置、UI 显示） | `Configs\Design\*.csv`（**不要**改 SO） | 跑 `ConfigKit.Cli` 生成 → Unity 菜单 `Tools/NBC/配置表/导入 TSV → ScriptableObject` |

> ⚠️ **手改 SO 一定会丢**：它是生成物，下次「导入 TSV」按 `id` 覆盖。而且它和服务端**本来就不是同一份**。
> 本次现场就留下了"三份不一致"（CSV 2000 / TSV 2000 / SO 1000）—— 要么把 CSV 改掉再重新生成，
> 要么把 SO 改回 2000，**别让它们继续分叉**。

> 📌 **后续（同一天，负责人确认）**：他要的就是 **1000** ⇒ 已按"正确流程"把**源 CSV** 改成 1000，
> 并跑 ConfigKit 重新生成（`Monster.tsv` / `Monster.json` 跟着变）。现在**三份一致，都是 1000**，
> 上面那两张表记的是**当时**的值（保留现场，不回头改历史）。
> 服务端重启即生效 —— **CSV 是运行时读的，不用重新编译**。

### 防复发（本次新加的两道机械检查）

| 检查 | 抓什么 | 证据 |
| --- | --- | --- |
| **服务端启动自述阵容数值**（`ServerTables.DescribeMonster/DescribeHero`） | 横幅直接念出"服务端**真正读到**的 hp/攻/移速"—— 上次只念掉落，这次把**数值**也念出来（同一个坑第二次，说明"只防一处"不够） | 启动日志：`阵容：怪 6003 狼王 hp 1000 / 攻 60`、`阵容：英雄 1001 剑士 hp 1200 / 移速 5000mm/s`（削血前它是 `hp 2000`） |
| **编辑器「检查」菜单改成逐字段比内容**（`ConfigImporter.CompareRows`） | **手改 SO**：原来"只报告"模式**只验证 TSV 能不能导入**，从不与现有资产比对 ⇒ 手改永远抓不到；现在按 `id` 建索引逐字段比，不一致就点名"哪张表 / 哪个 id / 哪个字段 / 资产值 vs 源表值" | 改 SO 后点 `Tools/NBC/配置表/检查（只报告，不写资产）`，报 `MonsterConfig.asset 与源表不一致：id=6003 字段 hp：资产=1000，源表=2000`；一致时报"与源表一致" |

> ⚠️ **"比时间"抓不到这一类**：手改之后 SO 比源表**更新**，所以按 mtime 的
> `ConfigStalenessCheck`（那套检查的是"忘了点导入"）会放过它。**判据必须与要抓的现象匹配** ——
> 想抓"内容被改"就只能**比内容**。

探针侧也钉了一条：`Tables_SelfReportMatchesTheSourceCsv` —— 直接读 `Monster.csv` 文本，
断言"服务端自述的 hp/攻 = CSV 里写的"（**不抄数字**，这样也顺带防"表改了、自述没跟上"）。

### 一句话讲清（面试版）

> 改了配置没生效，第一步不是猜缓存，而是**问"我改的是哪一份、运行时读的是哪一份"**：
> 这个项目里服务端读**源 CSV**、Unity 读**生成物 SO**，两份本来就是不同文件。
> 我把这件事做成两个机械检查：服务端启动**自述**它读到的数值（改错地方一眼可见），
> 编辑器检查菜单**逐字段比内容**（手改 SO 当场报差异）——
> 而且特意**不比时间**：手改的资产比源表更新，按 mtime 的检查会放过它。
